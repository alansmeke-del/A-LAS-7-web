# Auditoría web — 2026-09-24

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable directamente en esta corrida — el entorno de este sesión tiene bloqueado el egress de red hacia `alas7.com` (política de red del contenedor, no un fallo del sitio). Señal indirecta: el último build de GitHub Pages (ver abajo) corresponde exactamente al HEAD actual de `main` y terminó en éxito, así que el contenido desplegado debería coincidir con el repo. Recomiendo verificación manual directa (`curl -I https://alas7.com`) desde fuera de este entorno.

**Pago y promo:** ok en el código, no verificable en vivo (mismo bloqueo de red que arriba — `buy.stripe.com` y el proxy de Railway también están bloqueados desde esta sesión).
- El botón/CTA de Premium (`checkPremiumPrivacy` → `window.location.href`) y el link de promo (`#promoCta`) apuntan correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (index.html:361,391).
- El JS llama a `POST /generate-promo` con header `X-ALAS7-Secret` (index.html:508) y a `POST /subscribe` con el payload esperado (`email`, `groups`, `referral_code`) (index.html:447-451), ambos contra `gentle-laughter-production-7f2c.up.railway.app`. El cableado en el HTML es correcto.
- **Hallazgo crítico de seguridad (no nuevo esta corrida, pero sigue vivo en producción):** el token Bearer completo de la API de MailerLite y el secreto `X-ALAS7-Secret` (`alas7secret2026`) están hardcodeados en texto plano dentro del `<script>` del propio `index.html` (líneas 393 y 395), visibles para cualquiera con "ver código fuente". El JWT casi no expira (exp ≈ año 2126). Esto permite que cualquier visitante extraiga el secreto y genere códigos de promoción ilimitados (rompiendo la lógica de "un código por 5 minutos"), y potencialmente use el Bearer token para llamar directamente a la API de MailerLite. Introducido en el commit `13289ab` (14-may-2026); sigue desplegado en HEAD de `main`.

**Deploy Pages:** al día. El último build de Pages (run #35, commit `535f932` "Trigger Pages rebuild") corresponde al HEAD actual de `main` y terminó en `success` en 36s (2026-07-04 06:11–06:12 UTC). No hay commits nuevos en `main` desde entonces, así que no hay drift entre el HTML en el repo y lo desplegado. El incidente del 3-jul (build atorado/no disparado) ya quedó resuelto por ese mismo commit — el run anterior (#34) sí falló, pero el siguiente intento tuvo éxito.

**Problemas encontrados:**
1. **(Crítico, pre-existente)** Bearer token de MailerLite y secreto de promo expuestos en claro en el JS del cliente (index.html:393,395,508) — ver detalle arriba.
2. No se pudo verificar en vivo la salud de `alas7.com`, Stripe ni el proxy de Railway porque la política de red de este entorno de auditoría bloquea esos dominios (connect_rejected / 403 del proxy de egress). Esto es una limitación del entorno de esta sesión, no evidencia de que el sitio esté caído.
3. Los 3 íconos de redes sociales en el footer (Instagram/X/LinkedIn) usan `href="#"` sin URL real (index.html:321-323) — enlaces muertos/placeholder.
4. La sección de testimonios sigue con contenido de relleno ("Espacio reservado para un testimonio real...", "Nombre del lector") en las 3 tarjetas (index.html:264-278).
5. El footer muestra `[ Dirección física — CDMX, México ]` entre corchetes como placeholder, no una dirección real (index.html:329) — puede ser relevante para cumplimiento de aviso de privacidad / anti-spam.
6. Manejo de error silencioso: en `showPromo()` (index.html:506-513), si falla el `fetch` a `/generate-promo`, el catch cae silenciosamente a un código fijo `'ALAS7-2026'` sin registrar ni avisar — una caída del backend de promos pasaría desapercibida.
7. Cosmético: la clave de `sessionStorage` sigue llamándose `vita_promo_seen` (index.html:396), resto de marca antigua ("Vita") que ya se renombró en otras partes (el header del secreto de promo sí se renombró a `X-ALAS7-Secret` en un commit anterior).
8. No hay errores de sintaxis en el JS (`node --check` sobre el bloque `<script>` extraído: OK). El `CNAME` es correcto (`alas7.com`). No se encontraron referencias a `alas7.mx` ni otros dominios inexistentes. Las páginas legales (`aviso-de-privacidad.html`, `terminos.html`) enlazadas desde el footer y los modales existen en el repo.

**Recomendación:** (PERO NO SE CORRIGIÓ — solo se documenta)
- Prioridad 1: mover la generación de promos y la suscripción a MailerLite completamente al backend (Railway), de forma que el Bearer token y el secreto `X-ALAS7-Secret` nunca viajen al cliente. Rotar ambos secretos en cuanto se haga el cambio, ya que ya están expuestos públicamente en el HTML servido.
- Verificar manualmente `https://alas7.com`, el endpoint `/generate-promo` y el link de Stripe desde una red sin las restricciones de este entorno de auditoría, para confirmar salud en vivo.
- Completar o quitar los enlaces placeholder de redes sociales, reemplazar los testimonios de relleno por reales (o quitarlos), y poner la dirección física real en el footer.
- Opcional: renombrar la clave `vita_promo_seen` a algo con la marca actual, por consistencia.
