# Auditoría web — 2026-07-20

## [08:12 CDMX] Auditor web
**Estado:** problemas encontrados
**Sitio en vivo:** no verificable — la política de egress de este entorno de auditoría bloquea (403 en el CONNECT) las conexiones salientes a `alas7.com`, `www.alas7.com`, `buy.stripe.com` y el subdominio de Railway. No es evidencia de que el sitio esté caído, solo que este sandbox no puede confirmarlo. Igual que en todas las corridas anteriores (08-jul a 19-jul).
**Pago y promo:** ok por inspección estática — `index.html` referencia el Stripe Payment Link correcto (`https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, líneas 361/391), el endpoint `POST /generate-promo` con header `X-ALAS7-Secret` (línea 508) y el `POST /subscribe` (línea 447), ambos contra `https://gentle-laughter-production-7f2c.up.railway.app`. No se pudo probar en vivo por el bloqueo de red del punto anterior.
**Deploy Pages:** al día — el último build de Pages (`pages build and deployment`, run `28697306776`, 2026-07-04T06:11:27Z) terminó en `success` y corresponde exactamente al commit HEAD actual de `main` (`535f932`, "Trigger Pages rebuild (previous build stuck)"). No hay commits nuevos en `main` desde el 2026-07-04 — trece días sin cambios de código. El incidente del build atorado del 3-jul sigue resuelto.
**Problemas encontrados:**
1. **(Crítico, recurrente — sin cambios desde el 2026-07-08, 13 corridas seguidas)** `index.html` líneas 393 y 395 exponen en texto plano, en el JS del cliente, el secreto `X-ALAS7-Secret` (`alas7secret2026`) y un JWT Bearer completo de la API de MailerLite (`sub: "2317050"`, `scopes: []`, `exp` ≈ año 2126 — prácticamente sin expiración). Cualquiera puede verlos con "ver código fuente" y usarlos para: (a) llamar la API de MailerLite directamente con ese Bearer, o (b) golpear `/generate-promo` con el secreto expuesto para generar códigos de descuento arbitrarios sin pasar por la landing. Este hallazgo se ha reportado sin cambios en los PRs #1 al #12 (2026-07-08 → 2026-07-19), todos siguen abiertos y sin mergear — ninguno se ha atendido.
2. **(Menor, recurrente)** `showPromo()` (línea 506-513): el `catch` del fetch a `/generate-promo` no registra nada (sin `console.error`) y cae en silencio a un código fijo `ALAS7-2026`. Si el endpoint de Railway se cayera, no habría ninguna señal visible de que la promo dinámica dejó de funcionar.
3. **(Cosmético, recurrente)** 3 íconos de redes sociales en el footer (IG, X, LinkedIn — líneas 321-323) tienen `href="#"` sin URL real ni handler de JS.
4. **Sin novedad:** `node --check` sobre el único bloque `<script>` inline no reportó errores de sintaxis. `CNAME` correcto (`alas7.com`). No se encontraron dominios rotos ni referencias a `alas7.mx` u otros dominios inexistentes.

**Recomendación:** (no aplicada — solo diagnóstico)
1. Sacar `SECRET` y `BEARER` del cliente: mover la generación de código promo y la suscripción detrás del proxy de Railway (que ya existe), sin que el navegador necesite conocer ningún secreto. Rotar ambas credenciales — el JWT filtrado sigue siendo válido hoy, 13 días después de reportado por primera vez.
2. Este es el reporte número 13 del mismo hallazgo crítico sin ninguna acción. Hay 12 PRs de auditoría abiertos (#1–#12) acumulados sin revisar. Vale la pena que Alan revise el backlog de PRs de `claude/audit-web-*` — o decidir explícitamente posponer el fix, para que el auditor deje de re-reportarlo a diario.
3. Dar acceso de red desde este entorno de auditoría hacia `alas7.com`, `buy.stripe.com` y el subdominio de Railway (o complementar con un monitor de uptime externo) para poder verificar el sitio en vivo en cada corrida, en vez de depender solo de inspección estática del código.
