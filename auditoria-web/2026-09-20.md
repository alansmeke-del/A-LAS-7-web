# Auditoría web — 2026-09-20

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno — el proxy de salida de esta sesión bloqueó por política (403) todas las conexiones salientes a `alas7.com`, `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app`. No pude confirmar directamente si alas7.com responde 200. Como evidencia indirecta: el último build de GitHub Pages (run #35, commit `535f932`, 2026-07-04) terminó en `success` y ese commit es idéntico al HEAD actual de `main`, así que el deploy publicado debería reflejar el código actual — pero esto no sustituye una verificación HTTP real.

**Pago y promo:** Cableado correctamente en el código, pero tampoco pude probar la conectividad real por el mismo bloqueo de red.
- El botón Premium (`checkPremium()` → `window.location.href`) apunta a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, coincide con el Payment Link esperado.
- El JS llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret` y a `POST {API}/subscribe` con `Authorization: Bearer …`, ambos contra `https://gentle-laughter-production-7f2c.up.railway.app`, como se espera.
- **Hallazgo de seguridad (nuevo, no reportado antes):** el bearer token de MailerLite (JWT completo, `exp` ~año 2126, prácticamente sin expiración) y el secreto `X-ALAS7-Secret` (`alas7secret2026`) están **hardcodeados en texto plano en el `<script>` de `index.html`**, visibles para cualquiera que abra "ver código fuente". Cualquiera puede copiar ese bearer y llamar la API de MailerLite directamente (agregar suscriptores al grupo, etc.) sin pasar por el formulario, y el secreto de `/generate-promo` no protege nada porque está publicado junto con el resto del HTML. No es una caída del sitio, pero sí una exposición de credenciales de producción que vale la pena rotar/mover a un proxy sin credenciales expuestas.

**Deploy Pages:** Al día. El incidente del 3-jul (build que no se disparó y otro que quedó colgado 29 min) fue resuelto el 4-jul con el commit "Trigger Pages rebuild (previous build stuck)" (run #35), que sí completó con `conclusion: success`. No hay commits nuevos en `main` desde entonces (2026-07-04), y no hay builds fallidos o atorados pendientes en el historial reciente de Actions.

**Problemas encontrados:**
1. No se pudo verificar la salud real del sitio en vivo ni la conectividad de Stripe/Railway desde este entorno de auditoría — acceso de red saliente bloqueado por política del proxy (`connect_rejected`, 403). Esto es una limitación del entorno del auditor, no necesariamente un problema del sitio.
2. Bearer token de MailerLite y secreto `X-ALAS7-Secret` expuestos en texto plano en el JS público de `index.html` (línea ~393-395).
3. Los enlaces de redes sociales en el footer (Instagram, X, LinkedIn) son placeholders `href="#"` sin URL real — no rotos técnicamente (son manejados como anclas), pero no llevan a ningún perfil.
4. Documentación desactualizada: CLAUDE.md menciona funciones `toggleFreeBtn()` y `checkPremiumPrivacy()`, pero el código actual usa listeners anónimos y una función `checkPremium()` — no afecta el funcionamiento, pero la doc no coincide con el código.

**Sin hallazgos en:** sintaxis del JS (`node --check` pasa limpio), CNAME (dice correctamente `alas7.com`), no hay referencias a `alas7.mx` ni otros dominios inexistentes, todos los assets referenciados (`assets/logo7-v2.png`) existen en el repo, y no hay bloques try/catch que oculten errores de forma sospechosa (el catch de `/generate-promo` cae a un código de respaldo visible al usuario, y el catch de `/subscribe` sí muestra un mensaje de error al usuario).

**Recomendación:** (PERO NO SE ARREGLÓ — solo diagnóstico)
- Prioridad alta: rotar el bearer token de MailerLite y el secreto de promo, y moverlos detrás del proxy de Railway en vez de incluirlos en el HTML público (el frontend no debería necesitar el bearer directamente; el proxy podría inyectarlo server-side).
- Verificar manualmente (o desde un entorno con salida a internet) que alas7.com, el endpoint `/generate-promo` y el checkout de Stripe respondan correctamente, ya que este auditor no pudo confirmarlo por restricciones de red del entorno.
- Opcional/bajo: reemplazar los `href="#"` de redes sociales por URLs reales o quitarlos si no existen esas cuentas todavía.
- Opcional/bajo: actualizar CLAUDE.md para que los nombres de función coincidan con el código actual.
