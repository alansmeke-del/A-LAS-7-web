## [08:16 CDMX] Auditor web
**Estado:** problemas encontrados
**Sitio en vivo:** no verificable — el egress de red de este entorno bloquea (403 en el gateway) las conexiones salientes a `alas7.com`, `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app`. No es evidencia de que el sitio esté caído, solo que este auditor no pudo comprobarlo directamente esta corrida.
**Pago y promo:** ok (revisión estática de código)
- El botón Premium (`.js-premium`) apunta correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` tras pasar el gate de privacidad (`checkPremium()` → `window.location.href`).
- `showPromo()` llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret`, `API` = `https://gentle-laughter-production-7f2c.up.railway.app` — coincide con lo documentado en `CLAUDE.md`.
- El formulario de suscripción gratuita (`submitFree()`) hace `POST {API}/subscribe` con `Authorization: Bearer …` y el `groups` de MailerLite (`187372665902728776`) — cableado correctamente.
**Deploy Pages:** al día — el último workflow run de "pages build and deployment" (run #35, commit `535f9320`) terminó en `success` el 2026-07-04 y coincide exactamente con el HEAD actual de `main`. No hay commits pendientes de desplegar. El incidente del 3-jul (build no disparado / colgado 29 min) sigue resuelto, sin builds fallidos ni atorados desde entonces.
**Problemas encontrados:**
1. **(Crítico, seguridad, persiste ~41 días sin corregir)** El Bearer JWT de la API de MailerLite (`index.html:395`) y el secreto `X-ALAS7-Secret = 'alas7secret2026'` (`index.html:393`) siguen hardcodeados en texto plano dentro del JS del cliente, visibles para cualquier visitante vía "ver código fuente". El JWT decodificado tiene `exp` ≈ año 2126 (prácticamente no expira). Cualquiera puede extraer estas credenciales y llamar directamente a la API de MailerLite o al endpoint `/generate-promo` fuera del flujo normal del sitio. Este mismo hallazgo aparece reportado sin corregir en las auditorías desde 2026-07-14 (PR #7) hasta hoy — al menos 30 PRs abiertos y sin mergear (`claude/audit-web-2026-07-14` … `claude/audit-web-2026-08-23`) documentan el mismo problema.
2. **(Menor, sin cambios)** `showPromo()` traga en silencio los errores de `fetch('{API}/generate-promo')` y cae a un código fijo `'ALAS7-2026'` sin loguear ni avisar — si el endpoint de Railway cae, nadie se entera por este camino.
3. **(Menor, sin cambios)** Los íconos de redes sociales del footer (Instagram, X, LinkedIn) usan `href="#"` como placeholder, sin destino real.
4. **(Higiene, sin cambios)** `SESSION_KEY = 'vita_promo_seen'` conserva el naming previo "Vita" pese al rebrand a "A LAS 7".

Verificaciones sin hallazgos:
- `node --check` sobre el JS embebido de `index.html`: sin errores de sintaxis.
- `CNAME`: correcto, apunta a `alas7.com` (no `vitanews`, no `alas7.mx`).
- Enlaces internos (`aviso-de-privacidad.html`, `terminos.html`, `assets/logo7-v2.png`) referenciados desde el HTML: todos existen en el repo.
- Sin referencias a dominios inexistentes (`alas7.mx`, `vitanews`, `wek.com`).

**Recomendación:** mover el Bearer JWT de MailerLite y el secreto `X-ALAS7-Secret` al backend de Railway (que ya actúa de proxy) en vez de exponerlos en el JS del cliente, y rotar el JWT actual ya que quedó expuesto públicamente durante más de un mes. Registrar/loguear el fallo de `/generate-promo` en vez de sustituirlo en silencio. Sustituir los `href="#"` del footer por URLs reales o quitar los íconos. Dado que hay 30+ PRs de auditoría idénticos sin mergear, considerar revisar y cerrar los que ya no aporten información nueva, o decidir de una vez la remediación del hallazgo #1 para dejar de generarlo a diario. PERO NO LO ARREGLES — esto queda a criterio de Alan.
