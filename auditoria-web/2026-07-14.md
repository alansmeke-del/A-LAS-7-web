## [08:10 CDMX] Auditor web
**Estado:** problemas encontrados
**Sitio en vivo:** No se pudo verificar. El egress de red de esta sesión de auditoría bloquea (403 en el CONNECT) las conexiones salientes a `alas7.com`, `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app` — es una política de red del entorno de ejecución, no un indicio de que el sitio esté caído. Se recomienda repetir esta verificación desde un entorno con salida a esos dominios.
**Pago y promo:** No se pudo probar en vivo (mismo bloqueo de red). Verificación estática del HTML/JS:
- El botón Premium (`checkPremiumPrivacy()` → `window.location.href`) sí apunta al Stripe Payment Link correcto: `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`.
- El JS sigue llamando correctamente a `POST {API}/subscribe` y `POST {API}/generate-promo` con `API = https://gentle-laughter-production-7f2c.up.railway.app`, tal como está documentado.
- **Hallazgo crítico:** en `index.html` (líneas ~393-395) hay un **Bearer JWT completo y el secreto compartido `alas7secret2026` hardcodeados en texto plano** dentro del `<script>` público. Son visibles para cualquiera que abra "ver código fuente" de la página. El JWT trae un `exp` equivalente al año ~2126, es decir que en la práctica no expira. Con ese token, cualquiera podría llamar directamente al backend de Railway/MailerLite (agregar suscriptores, generar códigos promo) sin pasar por el checkbox de privacidad ni por ningún límite de la landing.
**Deploy Pages:** al día. El build más reciente de GitHub Pages (run `28697306776`, 2026-07-04 06:11 UTC) terminó en `success` sobre el commit `535f9320` ("Trigger Pages rebuild"), que es exactamente el HEAD actual de `origin/main`. El incidente del 3-jul (un build que no se disparó y otro que quedó colgado 29 min) ya quedó resuelto con ese commit de rebuild — no hay desfase entre el HTML publicado y `main`.
**Problemas encontrados:**
1. [CRÍTICO] Bearer token JWT y secreto `alas7secret2026` hardcodeados en el JS público de `index.html` (líneas 393 y 395), expuestos a cualquier visitante.
2. [No verificable hoy] No se pudo confirmar en vivo que `alas7.com` responda 200 ni que Stripe/Railway respondan — bloqueado por política de egress de este entorno de auditoría.
3. [Menor] Los íconos de redes sociales del footer (Instagram, X, LinkedIn) usan `href="#"` sin URL real; parecen placeholders pendientes de completar.
4. [Cosmético] La clave de `sessionStorage` se llama `vita_promo_seen` — residuo del nombre de un proyecto anterior ("Vita"), no afecta funcionalidad pero es una inconsistencia de naming.
**Recomendación:** Rotar el Bearer token de MailerLite y el secreto del endpoint de promo, y sacarlos del HTML público — deberían vivir solo en el servidor (Railway), nunca en el cliente. Es el hallazgo más urgente porque expone datos de suscriptores y el mecanismo de promociones a cualquiera que vea el código fuente. También conviene repetir esta auditoría desde un entorno con salida de red permitida a alas7.com/Stripe/Railway para confirmar la salud en vivo, ya que hoy no fue posible. No se hizo ningún cambio de código — esto lo decide Alan.
