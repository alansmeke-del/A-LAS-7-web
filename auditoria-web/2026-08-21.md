# Auditoría web — 2026-08-21

## [08:18 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno — el proxy de salida de red de la sesión bloquea (403) las conexiones directas a `alas7.com`, `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app`. No es evidencia de que el sitio esté caído; es una limitación de este auditor automatizado que debe resolverse (permitir esos dominios en la política de salida) para que futuras corridas puedan confirmar el estado en vivo.

**Pago y promo:** Cableado en el HTML correcto, pero con un hallazgo de seguridad serio:
- El botón premium apunta al Payment Link real de Stripe: `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (index.html:361, 391).
- El endpoint de promo se sigue llamando correctamente: `POST {API}/generate-promo` con header `X-ALAS7-Secret` (index.html:508 / script inline línea ~123).
- El formulario gratuito postea a `POST {API}/subscribe` (index.html:447) y sí incluye `referral_code`.
- **Hallazgo de seguridad:** el token Bearer completo de la API de MailerLite (JWT largo, expira en el año 2126) está hardcodeado en texto plano en el JavaScript del cliente (`const BEARER = 'eyJ0eXAi...'`, dentro del `<script>` al final de index.html). Cualquiera que vea el código fuente de la página puede extraer este token y usarlo para llamar directamente a la API de MailerLite (leer/añadir/borrar suscriptores u otras acciones según el alcance del token). Esto es más grave que un simple detalle de código porque compromete la base de datos de suscriptores, no solo la landing.
- **Hallazgo secundario (manejo de errores silencioso, punto 5 de la auditoría):** el `catch` del fetch a `/generate-promo` no registra el error en ningún lado (sin `console.error`, sin telemetría) y cae en silencio a un código promocional fijo `'ALAS7-2026'`. Si el endpoint de Railway se cae, el usuario nunca ve un error y recibe un código que podría no existir en Stripe, fallando el checkout sin que nadie se entere.

**Deploy Pages:** Al día. El último build de Pages (run #35, `pages build and deployment`, id 28697306776) completó con éxito el 2026-07-04 06:12 UTC para el commit `535f932` ("Trigger Pages rebuild"), que es el HEAD actual de `main`. No hay build atorado ni desfasado en este momento — el incidente del 3-jul (build no disparado / colgado 29 min) ya quedó resuelto por ese rebuild manual.

**Problemas encontrados:**
1. [Seguridad — alto] Token Bearer de MailerLite expuesto en texto plano en el JS del cliente de index.html.
2. [Manejo de errores — medio] El catch de `/generate-promo` falla en silencio sin log ni aviso, enmascarando una posible caída del endpoint de promo.
3. [UX menor] Los íconos de redes sociales del footer (Instagram, X, LinkedIn) usan `href="#"` sin ningún manejador de JS asociado — son enlaces muertos que no llevan a ningún perfil real (index.html:321-323).
4. [Cosmético] La constante `SESSION_KEY = 'vita_promo_seen'` conserva el nombre de marca anterior ("Vita News"), no afecta funcionalidad.
5. [Limitación del auditor] No se pudo verificar en vivo alas7.com, el Payment Link de Stripe ni el endpoint de Railway por bloqueo de red de salida en este entorno (403 del proxy de la sesión a esos dominios).

**Verificaciones sin hallazgos:**
- `node --check` sobre el JS embebido: sintaxis válida, sin errores.
- `CNAME`: contiene `alas7.com` correctamente (no quedó ningún residuo de dominios anteriores).
- `aviso-de-privacidad.html` y `terminos.html` existen en el repo y coinciden con los enlaces internos del footer/checkboxes.
- El HTML en vivo esperado (según el build de Pages) coincide con el HEAD de `main`.

**Recomendación:** (PERO NO SE CORRIGIÓ, solo se documenta)
1. Rotar de inmediato el token Bearer de MailerLite y moverlo detrás del proxy de Railway (que la landing llame a `/subscribe` en el proxy, y que el proxy — no el navegador — sea quien tenga el Bearer/API key). Es el hallazgo más urgente de esta corrida.
2. Añadir logging visible (aunque sea `console.error` + un aviso sutil o reintento) al catch de `/generate-promo` para poder detectar caídas del endpoint sin depender de que un usuario reporte el problema.
3. Quitar o enlazar los íconos de redes sociales del footer a los perfiles reales, o eliminarlos si no existen todavía.
4. Pedir que se habilite egress de red a alas7.com / buy.stripe.com / el dominio de Railway para este auditor automatizado, para poder confirmar el estado en vivo en próximas corridas.
