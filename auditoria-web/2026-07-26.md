# Auditoría web — 2026-07-26

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno — el proxy de red del sandbox rechazó (403 en el CONNECT) las conexiones salientes a `alas7.com`, `buy.stripe.com` y el endpoint de Railway. Esto es una restricción de política de red del entorno de auditoría, **no evidencia de que el sitio esté caído**. Recomiendo una verificación manual (`curl -I https://alas7.com`) desde una red sin ese bloqueo.

**Pago y promo:** Correcto a nivel de código, con un hallazgo de seguridad grave.
- El botón Premium (`index.html:361,391`) apunta al Stripe Payment Link correcto (`https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`) y `checkPremium()` redirige ahí tras pasar la validación de privacidad.
- El JS llama correctamente a `API + '/generate-promo'` (línea 508) y `API + '/subscribe'` (línea 447), con `API = 'https://gentle-laughter-production-7f2c.up.railway.app'`. No se pudo probar la respuesta en vivo por el mismo bloqueo de red del punto anterior.
- **HALLAZGO — credenciales expuestas en el JS público (`index.html:393,395`):** el script embebido tiene en texto plano un **Bearer token JWT completo de MailerLite** (`exp` decodificado = año **2126**, prácticamente sin caducidad, `scopes: []`) y un secreto `alas7secret2026` usado como header `X-ALAS7-Secret` para `/generate-promo`. Cualquiera que abra "ver código fuente" de la página puede copiar estas credenciales y usarlas directamente contra la API de MailerLite o para generar códigos promo arbitrarios sin pasar por el flujo de la página. Es un riesgo real porque toca dinero (promo/checkout) y datos de suscriptores.

**Deploy Pages:** Al día. El último build (`run 28697306776`, 2026-07-04T06:11:27Z) fue exitoso y corresponde exactamente al commit `535f932` (HEAD actual de `main`). Hubo un build fallido previo (`1c23348`, 05:42:45Z) pero fue superado por el rebuild exitoso — consistente con el commit `535f932 "Trigger Pages rebuild (previous build stuck)"`. No hay builds atorados actualmente.

**Problemas encontrados:**
1. **(Grave, seguridad)** Bearer token de MailerLite sin expiración práctica y secreto `X-ALAS7-Secret` hardcodeados en el HTML/JS público (`index.html:393,395`).
2. **(Info, no bloqueante)** Verificación en vivo de `alas7.com`, Stripe y el endpoint de Railway no fue posible desde este entorno por política de red del sandbox de auditoría (403 en el proxy saliente) — no se pudo confirmar HTTP 200 real.
3. **(Menor, cosmético)** Los íconos sociales (Instagram, X, LinkedIn, líneas 321-323) y el link "Suscríbete gratis" del footer (línea 308) usan `href="#"` — los tres primeros son placeholders sin URL real de red social; el cuarto es intencional (abre el modal vía JS). No afecta conversión.

**Verificaciones sin hallazgos:**
- `node --check` sobre el script embebido: sin errores de sintaxis.
- Los 21 IDs de DOM referenciados por el JS existen todos en el HTML (sin riesgo de `null` reference).
- CNAME correcto: `alas7.com` (no `vitanews`).
- Sin referencias a dominios inexistentes (`alas7.mx` u otros).
- `main` local coincide con `origin/main`, sin desfase de commits sin desplegar.

**Recomendación:** (PERO NO SE CORRIGIÓ — solo se avisa)
1. Rotar el token Bearer de MailerLite y el secreto `alas7secret2026` inmediatamente, y mover ambas credenciales a la capa del proxy Railway (que ya existe) en vez de exponerlas en el HTML público — el navegador no debería necesitar más que una llamada sin credenciales sensibles hardcodeadas al proxy propio.
2. Confirmar manualmente desde una red sin restricciones que `alas7.com`, el Stripe Payment Link y el endpoint `/subscribe` y `/generate-promo` responden 200 en vivo, ya que este auditor no pudo verificarlo por la política de red del entorno.
3. (Opcional, cosmético) Añadir URLs reales a los íconos de redes sociales del footer o quitarlos si aún no existen esas cuentas.
