# Auditoría web — 2026-09-10

## [08:13 CDMX] Auditor web

**Estado:** problemas encontrados (no críticos) + una verificación bloqueada

**Sitio en vivo:** NO VERIFICABLE — el proxy de salida de este entorno de auditoría
rechazó las conexiones HTTPS a `alas7.com` (`connect_rejected`, política de red del
entorno/organización, no un error del sitio). No se pudo confirmar código de
respuesta HTTP. Esto es una limitación del entorno de esta corrida, no evidencia
de que el sitio esté caído — pero significa que el chequeo más importante del día
quedó sin confirmar.

**Pago y promo:** revisado en código, no verificado en vivo (mismo bloqueo de red
para `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app`).
- El botón de pago (`#promoCta`, línea 361) apunta correctamente a
  `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, coincide con el Payment Link
  esperado.
- El JS llama correctamente a `API + '/generate-promo'` (línea 508) y
  `API + '/subscribe'` (línea 447), con `API` apuntando al proxy de Railway
  correcto.
- **Hallazgo:** el secreto `X-ALAS7-Secret` está hardcodeado en texto plano en el
  JS del cliente (`const SECRET = 'alas7secret2026'`, línea 393), visible en el
  código fuente de la página para cualquier visitante. Esto no es nuevo (viene
  desde junio), pero permite a cualquiera generar códigos promo llamando al
  endpoint directamente sin pasar por el flujo de la landing.
- **Hallazgo (manejo de error silencioso):** en el flujo de `generate-promo`
  (línea 507-513), si la llamada falla el `catch` cae silenciosamente a un código
  fijo `'ALAS7-2026'` sin ningún `console.error` ni señal visible. El usuario no
  nota nada raro, pero un fallo real del endpoint de promos quedaría invisible en
  producción. El flujo de `/subscribe` (línea 447-460), en cambio, sí muestra un
  mensaje de error al usuario — ese no es silencioso.

**Deploy Pages:** al día. El último build de Pages (`run #35`, `pages build and
deployment`) terminó con `conclusion: success` y corresponde al commit
`535f932` ("Trigger Pages rebuild (previous build stuck)"), que es el HEAD
actual de `main`. No hay commits nuevos en `main` desde el 2026-07-04 (más de
dos meses), así que no hay desfase entre el HTML publicado y el último commit.
El problema histórico de builds atorados/no disparados (3-jul) ya quedó resuelto
por ese mismo commit de rebuild.

**Problemas encontrados:**
1. No se pudo verificar en vivo `alas7.com`, el Stripe Payment Link, ni el
   endpoint `/generate-promo` — bloqueo de red del entorno de auditoría (no
   confirmado como falla real del sitio; solo una verificación pendiente).
2. Secreto `X-ALAS7-Secret` expuesto en texto plano en el JS del cliente
   (`alas7secret2026`) — cualquiera puede leerlo desde "ver código fuente".
3. `catch` silencioso en `generate-promo` sin logging — un fallo real del
   endpoint de promos no dejaría rastro visible.
4. Cosmético/bajo: 3 iconos sociales en el footer (Instagram, X, LinkedIn,
   líneas 321-323) tienen `href="#"` sin URL real. Los otros `href="#"`
   encontrados (líneas 66, 308) son botones `js-open-free` intencionales que
   abren el modal vía JS, no son enlaces rotos.

**JS/HTML — verificaciones que sí pasaron:**
- `node --check` sobre el bloque `<script>` inline: sin errores de sintaxis.
- `CNAME` contiene exactamente `alas7.com` (no `vitanews`, no `alas7.mx`).
- Sin referencias a dominios inexistentes tipo `alas7.mx` en el HTML/JS.

**Recomendación:** (PERO NO SE CORRIGIÓ NADA — solo se documenta)
- Repetir la verificación en vivo de `alas7.com`, Stripe y el endpoint de
  Railway desde un entorno sin el bloqueo de red visto hoy, para confirmar que
  el sitio y los pagos responden 200 realmente.
- Mover la generación/validación del secreto de promo al backend (Railway) en
  vez de exponerlo en el cliente, o rotar el secreto si ya se considera
  comprometido por estar público en el HTML desde junio.
- Agregar al menos un `console.error(e)` (o telemetría) en el `catch` de
  `generate-promo` para no perder visibilidad de fallos reales del endpoint.
- Opcional/bajo: poner URLs reales o quitar los iconos sociales sin destino en
  el footer.
