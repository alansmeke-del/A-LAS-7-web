# Auditoría web — 2026-08-10

## [08:26 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No se pudo verificar directamente. El entorno sandbox de este auditor bloquea por política de red la salida hacia `alas7.com`, `buy.stripe.com` y el dominio de Railway (`gentle-laughter-production-7f2c.up.railway.app`) — el proxy de egress devuelve `403 policy denial` en las tres conexiones. Esto es una restricción del entorno de ejecución, no evidencia de que el sitio esté caído. Como verificación indirecta: el último build de GitHub Pages sobre `main` terminó en éxito y coincide exactamente (mismo segundo) con el HEAD actual de `main`, así que lo desplegado debería reflejar el código más reciente.

**Pago y promo:** Revisión de código (no se pudo probar en vivo por el bloqueo de red arriba descrito):
- El botón/CTA de pago (`#promoCta`) y la constante `STRIPE` en el JS apuntan correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` ✓.
- La llamada `fetch(API + '/generate-promo', ...)` y `fetch(API + '/subscribe', ...)` están cableadas correctamente hacia el proxy de Railway ✓.
- **Hallazgo de seguridad (no estaba en el checklist pero es crítico dado que "es el dinero"):** el JS inline de `index.html` (líneas ~394-400) trae **hardcodeados en texto plano, visibles a cualquiera que haga "ver código fuente"**:
  - `SECRET = 'alas7secret2026'` — se envía como header `X-ALAS7-Secret` al endpoint `/generate-promo`. Al estar en el cliente, deja de ser un secreto: cualquiera puede copiarlo y llamar el endpoint directamente para generar códigos de promoción ilimitados, sin pasar por la landing ni por el temporizador/urgencia diseñados para limitar el descuento.
  - `BEARER = 'eyJ0eXAiOiJKV1Qi...'` — un JWT enviado como `Authorization: Bearer` en la llamada a `/subscribe`. Al decodificar el payload, tiene `exp: 4933854509` (~año 2126), es decir, prácticamente no expira. Cualquiera puede extraerlo del HTML público y usarlo para llamar el endpoint de suscripción (o potencialmente la API de MailerLite si el token tiene ese scope) fuera del flujo normal de la web — riesgo de abuso de la lista de correos o de la API subyacente.

  Esto rompe el propósito de tener un proxy en Railway (mantener credenciales fuera del cliente): actualmente el cliente sigue exponiendo ambos secretos.

**Deploy Pages:** Al día. Corrida más reciente (`pages build and deployment`, run `28697306776`, 2026-07-04T06:11:27Z) → `success`, y su timestamp coincide al segundo con el commit HEAD de `main` ("Trigger Pages rebuild (previous build stuck)"). El build fallido/atorado del 3-jul ya había sido resuelto en esa misma fecha (commit de rebuild). No hay commits nuevos en `main` desde el 2026-07-04 — nada pendiente de desplegar.

**Problemas encontrados:**
1. **[Crítico/seguridad]** Secretos hardcodeados en el JS público de `index.html`: `SECRET` (header del endpoint de promo) y un `BEARER` JWT de larga duración (exp ~año 2126) para el endpoint de suscripción. Ambos quedan expuestos a cualquier visitante vía "ver código fuente".
2. **[Info/limitación del auditor]** No se pudo confirmar en vivo que `alas7.com`, el Payment Link de Stripe y el endpoint de Railway respondan 200, por bloqueo de red del propio entorno sandbox (no es una falla del sitio).
3. **[Menor]** Los íconos sociales (Instagram, X, LinkedIn) en el footer usan `href="#"` sin URL real — quedan como placeholders sin destino.

**Recomendación:** (PERO NO SE CORRIGIÓ NADA — solo aviso)
- Prioridad alta: mover `SECRET` y el `BEARER` fuera del cliente. El endpoint `/generate-promo` y `/subscribe` deberían aceptar la petición desde el navegador sin credenciales sensibles embebidas — que el proxy de Railway sea quien agregue el Bearer/secret del lado servidor, o proteger esos endpoints con un mecanismo que no dependa de un secreto público (rate limiting por IP, CORS estricto, un token de sesión de corta duración, etc.). Rotar ambas credenciales una vez migradas, ya que las actuales deben considerarse comprometidas al haber estado públicas.
- Verificar manualmente (fuera de este entorno restringido) que alas7.com, el Payment Link de Stripe y `https://gentle-laughter-production-7f2c.up.railway.app/generate-promo` y `/subscribe` respondan correctamente, ya que este auditor no pudo confirmarlo por la política de red del sandbox.
- Opcional/menor: completar o quitar los enlaces sociales del footer (`href="#"`) para que no queden como placeholders vacíos.
