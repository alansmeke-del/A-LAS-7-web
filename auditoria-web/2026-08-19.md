# Auditoría web — 2026-08-19

## [08:18 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No se pudo verificar directamente. El sandbox de esta sesión bloquea TODO el tráfico HTTPS saliente hacia dominios externos (alas7.com, buy.stripe.com, y el proxy de Railway incluidos) a nivel de política de red — no es específico de alas7.com, así que **no es evidencia de que el sitio esté caído**, solo de que este auditor no pudo comprobarlo esta corrida. Recomiendo verificar manualmente o dar acceso de red al auditor.

**Pago y promo:** No se pudo probar en vivo (mismo bloqueo de red). Revisión estática del código:
- El botón Premium (`checkPremiumPrivacy()` → `window.location.href`) apunta correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`. ✓ coincide con el esperado.
- El JS llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret` y a `POST {API}/subscribe` con `Authorization: Bearer …`, ambos contra `https://gentle-laughter-production-7f2c.up.railway.app`. El cableado es correcto y coincide con lo documentado.
- **Hallazgo crítico de seguridad:** el bearer token de la API (JWT) y el secreto `X-ALAS7-Secret` (`alas7secret2026`) están **hardcodeados en texto plano en el `<script>` de `index.html`**, visibles para cualquiera que haga "ver código fuente" en la página en vivo. Decodifiqué el JWT: expira en el año **2126** (`exp: 4933854509`), es decir, no expira en la práctica. Cualquier visitante puede copiar ese bearer token y usarlo para llamar a la API de suscripción (MailerLite) directamente, fuera del formulario — potencial abuso de la lista de suscriptores o gasto de cuota de la API. Esto no es nuevo en esta corrida (viene de commits anteriores), pero no estaba documentado en auditorías previas porque este es el primer archivo de auditoría del repo.

**Deploy Pages:** Al día. Revisé el historial de runs del workflow `pages-build-deployment` vía la API de GitHub:
- El run más reciente (`28697306776`, commit `535f932` "Trigger Pages rebuild (previous build stuck)") completó con `success` el 2026-07-04 06:11–06:12 UTC, y corresponde exactamente al HEAD actual de `main` (mismo SHA `535f932091...`).
- El run inmediatamente anterior (`28696615691`, commit `1c23348`) había fallado (`failure`) a las 05:42, consistente con el incidente del 3-jul mencionado en las instrucciones; ese incidente ya quedó resuelto por el commit de rebuild.
- No hay commits nuevos en `main` desde el 2026-07-04 (hace ~6 semanas), así que no hay riesgo de desalineación HTML-vivo vs. `main` por deploys pendientes.

**Problemas encontrados:**
1. **(Crítico, seguridad)** Bearer token JWT de la API y secreto `X-ALAS7-Secret` expuestos en texto plano en el JS público de `index.html` (líneas ~393–395), con expiración efectiva en 2126.
2. **(Info/no verificable)** No se pudo confirmar en vivo que alas7.com, el Stripe Payment Link y el endpoint de Railway respondan, por bloqueo de red del propio sandbox del auditor — pendiente de verificación manual o de red habilitada.
3. **(Menor)** Los enlaces de redes sociales en el footer (Instagram, X, LinkedIn) son `href="#"` placeholders sin URL real — probablemente intencional hasta tener cuentas, pero vale confirmarlo.
4. **(Menor, cosmético)** La constante `SESSION_KEY = 'vita_promo_seen'` en el JS conserva el nombre del proyecto anterior ("Vita"/vitanews) en vez de "alas7" — inconsistencia de naming, sin impacto funcional pero vale limpiarla en algún refactor.
5. Sintaxis del JS embebido: validada con `node --check` sobre el bloque `<script>` extraído — sin errores. CNAME confirma `alas7.com` (correcto, no "vitanews").

**Recomendación:** (NO SE CORRIGIÓ NADA — solo diagnóstico)
- Prioridad 1: rotar el bearer token de la API y el secreto `X-ALAS7-Secret`, y mover ambos a un proxy/backend que no exponga el secreto al cliente (p. ej. que el frontend llame a un endpoint propio sin credenciales embebidas, y que el backend agregue el bearer del lado servidor). Exponer un token de vida efectiva infinita en HTML público es el hallazgo más serio de esta corrida.
- Verificar manualmente que alas7.com, el link de Stripe y el endpoint de Railway respondan 200, ya que este auditor no tuvo salida de red para confirmarlo.
- Opcional: reemplazar los `href="#"` de redes sociales por URLs reales o quitarlos si no aplican, y renombrar `vita_promo_seen` a algo consistente con la marca actual.
