# Auditoría web — 2026-09-03

## [08:17 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No se pudo verificar. El entorno de ejecución de esta sesión bloquea todo el tráfico saliente hacia `alas7.com`, `buy.stripe.com` y el dominio de Railway a nivel de política de red (el proxy de egreso responde 403 a cualquier CONNECT a esos hosts). No es una señal de que el sitio esté caído — es una limitación del auditor automatizado en este entorno. Recomendación aparte: dar acceso de red saliente a esos tres dominios para que las corridas futuras puedan de verdad comprobar el sitio en vivo.

**Pago y promo:** revisado solo por análisis estático del código (no se pudo probar en vivo):
- El botón/link de pago (`STRIPE` const y `href` del CTA de promo) apunta correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, que coincide con el Payment Link esperado.
- El JS llama correctamente a `POST {API}/generate-promo` con el header `X-ALAS7-Secret`, y `{API}` resuelve a `https://gentle-laughter-production-7f2c.up.railway.app`, igual que lo documentado.
- El formulario de suscripción gratuita hace `POST {API}/subscribe` con el email, el `group` (`187372665902728776`, coincide con CLAUDE.md) y el `referral_code`. Está bien cableado.
- **Hallazgo de seguridad (grave):** en `index.html` línea 395, el JS del cliente incluye un **Bearer token JWT hardcodeado en texto plano** (`const BEARER = '...'`) usado para autenticar el POST a `/subscribe`. Cualquier visitante puede ver el código fuente de la página y copiar este token. El JWT decodificado no tiene expiración práctica (`exp` cae en el año ~2126). Si es un token de API de MailerLite con permisos de escritura, cualquiera podría usarlo directamente contra la API de MailerLite fuera de la página (agregar/borrar suscriptores, agotar límites de tasa, etc.), sin pasar por el proxy de Railway ni por el checkbox de privacidad. Esto es un problema de negocio, no solo de estilo de código.

**Deploy Pages:** al día. El último build de GitHub Pages (run #35, commit `535f9320` "Trigger Pages rebuild (previous build stuck)") terminó en **success** el 2026-07-04, y ese commit es el `HEAD` actual de `main`. El incidente previo del 3-jul (run #34 falló sobre el mismo merge de `dev`, run stuck) ya quedó resuelto por ese rebuild — no es un problema activo, solo antecedente.

**Problemas encontrados:**
1. **[Grave]** Bearer token de API (MailerLite) hardcodeado en el JS del cliente, visible en el código fuente público (`index.html:395`). Sin expiración práctica.
2. **[Menor]** Manejo de error silencioso en `showPromo()` (`index.html:511-513`): si `/generate-promo` falla o no responde, el código simplemente usa el código genérico `'ALAS7-2026'` sin registrar ni avisar el fallo. Si el endpoint de Railway se cae, todos los visitantes recibirían el mismo código no-único de forma silenciosa, y nadie se enteraría de que el servicio está caído a menos que alguien lo reporte manualmente.
3. **[Menor]** Los 3 íconos de redes sociales en el footer (Instagram, X, LinkedIn — líneas 321-323) son `href="#"` sin URL real; son placeholders visuales sin destino.
4. **[Cosmético]** La constante `SESSION_KEY = 'vita_promo_seen'` (línea 396) conserva el nombre de la marca anterior ("Vita News") de un rebrand previo. No afecta funcionalidad, solo higiene de código.

**No se encontraron:** errores de sintaxis en el JS (validado con `node --check`), referencias a dominios inexistentes (p. ej. alas7.mx), ni inconsistencias en el CNAME (dice correctamente `alas7.com`). Las páginas enlazadas (`aviso-de-privacidad.html`, `terminos.html`) y el asset `assets/logo7-v2.png` existen en el repo.

**Recomendación:** (PERO NO SE CORRIGIÓ, solo se reporta)
- Prioridad 1: rotar el Bearer token expuesto y moverlo al backend de Railway (que el navegador nunca lo vea) — el proxy de Railway ya existe para `/subscribe`, así que el JS del cliente no debería necesitar ningún secreto de API.
- Prioridad 2: en el `catch` de `showPromo()`, además de mostrar el código de respaldo, registrar el fallo (p. ej. `console.error` o un beacon a un endpoint de monitoreo) para que un endpoint caído no pase inadvertido.
- Prioridad 3 (opcional): reemplazar los `href="#"` de redes sociales con URLs reales o quitar los íconos si no hay perfiles activos aún.
- Habilitar acceso de red saliente a alas7.com / buy.stripe.com / el dominio de Railway para este auditor automatizado, para poder verificar el sitio en vivo en corridas futuras en lugar de depender solo de análisis estático.
