## [08:14 CDMX] Auditor web
**Estado:** problemas encontrados (hallazgo crítico persiste)
**Sitio en vivo:** no verificado — el egress de red de esta sesión de auditoría bloquea (403 en el proxy de salida) las conexiones a alas7.com, buy.stripe.com y el endpoint de Railway. Esta restricción viene de política del entorno, no del sitio; se repite en corridas anteriores (ver auditorías del 8-08 y 8-26 en adelante).
**Pago y promo:** ok en el código, no verificado en vivo — `index.html` referencia el Stripe Payment Link correcto (`https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, línea 361 y 391) y el JS llama a `API + '/generate-promo'` y `API + '/subscribe'` contra `https://gentle-laughter-production-7f2c.up.railway.app` (líneas 392, 447, 508). No se pudo hacer la petición HTTP real por el bloqueo de red del auditor.
**Deploy Pages:** al día — el HEAD de `main` (`535f932`, commit "Trigger Pages rebuild") coincide con el run #35 de "pages build and deployment", que terminó `completed`/`success`. No hay commits nuevos en `main` desde el 4-jul; no hay builds atorados ni fallidos pendientes.
**Problemas encontrados:**
1. **[CRÍTICO, persiste desde hace semanas]** `index.html` línea 393 y 395 tiene hardcodeados en el JavaScript público un secreto de API (`SECRET = 'alas7secret2026'`, usado como header `X-ALAS7-Secret` para `/generate-promo`) y un **Bearer token JWT** completo (usado para `/subscribe`) cuyo `exp` decodificado es prácticamente perpetuo (año ~2126). Cualquier visitante puede ver el código fuente y copiar ambas credenciales para llamar directamente a los endpoints del proxy sin pasar por el sitio. Este hallazgo aparece reportado sin cambios en al menos 30 auditorías diarias previas (PRs #32 al #61) y sigue sin remediar.
2. **[menor]** Los íconos de redes sociales del footer (Instagram, X, LinkedIn, líneas 321-323) usan `href="#"` sin URL real — enlaces muertos si un visitante los clickea.
3. `node --check` sobre el JS embebido pasa sin errores de sintaxis. CNAME correcto (`alas7.com`, no `vitanews` ni otro dominio). No se encontraron URLs a dominios inexistentes tipo `alas7.mx`.
**Recomendación:** (PERO NO SE APLICÓ)
1. Rotar el Bearer token y el secreto `alas7secret2026` expuestos, y mover la autenticación de `/subscribe` y `/generate-promo` al lado servidor (proxy Railway) en vez de embeberla en el cliente — es explotable hoy tal cual está.
2. Verificar desde un entorno con salida de red sin restricciones que alas7.com, el Stripe Payment Link y los endpoints de Railway responden 200 en vivo.
3. Completar o quitar los `href="#"` de redes sociales del footer.
