# Auditoría web — 2026-09-17

## [08:10 CDMX] Auditor web
**Estado:** problemas encontrados (menores, no bloqueantes)

**Sitio en vivo:** No verificable desde este entorno — la política de red del
sandbox (egress proxy) bloqueó (403) las conexiones salientes a `alas7.com`,
`buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app`. No es un
indicio de que el sitio esté caído; es una restricción del entorno de
auditoría. Se recomienda una verificación manual de `curl -I https://alas7.com`
o revisar el status de GitHub Pages desde un entorno con salida a internet.

**Pago y promo:** ok en código, con 2 hallazgos a vigilar
- El botón Premium y el link del popup de promo apuntan correctamente al
  Stripe Payment Link real (`https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`),
  consistente en las 3 referencias del HTML/JS (`STRIPE`, `checkPremium()`,
  `promoCta`).
- El JS llama a `POST {API}/generate-promo` con el header `X-ALAS7-Secret` y a
  `POST {API}/subscribe` con `Authorization: Bearer …`, apuntando a
  `gentle-laughter-production-7f2c.up.railway.app`, igual que en el último
  commit de producción conocido. No se pudo confirmar que estos endpoints
  respondan (ver bloqueo de red arriba).
- **Hallazgo:** el token `Bearer` (JWT) usado para `/subscribe` está
  hardcodeado en el JS del cliente (`index.html`, línea ~395), visible para
  cualquiera que vea el código fuente de la página. Su campo `exp` decodifica
  a una fecha ~100 años en el futuro (año ~2126), es decir, no expira en la
  práctica. Es una credencial de API viva expuesta públicamente.
- **Hallazgo (manejo de errores silencioso):** en `showPromo()`
  (línea ~506-513), si la llamada a `/generate-promo` falla, el `catch` no
  registra el error en ningún lado (sin `console.error`, sin telemetría) y
  simplemente sustituye un código fijo `'ALAS7-2026'`. Si el backend de promos
  se cae, nadie se entera — el usuario ve un popup funcional con un código que
  probablemente no es válido en Stripe.

**Deploy Pages:** al día
- El último workflow "pages build and deployment" (run #35) para el commit
  `535f932` (HEAD actual de `main`) terminó con `conclusion: success` el
  2026-07-04 06:12 UTC.
- `main` no tiene commits nuevos desde entonces, así que el HTML publicado en
  Pages debería coincidir con el HEAD actual del repo.
- El problema reportado el 3-jul (un build no disparado y otro colgado 29 min,
  runs #33/#34) ya quedó resuelto por el commit "Trigger Pages rebuild" — no
  requiere acción.

**Problemas encontrados:**
1. Token Bearer (JWT, exp. ~año 2126) hardcodeado en el JS público de
   `index.html` — credencial de API expuesta a cualquiera que vea el código
   fuente.
2. `catch` silencioso en `showPromo()` — un fallo del endpoint
   `/generate-promo` pasa desapercibido y se enmascara con un código fijo.
3. 3 links placeholder `href="#"` en el footer (Instagram, X, LinkedIn,
   líneas 321-323) que no llevan a ningún perfil real.
4. Sección de testimonios (líneas 258-280) todavía tiene texto de relleno
   ("Espacio reservado para un testimonio real...") en vez de testimonios
   reales — no es un bug, pero está listo para reemplazar.
5. No se pudo verificar en vivo alas7.com, el Stripe Payment Link, ni los
   endpoints de Railway por bloqueo de red del sandbox (ver arriba) — pendiente
   de verificación manual.

**Verificado sin problemas:** `node --check` sobre el JS embebido no arrojó
errores de sintaxis; el `CNAME` contiene correctamente `alas7.com` (sin rastro
de `vitanews` u otro dominio incorrecto); no se encontraron referencias a
`alas7.mx` ni dominios inexistentes; los links legales
(`aviso-de-privacidad.html`, `terminos.html`) existen y están bien
referenciados.

**Recomendación:** (no corregido, solo para decisión de Alan)
- Rotar el token Bearer hardcodeado y moverlo a un proxy/backend en vez de
  exponerlo en el HTML público — es el hallazgo más importante de esta
  corrida.
- Agregar al menos un `console.error` (o telemetría) en el catch de
  `showPromo()` para poder detectar caídas del endpoint `/generate-promo` en
  vez de que fallen en silencio.
- Actualizar o quitar los 3 íconos sociales del footer que apuntan a `href="#"`.
- Verificar manualmente, desde una red sin restricciones, que alas7.com,
  el Stripe Payment Link y los dos endpoints de Railway respondan 200 — esta
  corrida no pudo confirmarlo por el bloqueo de egress del sandbox.
