## [08:10 CDMX] Auditor web
**Estado:** problemas encontrados (persisten, no críticos nuevos)
**Sitio en vivo:** No verificable — el entorno del auditor tiene bloqueada la salida a internet hacia alas7.com, buy.stripe.com y el proxy de Railway (403 de política de egress de la organización, confirmado en `/root/.ccr/__agentproxy/status`). No es una caída del sitio; es una restricción del sandbox de esta sesión. No se pudo confirmar código 200 en vivo.
**Pago y promo:** No verificable en vivo (mismo bloqueo de red). Revisión estática de `index.html` en `main` (commit `535f9320`, HEAD actual):
  - Botón de pago Premium: `STRIPE = 'https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01'` presente y correctamente cableado a `checkPremiumPrivacy` → `window.location.href`. Coincide con el link oficial.
  - Endpoint de promo: `API + '/generate-promo'` con header `X-ALAS7-Secret` sigue apuntando a `gentle-laughter-production-7f2c.up.railway.app`. JS sintácticamente correcto (`node --check` OK).
  - Formulario de suscripción: POST a `API + '/subscribe'` con `Authorization: Bearer ...` está cableado correctamente, incluye `referral_code` desde el input oculto.
**Deploy Pages:** Al día. Último build (`pages build and deployment`, run #35, commit `535f9320` "Trigger Pages rebuild") = **success**. El run #34 sobre el commit del merge de dev (`1c23348`) fue `failure`, pero fue re-disparado manualmente y el run #33 sobre el mismo contenido (`7e79b6f`) ya había sido `success`; el HEAD de `main` coincide con el último build exitoso. No hay indicios de builds atorados nuevos desde el incidente del 3-jul ya reportado.

**Problemas encontrados:**
1. **[CRÍTICO — persiste desde hace ~2 meses, reportado en 30+ auditorías sin acción]** El JS de `index.html` (línea ~395) sigue exponiendo en texto plano, visible por cualquiera con "ver código fuente":
   - Un **bearer token JWT completo** (`const BEARER = 'eyJ0eXAi...'`) usado para autenticar el POST a `/subscribe`. Al decodificar el payload, su campo `exp` es `4933854509`, es decir **vence en el año 2126** — es, en la práctica, un token permanente y de vigencia indefinida.
   - Un secreto (`const SECRET = 'alas7secret2026'`) enviado como header `X-ALAS7-Secret` al endpoint `/generate-promo`.
   Cualquier visitante puede copiar ese token y usarlo directamente contra la API detrás del proxy de Railway (suscribir emails arbitrarios al grupo de MailerLite, generar códigos promo sin límite, o lo que ese token autorice del lado del proxy) sin pasar por el sitio. Este es el mismo hallazgo señalado de forma consecutiva en los PRs #37 al #66 (2026-07-13 a 2026-09-11 aprox.), ninguno de los cuales ha sido mergeado.
2. **[Menor, no crítico]** Los íconos sociales de Instagram, X y LinkedIn en el footer (`href="#"`, líneas ~321-323) son enlaces muertos sin URL real — decoración sin destino, ya señalado en auditorías previas.
3. **[Menor, por diseño pero vale mencionarlo]** `showPromo()` (línea ~511) tiene un `catch` que, si `/generate-promo` falla, cae silenciosamente a un código estático `'ALAS7-2026'` sin avisar al usuario ni registrar el error en consola — funcional pero oculta fallos reales del endpoint de promo si empezara a fallar de forma sostenida.
4. **[Proceso, no de código]** Hay **30 pull requests de auditoría abiertos y sin mergear** (`claude/audit-web-2026-08-13` a `claude/audit-web-2026-09-11`, PRs #37–#66), todos reportando esencialmente el mismo hallazgo #1. Ninguno ha sido revisado ni cerrado.

**Recomendación:** (qué se debería arreglar, PERO NO LO ARREGLES)
- Máxima prioridad: rotar/revocar el bearer token de MailerLite y el `X-ALAS7-Secret` expuestos, y mover la autenticación hacia el proxy de Railway (que el proxy adjunte el secreto en el servidor, nunca en el HTML público). Un token que vence en 2126 expuesto en código fuente público es el hallazgo más grave de todo este historial de auditorías.
- Revisar y cerrar/mergear el backlog de 30 PRs de auditoría para que este hallazgo deje de repetirse día tras día sin resolución.
- Opcional: quitar o enlazar correctamente los íconos sociales del footer.
