## [08:10 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno. El proxy de salida de esta sesión (política de red de la organización) bloqueó las conexiones a `alas7.com`, `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app` con `connect_rejected` / `EGRESS_BLOCKED` (403 de la política, no un fallo del sitio). No pude confirmar el código HTTP real de la home. Recomiendo revisar alas7.com manualmente o desde un entorno sin esa restricción.

**Pago y promo:** Revisión estática del código (no se pudo probar en vivo por el bloqueo de red anterior):
- El botón Premium y el link del popup de promo apuntan correctamente al Payment Link de Stripe vigente: `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (`index.html` líneas 361, 391, 470, 515).
- El JS llama a `POST {API}/generate-promo` con el header `X-ALAS7-Secret` y a `POST {API}/subscribe`, ambos contra `https://gentle-laughter-production-7f2c.up.railway.app`, consistente con la convención vigente (`index.html` líneas 391-395, 447-451, 508). Cableado correcto.
- **Hallazgo de seguridad (nuevo, no reportado antes):** el `<script>` de `index.html` (líneas 393-395) expone en texto plano y visible por "Ver código fuente" tanto el secreto del endpoint de promo (`SECRET = 'alas7secret2026'`) como un **Bearer token JWT completo** usado para autenticar `/subscribe`. El JWT trae `"exp":4933854509` (año ~2126, prácticamente sin expiración) y `"scopes":[]` (sin scopes explícitos, lo que en la API de MailerLite normalmente implica acceso completo). Cualquier visitante puede copiar este token del HTML y usarlo directamente contra la API de MailerLite (o contra el proxy de Railway) fuera del flujo de la landing — para inscribir emails arbitrarios, potencialmente leer/exportar la lista de suscriptores u otras operaciones que el scope del token permita. Esto es independiente del bloqueo de red: es visible en el HTML público sin necesidad de llamarlo.

**Deploy Pages:** Al día. El último "pages build and deployment" en `main` (run #35, commit `535f932` "Trigger Pages rebuild (previous build stuck)") terminó en `success` el 2026-07-04. Ese commit sigue siendo el HEAD actual de `main` (sin commits nuevos desde entonces), por lo que el HTML en vivo debería coincidir con el último commit. El build fallido anterior (run #34, commit `1c23348`, conclusion `failure`) y el problema del 3-jul mencionado ya quedaron resueltos por el rebuild manual — no hay builds atorados pendientes ahora.

**Problemas encontrados:**
1. (Crítico / seguridad) Token Bearer de MailerLite y secreto de promo hardcodeados y expuestos en el HTML público (`index.html` líneas 393-395), con expiración ~año 2126. Explotable por cualquier visitante vía "Ver código fuente", sin relación con el bloqueo de red de esta auditoría.
2. No se pudo verificar en vivo si alas7.com responde 200, si el botón de Stripe resuelve, ni si los endpoints de Railway responden — el egress de este entorno bloqueó esos tres dominios por política organizacional. Es una limitación de esta corrida, no evidencia de que el sitio esté caído.
3. (Menor) Los íconos de redes sociales del footer (Instagram, X, LinkedIn) usan `href="#"` sin destino real — enlaces decorativos/placeholder, no rotos por error pero tampoco funcionales.
4. (Cosmético) Queda una variable `SESSION_KEY = 'vita_promo_seen'` en el JS, residuo del branding anterior "Vita News" — no afecta funcionalidad, pero es inconsistente con el rebrand a A LAS 7.
5. CNAME correcto (`alas7.com`), sin referencias a `alas7.mx` ni a dominios viejos, y el JS inline pasó `node --check` sin errores de sintaxis. No se detectaron `try/catch` que oculten errores de forma silenciosa: los catches existentes (`/subscribe` y `/generate-promo`) sí muestran mensaje de error o degradan a un código de respaldo visible.

**Recomendación:** (PERO NO SE CORRIGIÓ NADA — solo diagnóstico)
- Prioridad alta: rotar el Bearer token de MailerLite y el secreto de promo, y mover ambos al backend (Railway) para que el navegador nunca los reciba — el frontend solo debería llamar al proxy sin credenciales de MailerLite embebidas. Revisar si el token ya fue usado por terceros no autorizados.
- Confirmar manualmente (fuera de este entorno con restricciones de red) que alas7.com, el link de Stripe y los endpoints de Railway responden 200 en este momento, ya que esta corrida no pudo verificarlo.
- Opcional: reemplazar los `href="#"` de redes sociales por URLs reales o quitarlos si aún no existen esas cuentas.
- Opcional: renombrar `vita_promo_seen` a algo con el branding actual, por prolijidad.
