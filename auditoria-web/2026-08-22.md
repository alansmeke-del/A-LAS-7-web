# Auditoría web — 2026-08-22

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde esta sesión — la política de red (egress) de este entorno bloquea salidas HTTPS hacia `alas7.com` (`403` en el CONNECT del proxy, confirmado también contra `gentle-laughter-production-7f2c.up.railway.app` y `buy.stripe.com`). No es un hallazgo del sitio, es una limitación del entorno de auditoría. Verificación indirecta vía GitHub: el HEAD de `main` (`535f932`) coincide con el commit del último build de GitHub Pages exitoso (run #35, "Trigger Pages rebuild (previous build stuck)"), así que el repo y el último deploy están sincronizados.

**Pago y promo:**
- Botón/enlace de pago Premium: `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` está presente en `index.html` (variable `STRIPE`, usada en `checkPremium()` y en el CTA del popup de promo) y coincide con el Payment Link esperado. No se pudo confirmar que responda 200 por el bloqueo de red descrito arriba.
- Endpoint de promo: el JS llama correctamente a `POST {API}/generate-promo` con header `X-ALAS7-Secret`, donde `API = https://gentle-laughter-production-7f2c.up.railway.app`. Cableado correcto. No se pudo confirmar respuesta en vivo por el mismo bloqueo de red.
- Formulario de suscripción: `POST {API}/subscribe` con `Authorization: Bearer …` y `groups: [187372665902728776]`, incluye `referral_code`. Cableado correcto.
- **Hallazgo de seguridad (nuevo, no reportado antes):** el token Bearer de MailerLite usado en `/subscribe` está **hardcodeado en el JavaScript del cliente** (visible en "ver código fuente"). Es un JWT válido desde 2026-05-07 hasta **2126-05-07** (100 años — en la práctica no expira). Cualquier visitante puede copiar este token y usarlo para llamar la API de MailerLite directamente. Es una fuga de credencial de API en un archivo público.
- **Fallback silencioso en promo:** en `showPromo()`, si `fetch(API + '/generate-promo')` falla (endpoint caído, CORS, timeout, red), el `catch` no informa a nadie: simplemente muestra el código estático `ALAS7-2026` al usuario como si fuera válido y personalizado. Si ese código no existe realmente en Stripe/MailerLite, el usuario ve una oferta que fallará silenciosamente al aplicarla, sin log ni aviso.

**Deploy Pages:** Al día. Último build (run #35, commit `535f932`, 2026-07-04 06:11 UTC) completó con éxito. El build inmediatamente anterior (run #34, commit `1c23348`) había fallado, pero fue re-disparado manualmente por el mismo commit que hoy es HEAD de `main`, y ese re-intento sí tuvo éxito. No hay commits en `main` posteriores a ese build, por lo que no hay drift conocido entre el HTML en el repo y el que debería estar sirviendo Pages.

**Integridad HTML/JS:**
- `node --check` sobre el bloque `<script>` inline: **sin errores de sintaxis**.
- `CNAME`: contiene `alas7.com` — correcto (no `vitanews`, no `wek.com.mx`).
- No se encontraron referencias a dominios viejos/incorrectos (`alas7.mx`, `vitanews`, `wek.com.mx`) en `index.html`.
- Enlaces `href="#"` sin JS real detrás: los tres íconos de redes sociales en el footer (Instagram, X, LinkedIn, líneas ~321-323) apuntan a `href="#"` sin ningún handler — son enlaces muertos/placeholder, no rotos hacia un dominio incorrecto, pero tampoco funcionan. (Los `href="#"` de "Gratis" sí están bien: tienen `e.preventDefault()` + `openFree()` en el JS.)

**Problemas encontrados:**
1. Token Bearer de MailerLite (JWT, válido ~100 años) expuesto en texto plano en el JS del cliente — riesgo de abuso de la API de MailerLite por terceros.
2. Fallback silencioso en `generate-promo`: ante fallo del endpoint, se muestra un código de promo estático (`ALAS7-2026`) sin aviso visible ni logging, lo que puede generar una oferta que falla al aplicarse en Stripe.
3. Enlaces de redes sociales (Instagram, X, LinkedIn) en el footer son placeholders `href="#"` sin destino real.
4. No fue posible verificar en vivo `alas7.com`, el endpoint de Railway ni el Payment Link de Stripe desde esta sesión: la política de egress del entorno de auditoría bloquea (403) las conexiones salientes a esos tres dominios. Recomendado resolver esto en la configuración del entorno para que las próximas corridas sí puedan hacer la verificación en vivo.

**Recomendación:** (qué se debería arreglar, PERO NO SE ARREGLÓ en esta corrida)
- Rotar el token Bearer de MailerLite y moverlo detrás del proxy de Railway (que el cliente llame a `/subscribe` sin credencial embebida, y que el proxy agregue el Authorization header del lado servidor). Es el hallazgo más urgente porque es una fuga de credencial activa y de larga duración.
- Añadir un log o alerta (aunque sea un `console.error` + telemetría mínima) cuando `generate-promo` falla, en vez de degradar en silencio a un código estático que puede no ser válido.
- Wirear los íconos de redes sociales a las cuentas reales de A LAS 7, o quitarlos si aún no existen, para no mostrar enlaces muertos.
- Habilitar acceso de red saliente a `alas7.com`, al endpoint de Railway y a `buy.stripe.com` para las próximas corridas de este auditor, de forma que el chequeo de "sitio en vivo" y "pago/promo responde" pueda hacerse de verdad y no solo por inferencia desde GitHub.
