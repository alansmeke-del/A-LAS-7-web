# Auditoría web — 2026-09-16

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No se pudo verificar. El egress de red de esta sesión bloquea
por política de organización las conexiones salientes a `alas7.com`,
`buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app` (CONNECT
rechazado con 403 en el proxy de salida). No es un hallazgo sobre el sitio en
sí — es una limitación del entorno de esta corrida. Los puntos 1 y 2 del
checklist (salud del sitio en vivo, respuesta real del Stripe Payment Link y
del endpoint de promo) **no se pudieron comprobar hoy**. Se recomienda
habilitar esos dominios en la política de red del entorno programado, o
correr esta verificación puntual desde un entorno con salida abierta.

**Pago y promo:** No verificado en vivo (ver arriba). Revisión estática del
código:
- El botón premium (`checkPremium()` → `window.location.href = STRIPE`) apunta
  correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, coincide
  con lo documentado en CLAUDE.md.
- El JS llama a `POST {API}/generate-promo` y `POST {API}/subscribe` contra
  `https://gentle-laughter-production-7f2c.up.railway.app`, cableado
  correctamente según lo esperado.
- **Hallazgo de seguridad (no introducido hoy, preexistente en `index.html`):**
  las constantes `SECRET` (línea 393, secreto del header `X-ALAS7-Secret` para
  `/generate-promo`) y `BEARER` (línea 395, un JWT completo que parece ser el
  token bearer de la API de MailerLite, con expiración ~año 2126) están
  hardcodeadas en texto plano dentro del `<script>` público de `index.html`.
  Cualquiera puede verlas con "ver código fuente" y usarlas para llamar
  directamente a la API de MailerLite (gestionar la lista de suscriptores) o
  generar códigos promo ilimitados en el endpoint de Railway. Esto es un
  riesgo de abuso de promo y de la lista de correo — no es un problema de
  disponibilidad, pero sí de exposición de credenciales del lado cliente.
- **Manejo de error silencioso:** en `showPromo()` (línea ~506-513), si el
  `fetch` a `/generate-promo` falla o responde con error, el `catch` no
  registra nada y cae en silencio a un código fijo `'ALAS7-2026'`, sin avisar
  al usuario ni dejar rastro. Si el endpoint de Railway se cae, la landing
  seguiría funcionando de forma "normal" con un código genérico y nadie se
  enteraría desde el frontend.

**Deploy Pages:** Al día. El último build de Pages (run #35, commit `535f932`,
2026-07-04 06:11 UTC) terminó en `success` y coincide exactamente con el HEAD
actual de `origin/main`. No hay commits nuevos en `main` desde esa fecha, así
que no hay descuadre entre el HTML en vivo (asumiendo que el build se sirvió
bien) y el último commit. El problema previo del 3-jul (build atorado 29 min)
ya quedó resuelto por el commit "Trigger Pages rebuild".

**Problemas encontrados:**
1. No se pudo verificar la disponibilidad real de alas7.com, el Stripe
   Payment Link ni el endpoint de Railway — bloqueo de red del entorno de
   auditoría (no es evidencia de que algo esté roto en producción).
2. Secretos de API expuestos en el código cliente de `index.html`: `SECRET`
   (línea 393) y un JWT bearer de MailerLite (`BEARER`, línea 395) visibles a
   cualquiera que vea el código fuente de la página.
3. Manejo de error silencioso en `showPromo()`: un fallo del endpoint de
   promo no se reporta ni se registra, solo cae a un código fijo sin avisar.
4. Menor: los íconos sociales del footer (Instagram, X, LinkedIn) usan
   `href="#"` sin URL real ni manejador de clic — no llevan a ningún lado.
5. Menor: la clave de `sessionStorage` sigue llamándose `vita_promo_seen`
   (resto del nombre de marca anterior "Vita News"), sin impacto funcional.

Sin hallazgos: CNAME correcto (`alas7.com`), sin referencias a dominios viejos
tipo `alas7.mx` o `vitanews`, el JS inline pasa `node --check` sin errores de
sintaxis, y los enlaces internos (`aviso-de-privacidad.html`, `terminos.html`)
existen en el repo.

**Recomendación:** (PERO NO SE CORRIGIÓ NADA — solo aviso)
- Prioridad alta: mover `SECRET` y el token `BEARER` de MailerLite fuera del
  HTML público. El `BEARER` en particular es un token de larga vida
  (expira ~2126) que da acceso completo a la API de MailerLite si se filtra —
  debería vivir detrás del proxy de Railway (que ya existe para `/subscribe`)
  en vez de viajar al navegador. Rotar el token una vez movido.
- Agregar manejo de error visible/loggeado en `showPromo()` en vez de fallback
  silencioso, para poder detectar si el endpoint de promo se cae.
- Opcional: dar URLs reales a los íconos sociales del footer o quitarlos si
  no hay cuentas activas.
- Habilitar salida de red a alas7.com / buy.stripe.com / el dominio de
  Railway para este entorno de auditoría programado, para que las próximas
  corridas puedan comprobar disponibilidad real en vivo.
