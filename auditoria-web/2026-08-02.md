# Auditoría web — 2026-08-02

## [08:10 CDMX] Auditor web

**Estado:** problemas encontrados (limitación de verificación, no falla confirmada)

**Sitio en vivo:** No verificable desde este entorno. Las conexiones salientes a
`alas7.com`, al proxy de Railway (`gentle-laughter-production-7f2c.up.railway.app`)
y a Stripe (`buy.stripe.com`) fueron rechazadas con 403 por la política de red del
sandbox del auditor (`gateway answered 403 to CONNECT — policy denial`), confirmado
vía el status del proxy interno. Esto **no** es evidencia de que el sitio esté
caído — es una restricción del entorno donde corre este auditor automatizado, que
actualmente no tiene salida a internet general (solo a GitHub/Anthropic/registries).

**Pago y promo:** Código fuente correcto (verificado estáticamente, no en vivo):
- Botón de pago apunta al Stripe Payment Link real: `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`
  (`index.html:361` y `index.html:391`).
- `/generate-promo` se llama correctamente con header `X-ALAS7-Secret` (`index.html:508`).
- `/subscribe` está cableado con `Authorization: Bearer`, payload de email/grupo y
  `referral_code` desde el input oculto (`index.html:447-450`).
- No se pudo confirmar que estos endpoints respondan en vivo (ver limitación arriba).

**Deploy Pages:** Al día. El último workflow run de `pages-build-deployment`
(run #35, id `28697306776`) corresponde exactamente al commit HEAD de `main`
(`535f932` — "Trigger Pages rebuild (previous build stuck)") y terminó con
`conclusion: success` en 36s (2026-07-04 06:11:27–06:12:03 UTC). No hay commits
nuevos en `main` desde ese build, así que el HTML desplegado debería coincidir con
el repo. El incidente previo del 3-jul (build atorado 29 min / build no disparado)
ya quedó resuelto por ese commit de rebuild forzado.

**Problemas encontrados:**
1. **[Bloqueante de auditoría, no del sitio]** Este auditor no tiene salida de red
   a alas7.com / Railway / Stripe desde su entorno actual, por lo que los chequeos
   1 y 2 (salud en vivo, pago y promo funcionando) no pudieron ejecutarse esta
   corrida — solo se validó el código fuente estático.
2. 3 links `href="#"` sin URL real en el footer (íconos de Instagram, X, LinkedIn —
   `index.html:321-323`). Parecen placeholders nunca completados, no una regresión.
   (Los otros `href="#"` con `class="js-open-free"` en líneas 66 y 308 son
   intencionales: abren el modal de suscripción gratis vía JS.)
3. El catch de `/generate-promo` (`index.html:511`) cae silenciosamente a un código
   fijo `ALAS7-2026` sin avisar al usuario que el servidor de promo falló — si el
   endpoint está caído, todos los visitantes reciben el mismo código genérico sin
   que nadie se entere. Es un fallback de UX intencional, no un bug, pero limita la
   visibilidad de una falla real del backend.
4. Nada roto en CNAME (`alas7.com`, correcto), sintaxis del único bloque `<script>`
   inline (validado con `node --check`, sin errores), ni en los archivos locales
   referenciados (`aviso-de-privacidad.html`, `terminos.html`, `assets/logo7-v2.png`
   — todos existen).

**Recomendación:** (no corregido, solo se avisa)
- Dar salida de red a este auditor hacia alas7.com / Railway / Stripe (o correr un
  chequeo de salud en vivo desde otro lado) para poder validar de verdad los
  chequeos 1 y 2 en próximas corridas — hoy son un punto ciego.
- Completar o quitar los 3 íconos sociales del footer sin URL real.
- Opcional: que el fallback de `/generate-promo` muestre algún indicio (log a un
  endpoint de monitoreo, o un mensaje discreto) cuando cae al código genérico, para
  detectar caídas reales del backend de promociones sin depender de este auditor.
