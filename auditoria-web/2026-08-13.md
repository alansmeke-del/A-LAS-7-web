## [08:40 CDMX] Auditor web

**Estado:** problemas encontrados (ninguno crítico/bloqueante)

**Sitio en vivo:** No verificado directamente. La política de red de este entorno de auditoría bloquea la conexión saliente a alas7.com (CONNECT rechazado con 403 por el proxy egress del sandbox), por lo que no pude confirmar un 200 OK real desde aquí. Como evidencia indirecta: el último build de GitHub Pages para `main` terminó en **success** y coincide con el HEAD actual de `main` (commit `535f932`), lo que indica que el contenido desplegado sí refleja el último commit.

**Pago y promo:** ok (verificado en código, no en red en vivo).
- El botón/CTA de pago apunta correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (usado como `href` directo del CTA de promo y como constante `STRIPE` en el JS para el botón Premium y el prefill de código promocional).
- El JS llama correctamente a `API + '/generate-promo'` (`https://gentle-laughter-production-7f2c.up.railway.app/generate-promo`) con el header `X-ALAS7-Secret`, con fallback silencioso a un código estático `ALAS7-2026` si la llamada falla (comportamiento intencional, no un bug).
- El formulario de suscripción gratuita hace `POST` a `API + '/subscribe'` con el payload esperado (`email`, `groups`, `referral_code`).
- No pude verificar alcanzabilidad HTTP en vivo de Stripe ni del proxy de Railway desde este entorno por la misma restricción de red del sandbox.

**Deploy Pages:** al día. Último build (`2026-07-04T06:11:27Z`, run 28697306776) completó con **success** sobre el commit `535f932` de `main`, que es el HEAD actual. No hay builds atorados ni fallidos pendientes después de ese run.

**Problemas encontrados:**
1. **(Seguridad, preexistente)** El JWT Bearer de MailerLite está hardcodeado en texto plano dentro del `<script>` del cliente (`index.html`, dentro de `submitFree`), visible para cualquiera que vea el código fuente de la página. El token no tiene expiración práctica (`exp` ≈ año 2126). Cualquier visitante puede extraerlo y llamar a la API de MailerLite directamente. No es una regresión de hoy — se introdujo en el commit `13289ab` ("Route free subscriptions through Railway proxy") — pero sigue expuesto en producción ahora mismo.
2. **(Cosmético, menor)** Los íconos de redes sociales del footer (Instagram, X, LinkedIn) usan `href="#"` sin URL real; no llevan a ningún perfil.
3. **(Limitación de esta corrida)** No se pudo verificar en vivo alas7.com, Stripe ni el endpoint de Railway por la política de red del sandbox de auditoría (bloqueo de salida a esos dominios). No es evidencia de que algo esté caído, solo que esta corrida no pudo confirmarlo por HTTP directo.

No se encontraron: errores de sintaxis en el JS (validado con `node --check`), links rotos a dominios inexistentes (no hay referencias a `alas7.mx` ni `vitanews`), ni discrepancias en el CNAME (dice correctamente `alas7.com`). Todos los IDs de elementos que el JS referencia (`freeModal`, `premiumPrivacy`, `promoCta`, etc.) existen en el HTML.

**Recomendación:** (a) Rotar el Bearer token de MailerLite y mover la llamada de `/subscribe` para que el token viva solo en el backend de Railway, nunca en el cliente — ya existe el proxy, así que el JWT no debería viajar al navegador. (b) Rellenar o quitar los enlaces placeholder de redes sociales. (c) Si se quiere que este auditor confirme salud HTTP en vivo en corridas futuras, habilitar salida de red a alas7.com / buy.stripe.com / el dominio de Railway en el entorno del auditor. PERO NO SE ARREGLÓ NADA DE ESTO EN ESTA CORRIDA — queda a decisión de Alan.
