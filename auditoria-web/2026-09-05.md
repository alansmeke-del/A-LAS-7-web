## [08:11 CDMX] Auditor web

**Estado:** problemas encontrados (persistentes, ya reportados en auditorías previas)

**Sitio en vivo:** No verificable. El entorno de este auditor bloquea la salida HTTPS a hosts externos (alas7.com, buy.stripe.com, railway.app) por política de red del sandbox — todas las conexiones fueron rechazadas por el proxy de egreso (`connect_rejected`, política de organización). No es una señal de que el sitio esté caído, es una limitación del entorno del auditor. Esta misma limitación ya se reportó en auditorías anteriores (p. ej. 2026-08-26, 2026-08-08).

**Pago y promo:** ok (a nivel de código, no verificado en vivo)
- El botón de pago (`#promoCta`) y la constante `STRIPE` en el JS apuntan correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`.
- El JS llama a `API + '/generate-promo'` con `API = 'https://gentle-laughter-production-7f2c.up.railway.app'`, cableado correctamente.
- El formulario de suscripción hace POST a `API + '/subscribe'`, cableado correctamente.
- **Persiste (reportado en 30+ auditorías previas desde ~2026-08-06, sin mergear):** el secreto `X-ALAS7-Secret` está *hardcodeado* en el JS del cliente (`const SECRET = 'alas7secret2026';`, línea 393 de `index.html`), visible para cualquiera que vea el código fuente de la página. Cualquiera puede llamar directamente al endpoint `/generate-promo` con ese secreto sin pasar por el sitio.
- **Persiste:** el `catch` del fetch a `/generate-promo` (líneas 511-513) es silencioso — si el endpoint falla, cae a un código promo fijo `'ALAS7-2026'` sin registrar el error ni avisar al usuario. Esto puede ocultar caídas reales del servicio de promociones.

**Deploy Pages:** al día — el último build de GitHub Pages (run #35, `pages build and deployment`) terminó con `success` sobre el commit `535f932` (head actual de `main`), coincide con el HEAD de main. No hay build atorado ni desincronización. Los builds atorados/fallidos del 3-4 de julio (run #34 `failure`, y el stuck build) ya quedaron resueltos por el run #35.

**Integridad HTML/JS:**
- `node --check` sobre el bloque `<script>` embebido: sintaxis válida, sin errores.
- `CNAME` correcto: `alas7.com` (no vitanews, no alas7.mx).
- Sin referencias a dominios inexistentes (`alas7.mx`, `vitanews`, etc.) en `index.html`, `CNAME`, `aviso-de-privacidad.html`, ni `terminos.html`.
- `href="#"` presentes en: botones "Gratis"/"Suscríbete gratis" (intencional, abren el modal vía JS `js-open-free`) y en los íconos sociales de Instagram/X/LinkedIn del footer (línea 321-323) — estos últimos no tienen URL real, son placeholders sin destino. No es un error de código, pero no llevan a ningún lado si un usuario hace clic.

**Problemas encontrados:**
1. (Persistente, sin corregir en 30+ auditorías) Secreto `X-ALAS7-Secret` hardcodeado en el JS público del cliente.
2. (Persistente, sin corregir) `catch` silencioso en la llamada a `/generate-promo`: fallback sin logging ni aviso visible.
3. (Nuevo/cosmético) Íconos sociales del footer (IG/X/LinkedIn) con `href="#"` sin URL real.
4. Verificación en vivo del sitio no fue posible por restricción de red del entorno del auditor (no es un hallazgo del sitio en sí).

**Recomendación:** (no corregido, solo se documenta)
- Mover la generación de códigos promo detrás de un backend que no exponga el secreto en el cliente, o eliminar el secreto del JS y validar por otro medio (rate limiting por IP, captcha, etc.).
- Agregar `console.error` o telemetría al catch de `/generate-promo` para detectar caídas reales del servicio en vez de enmascararlas con el código fijo.
- Enlazar los íconos sociales del footer a las cuentas reales o quitarlos si no existen.
- Hay 30+ PRs de auditoría abiertos sin mergear desde ~2026-08-06 con el mismo hallazgo del secreto expuesto — vale la pena que Alan decida si se corrige o se cierra el hallazgo como aceptado, para dejar de repetirlo en cada corrida.
