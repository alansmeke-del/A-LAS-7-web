# Auditoría web — 2026-08-08

## [08:13 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** NO VERIFICABLE — el sandbox de esta corrida tiene bloqueado el
egress de red hacia `alas7.com` (el proxy del entorno responde `403` a cualquier
CONNECT a ese host). No pude confirmar si el sitio responde 200 OK ni si el HTML
en vivo coincide con el último commit de `main`. Esto aplica también a cualquier
corrida futura de este auditor con la misma configuración de red: no podrá
verificar salud del sitio en vivo mientras el entorno no tenga `alas7.com` en su
lista de dominios permitidos.

**Pago y promo:** NO VERIFICABLE EN VIVO por la misma razón — `buy.stripe.com` y
`gentle-laughter-production-7f2c.up.railway.app` también están bloqueados por la
política de red del sandbox (403 en el CONNECT). Revisión estática del código
(sí disponible):
  - El botón Premium (`checkPremiumPrivacy()` → `window.location.href`) apunta
    correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (aparece 2
    veces en `index.html`, valor correcto).
  - El JS llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret`, y a
    `POST {API}/subscribe` con `Content-Type: application/json` — ambos contra
    `https://gentle-laughter-production-7f2c.up.railway.app`, cableado como se
    documenta en CLAUDE.md.
  - **Hallazgo de seguridad (grave):** el script inline en `index.html` incluye
    un **JWT Bearer token hardcodeado en claro** (`const BEARER = 'eyJ0eXAi...'`,
    ~línea 10 del `<script>` final) que se envía en cada llamada a `/subscribe`.
    Decodificando el payload, el claim `exp` corresponde a una fecha ~año 2126 —
    es decir, es un token de vida prácticamente permanente. Cualquier persona
    que vea el código fuente de la página (Ctrl+U) puede copiar este token y
    usarlo para llamar la API de Railway/MailerLite directamente, sin pasar por
    el frontend. Esto es una exposición de credencial en cliente, no un bug de
    esta corrida — probablemente lleva así desde que se implementó el flujo,
    pero no había sido documentado como riesgo hasta ahora.

**Deploy Pages:** al día. El último build de GitHub Pages (`run 28697306776`,
"Trigger Pages rebuild") completó con `success` el 2026-07-04T06:11:27Z, mismo
día que el último commit en `main` (`535f9320`, "Trigger Pages rebuild (previous
build stuck)"). No hay commits nuevos en `main` desde el 2026-07-04, así que no
hay desfase deploy-vs-commit que reportar. (Se confirma también el incidente
previo del 3-jul mencionado en el prompt: build `28696615691` quedó en
`failure` a las 05:42:45Z antes del rebuild exitoso.)

**Problemas encontrados:**
1. **[GRAVE, no bloqueante para el sitio]** JWT Bearer token de MailerLite/API
   hardcodeado en el JS del cliente, visible en el código fuente público, con
   expiración efectivamente inexistente (~año 2126).
2. **[Limitación del auditor, no del sitio]** Este entorno de ejecución no tiene
   permitido el egress hacia `alas7.com`, `buy.stripe.com` ni el dominio de
   Railway — el auditor no puede confirmar salud en vivo del sitio ni de los
   endpoints de pago/promo mientras esto no cambie.
3. **[Menor]** Los íconos de redes sociales del footer (Instagram, X, LinkedIn)
   usan `href="#"` sin URL real de destino — parecen placeholders pendientes de
   completar, no error de código.

**Sin hallazgos:** no hay referencias a `alas7.mx` ni a `vitanews` en el HTML;
el `CNAME` es correcto (`alas7.com`); no hay errores de sintaxis en el JS
(validado con `node --check` sobre el bloque `<script>` extraído); el manejo de
errores en `/subscribe` y `/generate-promo` no es silencioso — ambos muestran
mensaje al usuario o usan un valor de respaldo visible (`ALAS7-2026`).

**Recomendación:** (no corregido — solo aviso)
- Prioridad 1: rotar el JWT Bearer expuesto en el cliente y mover la
  autenticación con MailerLite al backend de Railway (que el frontend llame a
  `/subscribe` sin credencial visible, y sea el proxy de Railway quien inyecte
  el Bearer del lado servidor). Es la corrección de mayor impacto de este
  reporte.
- Prioridad 2: ajustar la política de red de este entorno de auditoría para
  permitir egress hacia `alas7.com`, `buy.stripe.com` y el dominio de Railway,
  para que las corridas futuras sí puedan confirmar salud en vivo del sitio y
  de los endpoints de pago — hoy esa parte del auditor está ciega.
- Prioridad 3 (opcional, cosmético): completar o quitar los links `href="#"` de
  redes sociales en el footer.
