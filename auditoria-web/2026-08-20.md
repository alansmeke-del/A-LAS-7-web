# Auditoría web — 2026-08-20

## [08:17 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno — la política de red de esta
sesión bloquea (403) las conexiones salientes a `alas7.com`, `buy.stripe.com` y
`gentle-laughter-production-7f2c.up.railway.app`. No es evidencia de que el sitio
esté caído, solo que este auditor no puede comprobarlo por sí mismo esta vez.
Como sustituto, se verificó que el último build de GitHub Pages sobre `main`
(commit `535f932`, run #35) terminó en `success` el 2026-07-04 06:12 UTC, y que
no hay commits nuevos en `main` desde entonces — el HTML en vivo debería
corresponder al HEAD actual de `main`.

**Pago y promo:** Wiring correcto en el HTML/JS (no se pudo probar en vivo por el
bloqueo de red anterior):
- Botón Stripe (`#promoCta` y constante `STRIPE`) apunta al Payment Link esperado:
  `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` ✓
- `showPromo()` llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret`,
  y `API` = `https://gentle-laughter-production-7f2c.up.railway.app` ✓
- Formulario gratuito (`submitFree`) hace `POST {API}/subscribe` con el grupo de
  MailerLite y `referral_code` desde `?ref=` ✓
- Gate de privacidad presente en ambos flujos (checkbox → error → acción) ✓

⚠️ **Hallazgo crítico de seguridad:** en `index.html` línea 395, la constante
`BEARER` contiene un **token JWT completo de la API de MailerLite, embebido en
JS del lado del cliente**, visible para cualquiera con "ver código fuente". El
payload del JWT tiene `exp` en un timestamp astronómicamente lejano (~año 2126),
es decir, un token que en la práctica no expira nunca. Se usa en el header
`Authorization: Bearer …` de la llamada a `{API}/subscribe` (línea 449). Cualquier
visitante puede copiar este token y usarlo para llamar directamente a la API de
MailerLite (aunque el JWT trae `"scopes":[]`, el alcance real depende de cómo
MailerLite interprete un scope vacío — hay que confirmarlo, no asumir que es
inofensivo). Esto ya existía antes de hoy (no es una regresión de esta corrida),
pero no consta que se haya reportado en una auditoría previa — es la primera
entrada de este auditor.

**Deploy Pages:** Al día. Último run (#35, `535f932` — "Trigger Pages rebuild
(previous build stuck)") completó con `conclusion: success` el 2026-07-04
06:12 UTC. El run anterior (#34, sobre el merge de `dev`) había fallado
(`conclusion: failure`), pero fue corregido por el propio commit #35, que
disparó un rebuild exitoso. No hay commits en `main` posteriores a `535f932`,
así que no hay deploys pendientes ni desincronización HTML-vs-commit detectable
desde el repo. (El incidente del 3-jul mencionado en la instrucción —build no
disparado + build colgado 29 min— corresponde a los runs #29-#30 de esa fecha;
ya está resuelto y no se repite en el historial reciente.)

**Problemas encontrados:**
1. 🔴 **[Seguridad] Token Bearer de MailerLite hardcodeado en JS cliente**
   (`index.html:395`, usado en `index.html:449`) — token de larga vida expuesto
   públicamente vía "ver código fuente". Riesgo: uso indebido de la API de
   MailerLite (acceso a lista de suscriptores, envío de spam, cambios de
   configuración) por cualquier tercero que inspeccione la página.
2. 🟡 **[Menor] `SECRET` de generación de promos también expuesto en cliente**
   (`index.html:393`, `'alas7secret2026'`) — cualquiera puede llamar
   `/generate-promo` directamente sin pasar por el flujo del sitio y generar
   códigos ilimitados. Mismo patrón de diseño que el punto 1, pero de menor
   impacto (solo genera códigos promo, no expone datos de suscriptores).
3. 🟡 **[Menor] Enlaces sociales sin destino real** — los íconos de Instagram, X
   y LinkedIn en el footer (`index.html:321-323`) usan `href="#"` como
   placeholder permanente, no un error introducido hoy, pero sigue sin
   corregirse.
4. ⚪ **[Info] Verificación en vivo no realizada** — ver nota en "Sitio en vivo"
   arriba; no se pudo confirmar HTTP 200 real de `alas7.com`, del Payment Link
   de Stripe, ni del endpoint `/generate-promo` por bloqueo de red del propio
   entorno del auditor.

**Verificación adicional:** `node --check` sobre el único bloque `<script>`
inline no reportó errores de sintaxis. No se detectaron bloques `catch` que
oculten errores de forma sospechosa — el único `catch` (en `showPromo()`, línea
511) hace *fallback* visible a un código promo por defecto, comportamiento
esperado, no un error silenciado.

**Recomendación:** (no se corrige nada en esta corrida)
- Prioridad 1: revocar/rotar el token Bearer de MailerLite expuesto en
  `index.html:395` y mover la autenticación con MailerLite al backend del proxy
  de Railway (que el cliente nunca debería ver credenciales de MailerLite).
  Confirmar con MailerLite qué permisos reales tiene un JWT con `scopes: []`
  antes de asumir que el impacto es bajo.
- Prioridad 2: mover también `SECRET` (`X-ALAS7-Secret`) fuera del cliente, o
  como mínimo añadir rate-limiting del lado del proxy a `/generate-promo` ya
  que el secreto es trivialmente extraíble.
- Prioridad 3: completar o quitar los enlaces sociales `href="#"` del footer.
- Dar seguimiento en la próxima corrida a la verificación en vivo de
  `alas7.com`, Stripe y Railway si la política de red del entorno lo permite,
  o pedir que se habilite egress a esos dominios para este auditor.

---
_Generated by [Claude Code](https://claude.ai/code)_
