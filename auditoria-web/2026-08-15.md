# Auditoría web — 2026-08-15

## [08:14 CDMX] Auditor web

**Estado:** problemas encontrados

**Sitio en vivo:** No verificable — el entorno de este auditor tiene bloqueado el egress de red hacia `alas7.com`, `buy.stripe.com` y el proxy de Railway (`gateway answered 403 to CONNECT` en los 5 intentos). Es una limitación del entorno de auditoría, no evidencia de que el sitio esté caído. No se pudo confirmar el código HTTP real de la home ni comparar el HTML servido en vivo contra el último commit de `main`.

**Pago y promo:** Cableado correcto en el código fuente (no probado en vivo por el bloqueo de red anterior):
- El botón Premium (`.js-premium` → `checkPremium()`) apunta al Payment Link real de Stripe `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (índice `index.html:391,470`), coincide con el esperado.
- El JS llama correctamente a `POST {API}/generate-promo` con header `X-ALAS7-Secret` (`index.html:508`) y a `POST {API}/subscribe` (`index.html:447`), con `API = https://gentle-laughter-production-7f2c.up.railway.app`. Coincide con lo esperado.
- **Persiste sin corregir (día ~31 consecutivo, ya reportado en las auditorías del 2026-07-16 al 2026-08-14, PRs #9–#38):** el JWT Bearer completo de MailerLite y el secreto `X-ALAS7-Secret` (`alas7secret2026`) están hardcodeados en texto plano en el `<script>` público (`index.html:393,395`). Cualquiera puede copiarlos desde "Ver código fuente" y usarlos para llamar la API de MailerLite directamente (agregar/leer suscriptores del grupo) o generar códigos promo sin pasar por el sitio.

**Deploy Pages:** Al día. El último workflow run de Pages (`id 28697306776`) terminó `completed/success` en 2026-07-04 06:12:03Z sobre el commit `535f932` (mismo SHA que el HEAD actual de `main`). No hay builds atorados ni fallidos pendientes ahora mismo. (`main` no ha recibido commits nuevos desde ese día — 6 semanas de antigüedad, consistente con que no ha habido cambios de código, no es en sí un problema.)

**Problemas encontrados:**
1. **[Persiste, crítico, sin corregir hace ~31 días]** JWT Bearer de MailerLite y secreto `X-ALAS7-Secret` expuestos en el JS público del cliente (`index.html:393,395`).
2. **[Nuevo hallazgo de proceso]** Hay **30 pull requests de auditoría abiertos y sin mergear** (#9 a #38, del 2026-07-16 al 2026-08-14), todos documentando el mismo hallazgo crítico #1 sin que se haya tomado acción. El auditor sigue en modo solo-avisa por instrucción explícita, pero el volumen de PRs sin revisar sugiere que las notificaciones no se están atendiendo.
3. **[Menor]** Los 3 íconos sociales del footer (Instagram, X, LinkedIn — `index.html:321-323`) tienen `href="#"` sin URL real; no rompen nada pero no llevan a ningún perfil.
4. **[Menor, degradación silenciosa]** En `showPromo()` (`index.html:506-513`), si `POST /generate-promo` falla o no responde, el catch cae silenciosamente a un código estático `'ALAS7-2026'` y aun así redirige a Stripe con `prefilled_promo_code=ALAS7-2026`. Si ese código no existe o expiró en Stripe, el usuario ve un error de Stripe sin que el sitio lo haya anticipado. No hay logging ni aviso visible del fallback.
5. Sintaxis del JS validada con `node --check` sobre el bloque `<script>` extraído: **sin errores**.
6. `CNAME` = `alas7.com` — correcto, no apunta a `vitanews` ni otro dominio.
7. No se encontraron enlaces a dominios inexistentes (tipo `alas7.mx`).

**Recomendación:** (no corregido — solo se reporta)
- Prioridad 1: mover la llamada a MailerLite detrás del backend de Railway (que ya existe y ya maneja `/subscribe`) en vez de exponer el Bearer token completo en el cliente; rotar el token actual ya que lleva ~un mes expuesto públicamente. Mismo tratamiento para `X-ALAS7-Secret`.
- Revisar y cerrar/mergear el backlog de 30 PRs de auditoría (#9–#38) — están inflando el repo y ocultando si hay algo nuevo entre ellos.
- Opcional: mostrar un aviso visible (no solo consola) cuando `/generate-promo` falla, para no enviar a un usuario a Stripe con un código posiblemente inválido.
- Opcional: enlazar los íconos sociales del footer a los perfiles reales o quitarlos si no existen aún.
