## [08:12 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno — el egress de red de esta sesión bloquea por política (`connect_rejected` / 403) las conexiones salientes a `alas7.com`, `buy.stripe.com` y el proxy de Railway. No se pudo confirmar el código HTTP real de la home ni de los enlaces de pago/promo en esta corrida. Recomendación operativa: correr este chequeo desde un entorno con salida a internet sin restringir, o pedirle a Alan una verificación manual puntual (`curl -I https://alas7.com`).

**Pago y promo:** No verificable en vivo (mismo bloqueo de red). Revisión estática del código en `main` (HEAD `535f932`):
- El botón Premium (`checkPremiumPrivacy` → `window.location.href`) apunta correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, consistente con lo documentado.
- El JS llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret` y a `POST {API}/subscribe` con `Authorization: Bearer …`, ambos contra `gentle-laughter-production-7f2c.up.railway.app`, consistente con lo documentado. El cableado en el HTML se ve correcto; falta solo la confirmación en vivo de que esos endpoints respondan 200.
- **HALLAZGO CRÍTICO (seguridad):** `index.html` (línea ~395) tiene **hardcodeado en el JS del cliente** un Bearer token JWT completo usado para autenticar el POST a `/subscribe`. Se decodificó el payload (es información pública una vez expuesta): `sub: 2317050`, `aud: 4`, emitido 2026-05-07, **expira el 2126-05-07 (dentro de 100 años, prácticamente no expira)**. También hay un "secreto" hardcodeado (`SECRET = 'alas7secret2026'`) usado como header `X-ALAS7-Secret` para `/generate-promo` — al estar en el HTML público, no es secreto: cualquiera puede verlo con "ver código fuente". Cualquier visitante puede copiar este token de larga vida y usarlo para llamar directamente a los endpoints del proxy (o a la API de MailerLite si el token es de ahí), sin pasar por el sitio. Esto es explotable hoy mismo y lleva expuesto desde que se integró el flujo de suscripción/promo.

**Deploy Pages:** Al día. El build más reciente (`pages build and deployment` run #35, `Trigger Pages rebuild`) terminó en `success` sobre el commit `535f932`, que es el HEAD actual de `main`. No hay builds atorados ni fallidos pendientes en este momento (el run #34 sobre `1c23348` sí falló, pero fue superado por el rebuild exitoso posterior sobre el mismo contenido). El HTML en el repo coincide con el último commit de `main`.

**Problemas encontrados:**
1. **[Crítico/Seguridad]** Bearer token JWT y "secreto" de API hardcodeados en el JavaScript público de `index.html` (línea ~395), expuestos a cualquier visitante vía "ver código fuente". El JWT no expira hasta el año 2126.
2. **[Menor]** Los íconos de redes sociales en el footer (Instagram, X, LinkedIn) usan `href="#"` sin URL real — quedan como placeholders sin destino funcional.
3. **[No verificado — limitación del entorno]** No se pudo confirmar en vivo que `alas7.com`, el Payment Link de Stripe y los endpoints de Railway respondan 200, por bloqueo de egress de red en esta sesión de auditoría.
4. Sin hallazgos: no hay manejo de errores silencioso más allá de lo esperado (el `catch` en `showPromo()` cae a un código de respaldo `ALAS7-2026` visible al usuario, que es un comportamiento intencional documentado, no un error oculto). CNAME correcto (`alas7.com`). No se encontraron referencias a `alas7.mx` ni otros dominios inexistentes. El JS pasa `node --check` sin errores de sintaxis. `aviso-de-privacidad.html` y `terminos.html` existen y están enlazados correctamente.

**Recomendación:** (PERO NO SE ARREGLÓ)
- Prioridad 1: Rotar inmediatamente el Bearer token de MailerLite/Railway expuesto y el secreto `alas7secret2026`, y mover la autenticación a un endpoint server-side (el proxy de Railway) que no requiera exponer credenciales de larga vida en el cliente — por ejemplo, que el proxy guarde el token y el frontend solo llame a `/subscribe` sin credenciales, o con una clave de corta duración específica del dominio.
- Prioridad 2: Dar seguimiento en un entorno con acceso de red completo para confirmar en vivo que `alas7.com`, el Payment Link de Stripe y los endpoints `/subscribe` y `/generate-promo` responden 200.
- Prioridad 3 (cosmético): completar o quitar los enlaces `href="#"` de redes sociales en el footer.
