# Auditoría web — 2026-08-11

## [08:24 CDMX] Auditor web

**Estado:** problemas encontrados

**Sitio en vivo:** No verificable directamente desde esta corrida. La política
de red saliente de esta sesión de auditoría bloquea (403 del proxy de egress)
las conexiones a `alas7.com`, `buy.stripe.com` y
`gentle-laughter-production-7f2c.up.railway.app`. Esto es una limitación del
entorno del auditor, **no** evidencia de que el sitio esté caído — se
recomienda verificación manual (`curl -I https://alas7.com`) o habilitar esos
dominios en la política de red de este auditor para futuras corridas.

**Pago y promo:** ok en código fuente / no verificable en vivo (mismo bloqueo
de red que arriba). El botón premium apunta correctamente al Stripe Payment
Link (`https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, línea 361 y `STRIPE`
en el JS). El fetch a `/generate-promo` y el POST a `/subscribe` están
cableados contra `https://gentle-laughter-production-7f2c.up.railway.app`
correctamente. **Problema de seguridad encontrado:** el JS del cliente
(visible a cualquiera con "ver código fuente") expone en texto plano:
- El Bearer token JWT completo de MailerLite (`const BEARER = ...`, línea 395).
- El secreto `X-ALAS7-Secret: alas7secret2026` usado para llamar a
  `/generate-promo` (línea 393).

Cualquier visitante puede copiar esas credenciales y llamar directamente a la
API de MailerLite o generar códigos promo sin pasar por el sitio.

**Deploy Pages:** al día. El run más reciente del workflow
`pages-build-deployment` (id `28697306776`) corresponde exactamente al HEAD
actual de `main` (`535f932`, commit "Trigger Pages rebuild"), terminó en
estado `success` en 36s el 2026-07-04T06:11–06:12 UTC. No hay commits nuevos
en `main` desde entonces, así que no hay drift conocido entre el HTML en vivo
y el último commit. El build atorado de 29 min del 3-jul y el build que no se
disparó quedaron resueltos por ese mismo commit; no se ha repetido desde
entonces.

**Problemas encontrados:**
1. **(Seguridad, medio/alto)** Bearer token de MailerLite y secreto
   `X-ALAS7-Secret` hardcodeados en el JS del cliente (`index.html` líneas
   393 y 395) — visibles públicamente en el HTML servido.
2. **(Menor, observabilidad)** En `showPromo()` (índice ~línea 506-513), si el
   fetch a `/generate-promo` falla, el `catch` no registra nada (sin
   `console.error` ni reporte alguno) y sustituye silenciosamente un código
   fijo `ALAS7-2026`. Si el endpoint de promos se cae, nadie se entera —
   los usuarios ven un código que parece válido pero es genérico. El mismo
   patrón sin logging aplica al `catch` de `submitFree()`.
3. **(Cosmético, menor)** Los 3 íconos sociales del footer (Instagram, X,
   LinkedIn, líneas 321-323) usan `href="#"` como placeholder — no llevan a
   ningún perfil real.
4. **No verificable esta corrida:** salud en vivo de alas7.com, del Stripe
   Payment Link y de los endpoints de Railway, por el bloqueo de red descrito
   arriba.

**Verificado sin problemas:** sintaxis del JS válida (`node --check` limpio
sobre el bloque `<script>` extraído); `CNAME` correcto (`alas7.com`, no
`vitanews` ni otro dominio); todos los `href` internos apuntan a archivos que
sí existen en el repo (`aviso-de-privacidad.html`, `terminos.html`); grupo de
MailerLite (`187372665902728776`) consistente con lo documentado.

**Recomendación:** (no corregido — solo se avisa)
- Mover el Bearer token de MailerLite y el secreto `X-ALAS7-Secret` a un
  backend/proxy en vez del cliente; rotarlos ya que están expuestos
  públicamente en el HTML servido desde hace semanas.
- Agregar logging (`console.error` o un reporte a un servicio externo) en los
  `catch` de `/generate-promo` y `/subscribe` para detectar fallos reales del
  backend en vez de enmascararlos con fallbacks silenciosos.
- Verificar manualmente (o desde una red sin este bloqueo) que alas7.com, el
  link de Stripe y los endpoints de Railway respondan 200 en este momento.
- Opcional: quitar o completar los enlaces `href="#"` de los íconos sociales.
