# Auditoría web — A LAS 7 (alas7.com)

## [08:12 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No se pudo verificar directamente. El entorno de ejecución de este auditor bloquea (403, "policy denial") las conexiones salientes a `alas7.com`, `buy.stripe.com` y el proxy de Railway — no es un fallo del sitio, es una restricción de red del sandbox del auditor. Como verificación indirecta: el último build de GitHub Pages para el commit actual de `main` (`535f9320`) terminó en **success** el 2026-07-04 06:11 UTC, y no hay commits nuevos en `main` desde entonces, así que lo desplegado debería coincidir con el HEAD actual. Recomendado: correr este auditor desde un entorno con salida HTTPS habierta a esos dominios, o que Alan confirme con un `curl -I https://alas7.com` manual.

**Pago y promo:** Cableado correctamente en el código, pero no se pudo probar en vivo (mismo bloqueo de red):
- El botón Premium (`checkPremiumPrivacy` → `window.location.href`) apunta a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, coincide con el Payment Link esperado (`index.html:391,361`).
- El JS llama correctamente a `POST {API}/generate-promo` con header `X-ALAS7-Secret` (`index.html:508`) y a `POST {API}/subscribe` con `groups` y `referral_code` (`index.html:447-451`), ambos contra `gentle-laughter-production-7f2c.up.railway.app`.
- El popup de promo tiene fallback silencioso: si `/generate-promo` falla, usa el código fijo `ALAS7-2026` sin registrar el error en ningún lado (`index.html:506-513`). Funciona para el usuario, pero significa que si el endpoint de promo se cae, nadie se entera salvo que alguien note que todos los códigos son iguales.

**Deploy Pages:** Al día. Último build (`pages build and deployment`, run 28697306776) para `main@535f9320` = **success**, 2026-07-04. Sin commits nuevos desde entonces. No hay evidencia de builds atorados o fallidos pendientes de resolver (el fallo del 2026-07-04 05:42 fue anterior al build exitoso de las 06:11, ya resuelto).

**Problemas encontrados:**
1. **[CRÍTICO — seguridad/dinero] Token Bearer de MailerLite expuesto en texto plano en el JS del cliente** (`index.html:395`, variable `BEARER`). Es un JWT real y válido (decodificado: `sub: 2317050`, emitido 2026, `exp` en el año **2126** — prácticamente sin expiración). Cualquiera que abra "ver código fuente" en la landing puede copiar este token y llamar directamente a la API de MailerLite (agregar/gestionar suscriptores) sin pasar por el formulario ni por el proxy de Railway. Esto es una fuga de credencial real, no una simulación — debería rotarse y moverse detrás del proxy backend (que ya existe para `/subscribe` y `/generate-promo`) en vez de vivir en el HTML público.
2. **No se pudo confirmar en vivo que alas7.com, el Stripe Payment Link, ni el endpoint de promo respondan 200** — bloqueo de red del propio auditor, no evidencia de que algo esté roto, pero es un hueco de cobertura de esta corrida.
3. Enlaces `href="#"` sin destino real en los íconos de redes sociales del footer (Instagram, X, LinkedIn — `index.html:321-323`). No rompen nada funcionalmente, pero son enlaces muertos de cara al usuario.
4. Contenido placeholder aún en vivo: los 3 testimonios son texto genérico ("Espacio reservado para un testimonio real...") y el footer tiene un placeholder literal `[ Dirección física — CDMX, México ]` sin rellenar (`index.html:266-277,329`). No es un bug de código, pero es contenido de marcador de posición expuesto al público.
5. `node --check` sobre el JS embebido no encontró errores de sintaxis. El CNAME es correcto (`alas7.com`, no `vitanews` ni otro dominio). No se encontraron URLs a dominios inexistentes tipo `alas7.mx`.

**Recomendación:** Rotar el token Bearer de MailerLite expuesto en `index.html:395` y moverlo al backend (proxy de Railway) en vez de exponerlo en el cliente — es el hallazgo más urgente de esta corrida. Después, revisar los enlaces sociales muertos y el contenido placeholder (testimonios, dirección) antes de que gane más tráfico. PERO NO SE ARREGLA NADA AQUÍ — esto queda documentado para que Alan decida.
