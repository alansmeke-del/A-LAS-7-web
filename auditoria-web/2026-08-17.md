# Auditoría web — 2026-08-17

## [08:18 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno — la política de egress de red de esta sesión bloquea la salida a alas7.com (403 en el proxy de salida, denegación de política de organización, no un fallo del sitio). No pude confirmar el código HTTP real. Recomiendo verificación manual o desde un entorno con acceso de red completo.

**Pago y promo:** No verificable en vivo por la misma razón (buy.stripe.com y el endpoint de Railway también bloqueados por egress — 403). A nivel de código, el cableado se ve correcto:
- El botón Premium (`checkPremiumPrivacy` → `js-premium`) apunta a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, coincide con el Payment Link documentado.
- El popup de promoción llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret`, y en caso de fallo cae de forma silenciosa a un código fijo `ALAS7-2026` sin avisar al usuario ni registrar el error — funcionalmente no rompe la conversión, pero enmascara una posible caída del endpoint.
- El formulario gratuito hace `POST {API}/subscribe` con el group ID `187372665902728776` (coincide con lo documentado) y sí muestra error visible al usuario si falla.

⚠️ **Hallazgo de seguridad (no pedido explícitamente, pero crítico para pago/promo):** en `index.html` (bloque `<script>`, líneas ~391-395) están hardcodeados en texto plano, visibles a cualquiera que vea el código fuente de la página:
- Un **Bearer token JWT completo** de la API de MailerLite (con fecha de expiración en el año ~2126, es decir, prácticamente sin vencer).
- El **secreto `X-ALAS7-Secret`** (`alas7secret2026`) que protege el endpoint `/generate-promo`.

Cualquier visitante puede copiar estas credenciales desde "Ver código fuente" y usarlas para llamar directamente a la API de MailerLite (agregar/leer suscriptores del grupo) o generar códigos de promoción ilimitados sin pasar por la landing. Esto no tumba el sitio, pero es una exposición de credenciales en el cliente — vale la pena revisarlo con prioridad alta aunque esté fuera del alcance estricto de "detectar caídas".

**Deploy Pages:** Al día. El último build de GitHub Pages (run 28697306776, commit `535f9320`) terminó en `success` el 2026-07-04T06:11:27Z, y ese commit es exactamente el HEAD actual de `main` — no hay commits nuevos sin desplegar desde entonces. El incidente previo del 3-jul (un build que no se disparó y otro que quedó colgado ~29 min, commit `1c23348b` con conclusion `failure`) ya fue resuelto por el commit "Trigger Pages rebuild (previous build stuck)" y el build subsecuente fue exitoso. Sin pendientes de deploy hoy.

**Problemas encontrados:**
1. **Credenciales expuestas en el cliente** (severidad alta): Bearer JWT de MailerLite y secreto `X-ALAS7-Secret` hardcodeados en el JS inline de `index.html`, visibles en el código fuente público.
2. **Enlaces sociales rotos/placeholder** (severidad baja): los tres íconos del footer (Instagram, X, LinkedIn) usan `href="#"` sin destino real — no llevan a ningún perfil.
3. **Fallo silencioso en el popup de promo** (severidad baja-media): si `/generate-promo` falla, el código cae a `ALAS7-2026` fijo sin loguear ni avisar — un usuario podría recibir un código no único sin que nadie se entere de que el endpoint está caído.
4. **Verificación en vivo bloqueada** (limitación de este auditor, no del sitio): no pude confirmar por HTTP directo que alas7.com, el Stripe Payment Link ni el endpoint de Railway respondan hoy, por restricción de red del entorno de ejecución.

Sin hallazgos en: integridad de sintaxis JS (`node --check` pasa limpio sobre el `<script>` inline), archivos locales referenciados (`aviso-de-privacidad.html`, `terminos.html`, `assets/logo7-v2.png` — todos existen), CNAME (contiene correctamente `alas7.com`, sin rastro de `vitanews` u otros dominios), ni enlaces a dominios inexistentes tipo `alas7.mx`.

**Recomendación:** (PERO NO SE CORRIGIÓ — solo se reporta)
1. **Prioridad alta:** mover el Bearer token de MailerLite y el secreto `X-ALAS7-Secret` fuera del cliente. Lo correcto es que el proxy de Railway agregue esas credenciales del lado del servidor y que la landing solo llame a `/subscribe` y `/generate-promo` sin pasar secretos — hoy cualquier persona puede extraerlos del código fuente.
2. Rotar el Bearer token de MailerLite ya que ha estado expuesto públicamente en el HTML servido.
3. Rellenar los `href="#"` del footer con las URLs reales de redes sociales, o quitar los íconos si aún no existen esas cuentas.
4. Considerar loguear (aunque sea con un `console.warn` o un evento de analítica) cuando `/generate-promo` falla y se usa el código de respaldo, para poder detectar caídas del endpoint de promo sin depender de que un usuario reporte un código repetido.
5. Verificar manualmente (o desde un entorno con acceso de red saliente) que alas7.com, el Payment Link de Stripe y el endpoint de Railway efectivamente respondan 200, ya que este auditor no pudo confirmarlo hoy por restricción de egress.
