# Auditoría web — 2026-07-17

## [08:10 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno — la política de red de esta sesión bloquea (403 de política de egress, no un error del sitio) las conexiones salientes a `alas7.com`, `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app`. No es evidencia de que el sitio esté caído; es una limitación del entorno de auditoría. Recomiendo que la corrida se haga desde un entorno con egress abierto a esos hosts, o que se agregue un check externo (UptimeRobot, GitHub Action con curl, etc.) que no dependa de este agente.

**Pago y promo:** ok (cableado) / ⚠️ hallazgo crítico de seguridad — ver abajo
- El botón "Ir a Premium" y el CTA del popup de promo apuntan correctamente al Payment Link de Stripe `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (constante `STRIPE` en `index.html:391`, usada en `checkPremium()` y en `promoCta.href`). Cableado correcto, no verificable en vivo por el bloqueo de red.
- El JS llama correctamente a `POST {API}/generate-promo` con header `X-ALAS7-Secret` (`index.html:508`) y a `POST {API}/subscribe` con `Authorization: Bearer …` (`index.html:447-451`), ambos contra el proxy de Railway. Cableado correcto, no verificable en vivo por el bloqueo de red.
- **CRÍTICO — credenciales expuestas en el cliente:** `index.html` líneas 393 y 395 embeben en texto plano, visibles a cualquiera que haga "ver código fuente":
  - `SECRET = 'alas7secret2026'` — el secreto `X-ALAS7-Secret` que protege `/generate-promo`.
  - `BEARER = 'eyJ0eXAi...'` — un JWT completo de la API de MailerLite, con `exp` fijado muy lejos en el futuro (prácticamente no expira).
  Cualquier visitante puede copiar estos valores y llamar ambos endpoints directamente, sin pasar por la landing: generar códigos de promoción ilimitados (rompe el descuento/precio del plan Premium) y golpear la API de MailerLite con el bearer token filtrado (alta de suscriptores arbitraria, abuso de cuota, o peor si el scope del token permite más que `subscribe`). Esto está en producción desde el commit `cb6f7a9` (2026-07-03) — llevaría ~2 semanas expuesto en vivo.

**Deploy Pages:** al día — el build de Pages más reciente (run `28697306776`) corresponde al commit `535f932` ("Trigger Pages rebuild"), que es el HEAD actual tanto de `main` como del branch de trabajo, y terminó con `conclusion: success` el 2026-07-04 06:11 UTC. El build atorado/fallido del 3-jul (`1c23348`, `conclusion: failure`, 05:42 UTC) fue efectivamente reemplazado por este rebuild exitoso. No hay desfase entre el HTML en vivo (asumiendo que el deploy corrió) y el último commit de `main`.

**Problemas encontrados:**
1. 🔴 Crítico: `SECRET` (X-ALAS7-Secret) y `BEARER` (JWT de MailerLite) hardcodeados en el JS del cliente en `index.html:393,395` — visibles para cualquier visitante, permiten abuso del endpoint de promociones y llamadas directas a la API de MailerLite fuera del sitio.
2. Verificación de salud en vivo, Stripe y Railway bloqueada por la política de red de este entorno de auditoría (403 de política, no del sitio) — no se pudo confirmar HTTP 200 real en `alas7.com` ni respuesta real de los endpoints.
3. Menor: 3 links de redes sociales en el footer (Instagram, X, LinkedIn) usan `href="#"` sin URL real y sin handler de JS — quedan como placeholders muertos (`index.html:321-323`). Los demás `href="#"` (nav "Gratis", footer "Suscríbete gratis") sí están interceptados por JS (`js-open-free`) y no son un problema.
4. `node --check` sobre el `<script>` inline no reportó errores de sintaxis. `CNAME` contiene `alas7.com` (correcto, no `vitanews`). No se encontraron referencias a `alas7.mx` ni otros dominios inexistentes.

**Recomendación:** (PERO NO SE CORRIGIÓ NADA)
- Prioridad 1: sacar `SECRET` y `BEARER` del HTML/JS del cliente. Mover la llamada a `/generate-promo` y `/subscribe` detrás del proxy de Railway para que el secreto/token vivan solo en el servidor, o al menos rotar ambas credenciales ya (el JWT filtrado sigue siendo válido) y usar un endpoint público sin secreto embebido, con rate-limiting server-side.
- Dar acceso de red desde el entorno de auditoría a `alas7.com`, `buy.stripe.com` y el subdominio de Railway, o complementar con un monitor externo (uptime check) para que esta auditoría pueda confirmar salud real del sitio y de los endpoints de pago/promo.
- Opcional/menor: poner URLs reales o quitar los 3 íconos de redes sociales del footer si no hay cuentas activas todavía.
