## [08:19 CDMX] Auditor web

**Estado:** problemas encontrados

**Sitio en vivo:** No verificable directamente — el sandbox de este auditor tiene bloqueado el egress de red hacia `alas7.com`, `buy.stripe.com` y el proxy de Railway (política de red del entorno, `connect_rejected`/403 en el proxy de salida). No pude hacer un `curl`/fetch real a alas7.com para confirmar 200 OK. Como proxy: el último workflow de "pages build and deployment" en `main` (run #35, commit `535f932`) terminó en `success` y coincide con el HEAD actual de `main`, así que el contenido publicado debería reflejar el último commit. Recomendado: que alguien con acceso de red normal confirme manualmente que https://alas7.com carga.

**Pago y promo:** No verificable en vivo por el mismo bloqueo de red (no se pudo hacer POST/GET real a Stripe ni al endpoint de Railway). Revisión estática del código en `index.html` (HEAD de `main`):
- El botón premium (`id="promoCta"` y el flujo `checkPremium()`) apunta correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` — coincide con el Payment Link esperado.
- El JS llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret` y a `POST {API}/subscribe`, con `API = https://gentle-laughter-production-7f2c.up.railway.app` — el cableado del código es correcto y consistente con lo documentado.
- **Hallazgo de seguridad (severo):** en el `<script>` de `index.html` (línea ~395) hay un **JWT Bearer token de MailerLite hardcodeado en texto plano**, enviado como header `Authorization: Bearer ...` directamente desde el navegador en cada llamada a `/subscribe`. Decodificando el payload del JWT, su `exp` corresponde a aproximadamente el año 2126 (prácticamente no expira) y `scopes` viene vacío. Cualquier visitante puede ver el código fuente de la página, copiar ese token, y usarlo directamente contra la API de MailerLite fuera del sitio (potencialmente para leer/manipular la cuenta según el alcance real del token). Esto ya estaba así antes de esta corrida (no es un cambio reciente), pero no había sido documentado en ninguna auditoría previa — es la primera vez que corre este auditor (no existía carpeta `auditoria-web/`).

**Deploy Pages:** Al día. El run #35 (`Trigger Pages rebuild`, commit `535f932`) es el más reciente y terminó en `success`, y `535f932` es el HEAD actual de `main`. El problema de builds atorados/fallidos del 3-jul (run #34 `failure`, y reportes de un build que no se disparó o quedó colgado 29 min) ya quedó resuelto por ese rebuild manual — no hay evidencia de builds atorados pendientes ahora.

**Problemas encontrados:**
1. (Severo, preexistente) JWT Bearer token de MailerLite hardcodeado en el JS público de `index.html`, con expiración ~año 2126 y sin rotación — expone la cuenta de MailerLite a cualquiera que vea el código fuente.
2. (Informativo/limitación del auditor) No se pudo verificar en vivo alas7.com, el Payment Link de Stripe, ni el endpoint de Railway porque el entorno de este auditor bloquea el egress de red hacia esos dominios. Esta corrida se apoyó en: (a) el estado del build de GitHub Pages vía API, y (b) revisión estática del HTML/JS del HEAD de `main`. No es lo mismo que confirmar que el sitio responde 200 ahora mismo.
3. (Menor) Los íconos sociales del footer (Instagram, X, LinkedIn) usan `href="#"` sin URL real de destino — no rompen nada pero son enlaces muertos.
4. Sin hallazgos: no hay referencias a `alas7.mx` ni a dominios viejos, el `CNAME` dice correctamente `alas7.com`, `aviso-de-privacidad.html` y `terminos.html` (referenciados desde el footer) existen en el repo, y el JS interno pasó `node --check` sin errores de sintaxis. El manejo de errores del `fetch` a `/subscribe` y `/generate-promo` no es silencioso: sí muestra mensaje de error al usuario o hace fallback visible (código promocional por defecto).

**Recomendación:** Rotar/revocar el token Bearer de MailerLite expuesto y mover esa llamada para que el proxy de Railway inyecte el secreto del lado del servidor (el navegador no debería necesitar nunca ese Bearer token). También pedir que alguien con acceso de red normal confirme manualmente que alas7.com, el Payment Link de Stripe y el endpoint /generate-promo responden correctamente, ya que este auditor no pudo hacerlo por restricciones de red del entorno. Opcionalmente, poner URLs reales (o quitar los íconos) en los enlaces sociales del footer. PERO NO LO ARREGLES — esto es solo un reporte.
