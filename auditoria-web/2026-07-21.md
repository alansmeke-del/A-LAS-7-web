# Auditoría web — 2026-07-21

## [08:12 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No se pudo verificar directamente en esta corrida. El entorno de ejecución de este auditor tiene bloqueado, por política de red del sandbox, el tráfico saliente hacia `alas7.com`, `buy.stripe.com` y el proxy de Railway (el gateway devuelve 403 al intentar el CONNECT). Esto **no es evidencia de que el sitio esté caído** — es una limitación de este entorno, no del sitio. Recomiendo una verificación manual o desde un entorno sin esa restricción.

**Pago y promo:** ok en el código (cableado correcto), con una excepción de seguridad importante:
- El link de Stripe `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` está presente y correctamente enlazado tanto en `checkPremium()` (botones "Premium") como en el CTA del popup de promo (con `prefilled_promo_code`). Coincide con el link esperado.
- El endpoint `/generate-promo` se sigue llamando correctamente con el header `X-ALAS7-Secret`.
- El formulario `/subscribe` está bien cableado (email + groups + referral_code).
- No pude confirmar en vivo que estos endpoints respondan 200 (ver limitación de red arriba).
- **Hallazgo de seguridad:** en `index.html` líneas 393 y 395, el JS tiene hardcodeados en texto plano — visibles para cualquier visitante vía "Ver código fuente" — (1) el secreto `X-ALAS7-Secret` (`alas7secret2026`) y (2) un Bearer JWT completo usado para autenticar el POST a `/subscribe`. Decodifiqué el payload del JWT (sin verificar firma): `sub=2317050`, `scopes=[]`, y el campo `exp` corresponde aproximadamente al año 2126 — en la práctica, un token que no expira. Esto es preexistente desde el commit `cb6f7a9` (4-jul-2026), no es una regresión de hoy, pero sigue siendo una credencial viva expuesta públicamente que cualquiera puede copiar y usar directamente contra el backend, sin pasar por el proxy de Railway.

**Deploy Pages:** al día. El último run de "pages build and deployment" (id `28697306776`) terminó en `success` en 36s, para el commit `535f9320`, que es exactamente el HEAD actual de `main`. El incidente del 3-jul (un build no se disparó y otro quedó colgado 29 min) ya fue resuelto ese mismo día con el commit "Trigger Pages rebuild (previous build stuck)". No hay builds pendientes ni fallidos después de ese.

**Problemas encontrados:**
1. **[Limitación de este entorno]** No se pudo confirmar en vivo que `alas7.com`, el link de Stripe y el endpoint de Railway respondan — bloqueado por la política de salida de este sandbox, no es evidencia de falla real del sitio.
2. **[Seguridad]** Bearer JWT y secreto `X-ALAS7-Secret` hardcodeados en el HTML público (líneas 393/395 de `index.html`), visibles para cualquier visitante; el JWT no tiene expiración práctica (~año 2126). Preexistente desde el 4-jul-2026, no es nuevo de hoy.
3. **[UX/enlaces]** Los 3 íconos de redes sociales del footer (Instagram, X, LinkedIn) usan `href="#"` sin URL real — son enlaces muertos.
4. **[Manejo de errores silencioso]** En `showPromo()`, si la llamada a `/generate-promo` falla, el código cae silenciosamente a un valor fijo `'ALAS7-2026'` sin loggear ni avisar a nadie. Si ese código no existe como promo code real en Stripe, el usuario ve un "descuento aplicado" que puede fallar en el checkout sin que nadie se entere del problema.
5. **[Contenido, no técnico]** Placeholders sin llenar: la dirección física del footer sigue como `[ Dirección física — CDMX, México ]` y los 3 testimonios son texto de relleno. No es un bug, pero la dirección física suele ser requisito legal/anti-spam.

Verificaciones sin hallazgos: `node --check` sobre el JS extraído del `<script>` no reportó errores de sintaxis. El `CNAME` dice correctamente `alas7.com` (no `vitanews` ni otro dominio). No se encontraron referencias a dominios inexistentes tipo `alas7.mx`. `aviso-de-privacidad.html` y `terminos.html` existen y están enlazados correctamente.

**Recomendación:** (qué se debería arreglar, PERO NO SE ARREGLÓ EN ESTA CORRIDA)
- Prioridad alta: sacar el Bearer JWT y el `X-ALAS7-Secret` del cliente — que la autenticación hacia MailerLite/backend viva solo del lado del proxy de Railway, no en el navegador. Rotar el JWT actual ya que quedó público desde julio.
- Verificar manualmente (o desde un entorno sin la restricción de red descrita) que `alas7.com`, el link de Stripe y el endpoint de Railway respondan 200 hoy mismo.
- Enlazar los íconos de redes sociales a URLs reales o quitarlos si no hay cuentas activas.
- Agregar algún aviso visible o log cuando `/generate-promo` falla y se usa el código de respaldo, para detectar promo codes de Stripe rotos.
- Completar la dirección física real en el footer.
