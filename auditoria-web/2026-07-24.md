## [08:11 CDMX] Auditor web

**Estado:** problemas encontrados

**Sitio en vivo:** No se pudo verificar. El entorno de ejecución de esta corrida bloquea por política de red las salidas hacia alas7.com, buy.stripe.com y el proxy de Railway (el gateway responde 403/`connect_rejected` en las tres). No es evidencia de que el sitio esté caído — es una limitación de este entorno de auditoría. Recomendado verificar con un ping/monitor externo (UptimeRobot, etc.) o desde un entorno con salida a internet abierta.

**Pago y promo:** ok en código, no verificable en vivo esta corrida.
- El botón Premium apunta a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` — coincide con el Payment Link esperado (`index.html:391,361`).
- El popup de promo llama correctamente a `POST {API}/generate-promo` con header `X-ALAS7-Secret` (`index.html:508`).
- El formulario gratuito hace `POST {API}/subscribe` con el payload esperado, incluyendo `referral_code` (`index.html:447-451`).
- Todo el cableado JS es sintácticamente correcto (`node --check` limpio sobre el `<script>` extraído).
- ⚠️ **Hallazgo de seguridad (no nuevo, pero sin remediar):** el token Bearer JWT de MailerLite está hardcodeado en texto plano en el JS del cliente (`index.html:395`, variable `BEARER`), visible para cualquiera que haga "ver código fuente". Igualmente el secreto `X-ALAS7-Secret` (`alas7secret2026`, línea 393) está expuesto en el cliente, lo que anula su propósito de anti-abuso: cualquiera puede leerlo y llamar a `/generate-promo` directamente sin pasar por el flujo del sitio. Confirmado en el historial de git que este patrón viene desde los commits `cb6f7a9`/`7e79b6f` (redise��o "de-runtimed"), no es de hoy. Riesgo: alguien podría usar el Bearer token para interactuar directamente con la cuenta de MailerLite (agregar/borrar suscriptores del grupo, etc.) fuera del formulario del sitio.

**Deploy Pages:** al día. El último build de Pages para el HEAD actual de `main` (commit `535f932`, "Trigger Pages rebuild") corrió y **completó con éxito** el 2026-07-04 06:12 UTC (36s de duración). No hay commits nuevos a `main` desde entonces, así que no hay nada pendiente ni atorado. (El build fallido/atorado del 3-jul mencionado en la consigna ya quedó resuelto por ese rebuild.)

**Problemas encontrados:**
1. No se pudo verificar en vivo alas7.com, el Payment Link de Stripe ni el proxy de Railway — bloqueo de red del entorno de auditoría, no confirmación de falla real.
2. Bearer token de MailerLite y el secreto `X-ALAS7-Secret` expuestos en texto plano en el JS del cliente (`index.html:393,395`) — sin remediar desde el rediseño de julio.
3. Manejo de error silencioso en `showPromo()` (`index.html:506-513`): si `/generate-promo` falla, el catch cae a un código estático `'ALAS7-2026'` sin avisar al usuario ni dejar rastro — si el endpoint está caído, el visitante recibe un código que probablemente no sea válido en Stripe, sin ningún indicio del error.
4. Cosmético/menor: los íconos de redes sociales del footer (Instagram, X, LinkedIn) son `href="#"` sin destino real (`index.html:321-323`). Y la variable `SESSION_KEY = 'vita_promo_seen'` (línea 396) es un remanente del nombre anterior del proyecto ("Vita"), no afecta funcionalidad pero es inconsistencia de naming.
5. CNAME correcto: dice `alas7.com` ✓. No se encontraron referencias a dominios incorrectos (tipo alas7.mx) en el HTML.

**Recomendación:** (PERO NO SE CORRIGIÓ NADA — solo aviso)
- Prioridad alta: mover el Bearer token de MailerLite fuera del cliente. Debería vivir solo en el proxy de Railway (server-side), y el cliente no debería necesitar ni ver ese token — el proxy ya existe justo para esto. Rotar el token actual una vez movido, ya que quedó expuesto públicamente en el HTML servido.
- El secreto `X-ALAS7-Secret` tiene el mismo problema: si su intención es gatear el endpoint de promo, hoy no gatea nada porque es público. Considerar rate-limiting/validación en el servidor en su lugar.
- Agregar manejo visible (o al menos logging) cuando `/generate-promo` falla, en vez de fallback silencioso a un código fijo.
- Confirmar salud real de alas7.com/Stripe/Railway desde un canal con acceso a internet (monitor externo), ya que esta corrida no pudo confirmarlo.
