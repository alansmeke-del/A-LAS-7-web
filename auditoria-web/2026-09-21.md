# Auditoría web — 2026-09-21

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados (1 hallazgo de seguridad relevante; sin confirmación de caída de sitio/pago)

**Sitio en vivo:** No verificado — el entorno de ejecución de este auditor tiene bloqueado el egress de red hacia dominios externos (alas7.com, buy.stripe.com, railway.app devuelven "connect_rejected / 403" del proxy de salida). No se pudo hacer una petición HTTP real a alas7.com en esta corrida. Recomendado verificar manualmente o habilitar egress para el auditor.

**Pago y promo:** No se pudo probar en vivo (mismo bloqueo de red). Revisión estática del código en `index.html` (HEAD actual de `main`, commit `535f9320`):
- El botón Premium y el CTA del popup de promo usan `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` — coincide con el Payment Link esperado (`STRIPE` const, línea 391; usado en líneas 361 y 470).
- El JS llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret` (línea 508) y a `POST {API}/subscribe` con `Authorization: Bearer …` (línea 447), donde `API = https://gentle-laughter-production-7f2c.up.railway.app` (línea 392) — coincide con el endpoint esperado y está bien cableado.
- El formulario de suscripción gratuita está correctamente conectado al POST `/subscribe`, incluyendo el `referral_code` desde `?ref=`.

**Deploy Pages:** Al día. El último build de GitHub Pages (run #35, "Trigger Pages rebuild (previous build stuck)", completado 2026-07-04 06:12 UTC) terminó en **success** y su `head_sha` (`535f9320…`) coincide exactamente con el HEAD actual de `main`. No hay commits en `main` posteriores a esa fecha, así que no hay desfase entre el HTML en vivo (asumiendo que el bloqueo de red no oculta un problema) y el último commit. El incidente del 3-jul (build no disparado + build colgado 29 min) ya quedó resuelto por ese mismo commit de rebuild.

**Problemas encontrados:**
1. **(Seguridad, no es regresión de hoy pero sigue expuesto) Credenciales hardcodeadas en JS del lado del cliente**, visibles en el código fuente de `index.html` para cualquier visitante:
   - `SECRET = 'alas7secret2026'` (header `X-ALAS7-Secret` del endpoint `/generate-promo`, línea 393).
   - Un JWT `Bearer` completo para el endpoint `/subscribe` (línea 395), cuyo campo `exp` decodifica a un timestamp en el año 2126 (prácticamente no expira nunca).
   Cualquiera puede copiar estos valores desde "ver código fuente" y llamar directamente a la API de Railway: generar códigos de promo ilimitados o intentar abusar del endpoint de suscripción con el Bearer token. Esto no bloquea el sitio ni el pago hoy, pero es una superficie de abuso activa sobre el dinero (códigos de descuento) y sobre la base de suscriptores.
2. **(Cosmético, preexistente) Enlaces sociales sin destino real:** los íconos de Instagram/X/LinkedIn en el footer (`href="#"`, líneas 321–323) no apuntan a ningún perfil — no rompen nada funcionalmente pero son links "muertos" de cara al usuario.
3. **(Menor) Manejo de error silencioso en el popup de promo:** si `POST /generate-promo` falla (endpoint caído, timeout, etc.), el `catch` en `showPromo()` (línea 511-513) no registra ni reporta nada — simplemente sustituye por el código estático `'ALAS7-2026'` sin avisar a nadie. Si Railway cae, todos los visitantes verían silenciosamente el mismo código de respaldo indefinidamente sin que se note en ningún log.
4. **No es un hallazgo nuevo, pero limita esta auditoría:** no se pudo confirmar en vivo que alas7.com responda 200, que el Payment Link de Stripe esté activo, ni que `/generate-promo` y `/subscribe` respondan correctamente, por el bloqueo de red saliente del entorno del auditor. Todo lo anterior sobre pago/promo es solo revisión estática de código, no una prueba end-to-end.

**Recomendación:** (informativo — no se corrigió nada)
- Prioridad alta: mover `SECRET` y el `BEARER` fuera del JS del cliente (proxy/backend que firme la petición, o al menos rotar el JWT y usar uno de corta duración) — ahora mismo cualquiera puede leerlos desde el código fuente público.
- Agregar algún tipo de log/alerta cuando `/generate-promo` falla y se usa el código de respaldo, para detectar caídas del backend sin depender de que un usuario reporte el problema.
- Reemplazar o quitar los enlaces sociales `href="#"` del footer.
- Habilitar acceso de red saliente para este auditor (o correr las verificaciones HTTP en vivo desde un runner con egress) para que las próximas corridas puedan confirmar sitio/pago/promo end-to-end en vez de solo revisión estática.
