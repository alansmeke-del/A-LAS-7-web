# Auditoría web — 2026-07-15

## [08:11 CDMX] Auditor web
**Estado:** limpio (con salvedad de verificación en vivo)
**Sitio en vivo:** No verificable esta corrida — la política de red de este entorno bloqueó (403 en el CONNECT) las conexiones salientes a `alas7.com`, `buy.stripe.com` y el proxy de Railway. No es evidencia de caída, es una limitación del entorno de auditoría; se recomienda confirmar manualmente o correr el auditor desde un entorno con esos dominios permitidos.
**Pago y promo:** Cableado correcto en el código (no verificado en vivo por el bloqueo de red):
  - Botón premium (`checkPremium()`) redirige a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` — coincide con el Payment Link esperado (index.html:391, 470).
  - Promo (`showPromo()`) hace `POST` a `${API}/generate-promo` con header `X-ALAS7-Secret` (index.html:508) y precarga el código en el CTA de Stripe (`?prefilled_promo_code=...`).
  - Suscripción gratis (`submitFree()`) hace `POST` a `${API}/subscribe` con Bearer token, incluyendo `referral_code` desde el input oculto (index.html:447-451).
**Deploy Pages:** Al día. El último build de GitHub Pages para `main` (run 28697306776, 2026-07-04T06:11:27Z) terminó en `success` sobre el commit `535f9320`, que es exactamente el HEAD actual de `origin/main`. El build fallido previo (`1c23348b`, 05:42) del 3-jul ya quedó resuelto por el commit de rebuild forzado — no hay desfase entre el HTML publicado y el último commit.
**Problemas encontrados:**
  1. Manejo de error silencioso en `showPromo()` (index.html:506-513): si `/generate-promo` falla, el `catch` no registra nada (ni `console.error`) y cae a un código fijo `'ALAS7-2026'` hardcodeado. Si el endpoint de promos se cae, la landing seguiría mostrando un código funcional en apariencia sin ninguna señal de que el servicio real está roto.
  2. Enlaces sociales muertos: los íconos de Instagram, X y LinkedIn en el footer usan `href="#"` sin URL real (index.html:321-323). No rompen nada pero no llevan a ningún lado.
  3. Detalle cosmético: la clave de `sessionStorage` para el popup de promo sigue llamándose `vita_promo_seen` (index.html:396), residuo de un nombre de marca anterior — no afecta funcionalidad.
  4. El secreto del header de promo (`X-ALAS7-Secret: 'alas7secret2026'`, index.html:393) está hardcodeado en el JS del cliente y por tanto visible en el código fuente de la página — no ofrece protección real si el endpoint depende de él para autorización.
  5. No se verificó en vivo la salud de `alas7.com`, del Payment Link de Stripe ni del endpoint de Railway por el bloqueo de red del entorno (ver arriba).
**Recomendación:** Confirmar manualmente (o desde un entorno sin el bloqueo de red) que `alas7.com`, el Payment Link de Stripe y `/subscribe` + `/generate-promo` respondan 200. Agregar al menos un `console.error` en el catch de `showPromo()` para no perder visibilidad si el servicio de promos falla. Los enlaces sociales y el nombre de la sessionStorage key son cosméticos, se pueden dejar para cuando haya tiempo. No se tocó código — solo diagnóstico.
