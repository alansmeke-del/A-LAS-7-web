# Auditoría web — 2026-08-14

## [08:37 CDMX] Auditor web
**Estado:** problemas encontrados (menores, no críticos)

**Sitio en vivo:** No verificable — el entorno de ejecución de este auditor tiene
bloqueado el egress de red saliente hacia dominios externos (alas7.com,
buy.stripe.com, gentle-laughter-production-7f2c.up.railway.app devuelven
`EGRESS_BLOCKED` / 403 en el proxy). No pude hacer un `curl`/fetch real al sitio
en vivo ni al Stripe Payment Link ni al endpoint de promo. Esto es una limitación
del entorno, no un hallazgo sobre el sitio — recomiendo que una corrida futura con
acceso a red confirme el 200 OK real.

**Deploy Pages:** Al día. Verificado vía GitHub Actions API:
- El HEAD actual de `main` (`535f9320`, "Trigger Pages rebuild (previous build stuck)")
  coincide exactamente con el `head_sha` del último run de `pages-build-deployment`
  (run 28697306776), que terminó `completed`/`success` el 2026-07-04T06:11:27Z.
- El incidente del 3-jul mencionado (build no disparado + build colgado 29 min) ya
  quedó resuelto: el run anterior sobre el commit `1c23348` (`Merge branch 'dev'`)
  falló (`completed`/`failure`) a las 05:42 UTC, y el commit siguiente
  "Trigger Pages rebuild" corrigió el build a las 06:11 UTC, que es el que sigue
  vigente hoy. No hay commits nuevos en `main` desde entonces (más de un mes sin
  cambios), así que no hay drift entre el HTML en vivo esperado y el último commit.

**Pago y promo (revisión estática del código, sin poder golpear la red):**
- El link de Stripe en el HTML (`STRIPE` const, index.html:391) es exactamente
  `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`, el Payment Link correcto. El
  botón premium (`checkPremium()`) lo usa via `window.location.href` tras pasar el
  gate de privacidad — cableado correcto.
- El endpoint de promo (`API` const, index.html:392) apunta a
  `https://gentle-laughter-production-7f2c.up.railway.app`, y `showPromo()` hace
  `POST /generate-promo` con header `X-ALAS7-Secret`. El submit del form gratis
  hace `POST /subscribe` con `Authorization: Bearer` + `groups`/`referral_code` —
  cableado correcto según el código.
- **Hallazgo menor:** en `showPromo()` (index.html:506-513), si el fetch a
  `/generate-promo` falla o el endpoint no responde, el `catch` cae silenciosamente
  a un código fijo `'ALAS7-2026'` sin avisar al usuario ni loguear el error. Si ese
  código no existe como promo code real en Stripe, el usuario llega al checkout con
  un `prefilled_promo_code` inválido — potencial fricción/pérdida de conversión sin
  que nadie se entere de que el endpoint estaba caído. No lo corregí (fuera de
  alcance de este auditor).

**Integridad del HTML/JS:**
- `node --check` sobre el script embebido: sintaxis OK, sin errores.
- CNAME correcto: contiene exactamente `alas7.com` (no `vitanews` ni otro dominio).
- No se encontraron referencias a dominios inexistentes (`alas7.mx` u otros).
- Los `href="#"` de la barra de suscripción ("Gratis"/"Suscríbete gratis") son
  intencionales — abren el modal vía JS (`js-open-free`), no son links rotos.
- **Hallazgo menor:** los 3 íconos de redes sociales en el footer (Instagram, X,
  LinkedIn — index.html:321-323) tienen `href="#"` sin URL real. Son placeholders
  visuales sin destino funcional.
- Los `<img src="assets/logo7-v2.png">` (5 usos) resuelven correctamente contra el
  archivo existente en `assets/`.

**Problemas encontrados:**
1. (Menor) `showPromo()` traga en silencio los errores del endpoint `/generate-promo`
   y cae a un código de promo estático sin avisar al usuario — riesgo de checkout
   con promo inválido si Railway está caído.
2. (Menor/cosmético) Links de redes sociales en el footer son placeholders (`href="#"`)
   sin URL real.
3. (Limitación del entorno, no del sitio) No se pudo verificar en vivo el 200 OK de
   alas7.com, la respuesta del Stripe Payment Link, ni la respuesta del endpoint de
   promo — el egress de red del auditor está bloqueado hacia esos dominios.

**Recomendación:** Nada crítico ni urgente. Cuando se quiera pulir: (a) que
`showPromo()` muestre algún aviso o registre el fallo (p. ej. `console.warn`) en
vez de fallback silencioso al código fijo, y/o que el código fijo de respaldo sea
uno que sí exista en Stripe; (b) poner URLs reales en los íconos de redes sociales
o quitarlos si no hay cuentas activas. PERO NO LO ARREGLES — esto es solo un aviso,
Alan decide qué hacer. También recomiendo que una corrida futura del auditor con
acceso de red sin restricciones confirme el estado 200 real del sitio en vivo, ya
que esta corrida no pudo hacerlo.
