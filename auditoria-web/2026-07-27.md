# Auditoría web — 2026-07-27

## [08:12 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno. El proxy de red de esta sesión bloqueó (403, política de egress) las conexiones salientes a `alas7.com`, `buy.stripe.com` y el endpoint de Railway — no es un resultado del sitio, es una restricción del entorno de auditoría. No se pudo confirmar el código HTTP real de la home. Recomendación operativa: correr este chequeo desde un entorno con salida de red permitida a esos tres hosts, o pedirle a Alan una verificación manual puntual (`curl -I https://alas7.com/`).

**Pago y promo:** No verificable en vivo (mismo bloqueo de red que arriba). Revisión estática del código sí fue posible:
- El botón Premium y el CTA del popup de promo sí apuntan al Stripe Payment Link correcto: `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (líneas 361 y 391 de `index.html`).
- El JS llama correctamente a `POST {API}/generate-promo` con el header `X-ALAS7-Secret` y a `POST {API}/subscribe` con el payload `{email, groups, referral_code}`, donde `API = https://gentle-laughter-production-7f2c.up.railway.app` (líneas 392, 447-451, 508).
- El formulario de suscripción gratuita está cableado al endpoint `/subscribe` y el gate de privacidad (checkbox → botón disabled) funciona como está documentado en CLAUDE.md.
- **Hallazgo de seguridad (preexistente, no es de hoy):** el JS embebe en texto plano un Bearer JWT completo de la API de MailerLite (`const BEARER = '...'`, línea 395) y un "secreto" de promo (`SECRET = 'alas7secret2026'`, línea 393). Ambos son visibles para cualquier visitante vía "ver código fuente" — no son secretos reales una vez publicados. El JWT tiene `exp` fijado a un timestamp del año ~2126, es decir, prácticamente no expira. Cualquiera puede copiar ese token y llamar directamente a la API de MailerLite con los mismos scopes que tiene la landing (alta a grupos, etc.), sin pasar por el proxy de Railway. Esto existe desde el commit `cb6f7a9` (2026-07-04), por lo que lleva ~3 semanas expuesto en producción. Introducido en el rediseño, no en un cambio reciente.

**Deploy Pages:** Al día. El build de GitHub Pages más reciente (run `28697306776`, commit `535f9320` = HEAD actual de `main`) terminó en `success` el 2026-07-04T06:11Z. No hay commits en `main` desde entonces, así que el HTML publicado debería coincidir con el repo. El incidente del 3-jul (build que no se disparó + build colgado 29 min) ya quedó resuelto por el commit "Trigger Pages rebuild" — no hay builds atorados ni fallidos pendientes hoy.

**Problemas encontrados:**
1. [Seguridad, preexistente] Bearer JWT de MailerLite y secreto de promo hardcodeados en el JS público de `index.html` (líneas 393 y 395) — expuestos a cualquier visitante, JWT sin expiración efectiva.
2. [Menor, preexistente] Los tres íconos sociales del footer (Instagram, X, LinkedIn — líneas 321-323) usan `href="#"` sin URL real ni handler JS; son enlaces muertos.
3. [Manejo de errores silencioso, preexistente] En `showPromo()` (línea 506-513), si `/generate-promo` falla, el catch cae a un código fijo `'ALAS7-2026'` sin loguear el error — un fallo del endpoint pasaría inadvertido y todos los visitantes verían el mismo código "único". Igual en `submitFree()` (línea 457-460): el catch solo muestra un mensaje genérico sin registrar la causa real.
4. [No verificable] Salud en vivo del sitio y de los endpoints de pago/promo — bloqueado por política de red de este entorno, ver nota arriba.

Sin hallazgos en: sintaxis del JS (`node --check` sobre el bloque `<script>` extraído, sin errores), CNAME (`alas7.com`, correcto), enlaces internos a `aviso-de-privacidad.html` / `terminos.html` (ambos archivos existen en el repo), ni referencias a dominios incorrectos (`alas7.mx` no aparece en el HTML).

**Recomendación:** (PERO NO SE ARREGLÓ — solo aviso)
- Prioridad alta: rotar/revocar el Bearer token de MailerLite expuesto y moverlo detrás del proxy de Railway (que el frontend llame solo a `/subscribe` sin credenciales, y que el proxy añada el Authorization header server-side). Mismo tratamiento para el secreto de promo si se quiere que cumpla una función real de anti-abuso.
- Prioridad baja: cablear los íconos sociales a URLs reales o quitarlos si las cuentas no existen todavía.
- Prioridad baja: añadir `console.error(err)` en los catch de `submitFree` y `showPromo` para poder detectar fallos reales de los endpoints en vez de que se enmascaren silenciosamente.
- Verificar manualmente (fuera de este entorno) que `alas7.com`, el Stripe Payment Link y el endpoint de Railway respondan 200, ya que esta corrida no pudo confirmarlo por la política de red del sandbox.
