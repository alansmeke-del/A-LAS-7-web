# Auditoría web — 2026-07-13

## [08:12 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno. El sandbox del auditor
bloquea por política de egress cualquier conexión saliente a hosts externos
arbitrarios (`alas7.com`, `www.alas7.com`, `buy.stripe.com` y el proxy de
Railway devolvieron todos "403 gateway CONNECT rejected" — bloqueo de la
política del proxy, no una respuesta real del servidor). No se puede afirmar
que el sitio esté caído ni que esté arriba con este método; se necesita un
chequeo HTTP real desde una máquina con salida a internet (o un monitor tipo
UptimeRobot/curl externo) para confirmar el estado en vivo.

**Pago y promo:** Revisión estática del HTML/JS servido en `main` (commit
`535f932`, el mismo que ya está publicado por Pages):
- El botón premium y la constante `STRIPE` apuntan correctamente a
  `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (coincide con el link
  esperado), tanto en el `href` del CTA de promo como en el JS.
- `API` apunta a `https://gentle-laughter-production-7f2c.up.railway.app`;
  el formulario gratuito hace `POST` a `/subscribe` y el popup de promo llama
  a `POST /generate-promo`. Ambos están cableados como se espera.
- No se pudo verificar que estos endpoints respondan en vivo (mismo bloqueo
  de red del punto anterior).
- **Hallazgo de seguridad no solicitado pero relevante:** el script inline
  (`index.html`, líneas ~393-395) tiene **hardcodeados en texto plano,
  visibles para cualquiera que haga "ver código fuente" del sitio público**:
  - `SECRET = 'alas7secret2026'` — usado como header `X-ALAS7-Secret` para
    llamar a `/generate-promo`.
  - Un JWT `Bearer` completo para la API de MailerLite/Railway, con `exp`
    fijado a un timestamp del año ~2126 (prácticamente no expira nunca).
  Cualquier visitante puede copiar estas credenciales del código fuente y
  llamar directamente a `/generate-promo` o al endpoint de suscripción sin
  pasar por la página. Esto es una exposición de credenciales, no un bug de
  funcionalidad — no lo toqué, solo lo reporto.
- El fallback de `showPromo()`: si `/generate-promo` falla, el `catch` no
  muestra ningún error — simplemente asigna el código fijo `ALAS7-2026` y
  sigue como si nada. Esto es intencional (no bloquea la conversión), pero
  significa que si ese endpoint se cae, **nadie se entera** salvo por este
  tipo de auditoría manual — no hay alerta ni log visible.

**Deploy Pages:** Al día. El último build de GitHub Pages
(run `28697306776`, "pages build and deployment") corresponde exactamente al
HEAD actual de `main` (`535f932...`, "Trigger Pages rebuild (previous build
stuck)") y terminó con `conclusion: success` el 2026-07-04 06:12 UTC. No hay
commits nuevos en `main` desde entonces, así que no hay nada pendiente de
desplegar. El incidente del 3-jul (build no disparado / build colgado 29 min)
ya no aparece como pendiente: el run inmediatamente anterior a este
(`28696615691`, commit `1c23348`) falló, y el siguiente rebuild manual lo
resolvió. Parece ya resuelto, sin necesidad de acción.

**Problemas encontrados:**
1. Imposible verificar en vivo la salud de alas7.com y de los endpoints de
   Stripe/Railway desde este entorno (bloqueo de red del sandbox, no del
   sitio).
2. Credenciales sensibles (secreto de promo + JWT Bearer sin expiración
   práctica) hardcodeadas en el HTML/JS público — visibles vía "ver código
   fuente".
3. Fallo silencioso en `/generate-promo`: si el endpoint cae, el usuario ve
   un código de respaldo y nadie se entera de que la API está rota.
4. Los íconos de redes sociales (Instagram/X/LinkedIn) usan `href="#"` —
   no llevan a ninguna parte real (probablemente placeholders intencionales,
   no un bug, pero vale la pena confirmarlo).

**Sin problemas:** sintaxis de JS válida (`node --check` limpio), CNAME
correcto (`alas7.com`, no `vitanews` ni `alas7.mx`), no hay referencias a
dominios inexistentes.

**Recomendación:** (no se corrigió nada, solo se reporta)
- Rotar el JWT Bearer y el secreto `alas7secret2026`, y mover ambos detrás
  del proxy de Railway en vez de incluirlos en el HTML público — que el
  cliente llame a un endpoint propio sin credenciales visibles.
- Añadir un monitor externo real (uptime check) para alas7.com y para los
  dos endpoints de Railway, ya que este auditor no puede verificarlos desde
  este entorno.
- Considerar loggear o alertar cuando `/generate-promo` responde con error,
  en vez de fallar en silencio hacia el código fijo.
- Confirmar si los links de redes sociales son placeholders a propósito o
  deberían apuntar a cuentas reales.
