# Auditoría web — 2026-07-18

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno de ejecución. El sandbox de esta
sesión bloquea todas las solicitudes salientes a dominios fuera de una lista blanca
del proxy (confirmado: incluso `https://anthropic.com` y `https://example.com`
devuelven 403/timeout desde aquí, no solo alas7.com). Esto **no es evidencia** de que
el sitio esté caído — es una limitación del entorno de auditoría, no del sitio.
Recomiendo correr un chequeo de uptime real (UptimeRobot, curl desde una máquina con
salida a internet, etc.) para confirmar 200 OK.

**Pago y promo:**
- Stripe Payment Link correcto y presente en el HTML: `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`
  (CTA en línea 361, `const STRIPE` en línea 391). El gate de privacidad premium
  (`checkPremiumPrivacy` → `window.location.href = STRIPE`) está intacto (líneas 465-472).
- Endpoint de promo: el JS llama correctamente a `POST {API}/generate-promo` con header
  `X-ALAS7-Secret` (línea 508). No se pudo probar la respuesta real del endpoint por la
  restricción de red mencionada arriba.
- Formulario de suscripción: `POST {API}/subscribe` con `Authorization: Bearer …` y body
  `{email, groups, referral_code}` (líneas 447-451) — cableado correctamente, consistente
  con lo documentado en CLAUDE.md.
- **HALLAZGO CRÍTICO (seguridad, afecta directamente el dinero/promos):** el Bearer JWT
  completo de MailerLite y el secreto `X-ALAS7-Secret` (`'alas7secret2026'`) están
  **hardcodeados en texto plano en el JS del cliente** (líneas 393 y 395 de `index.html`).
  Cualquier visitante puede verlos con "ver código fuente" y:
  - usar el Bearer token directamente contra la API de MailerLite (fuera del proxy Railway), o
  - golpear `/generate-promo` directamente con el secreto expuesto, generando códigos de
    descuento arbitrarios sin pasar por la página ni por ningún límite de la landing.
  Esto es explotable ahora mismo por cualquiera que inspeccione el HTML servido.

**Deploy Pages:** al día. El último "pages build and deployment" fue **success** para el
commit `535f9320` (2026-07-04 06:11 UTC), que es exactamente el HEAD actual de `main`.
El historial confirma el incidente del 3-jul reportado: un run **failure** a las 05:42 UTC
para el commit `1c23348b`, seguido ~29 min después por el run exitoso para `535f9320`
("Trigger Pages rebuild (previous build stuck)") — consistente con lo ya conocido y ya
resuelto. `index.html` en `origin/main` coincide byte a byte con lo que debería estar
desplegado (sin diffs pendientes).

**Problemas encontrados:**
1. **(Crítico, seguridad)** Bearer JWT de MailerLite y secreto `X-ALAS7-Secret` hardcodeados
   en el JS del cliente, visibles para cualquier visitante (`index.html` líneas 393, 395).
2. **(Menor)** 3 enlaces muertos en el footer: los íconos de Instagram/X/LinkedIn
   (`index.html` líneas 321-323) tienen `href="#"` sin ningún handler de JS asociado —
   no llevan a ningún lado al hacer clic (a diferencia de los otros `href="#"` en líneas
   66 y 308, que sí abren el modal de suscripción vía `.js-open-free`).
3. **(Menor)** Manejo de error silencioso en `showPromo()` (línea 506-513): el `catch`
   alrededor del fetch a `/generate-promo` no registra el error en ningún lado (ni
   `console.error`) y cae silenciosamente a un código hardcodeado `'ALAS7-2026'`. Si el
   endpoint de Railway se cayera de forma permanente, el sitio seguiría "funcionando"
   mostrando siempre el mismo código sin que nadie se entere.
4. **(Sin verificar)** Salud HTTP real de `alas7.com` — no verificable desde este entorno
   (ver sección "Sitio en vivo" arriba).

**Recomendación:** (qué se debería arreglar, PERO NO SE ARREGLÓ en esta corrida)
- **Prioridad alta:** sacar el Bearer JWT y el secreto `X-ALAS7-Secret` del JS del cliente;
  moverlos al backend (proxy Railway), que ya existe para este propósito, para que nunca
  se sirvan en el HTML público.
- Confirmar la salud real de `alas7.com` con una herramienta que sí tenga salida a
  internet (este auditor automatizado no puede hacerlo desde su entorno actual).
- Arreglar o quitar los 3 enlaces `href="#"` de redes sociales en el footer.
- Agregar al menos un `console.error` (o telemetría) en el catch de `generate-promo` para
  poder detectar si ese endpoint deja de responder.

Sin cambios a `index.html`, sin merges, sin tocar `main`. Solo documentación de auditoría.
