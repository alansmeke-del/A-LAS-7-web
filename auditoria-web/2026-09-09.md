# Auditoría web — 2026-09-09

## [08:16 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable esta corrida. El egress de red de este entorno de ejecución está bloqueado a nivel de política para *todos* los dominios externos (se confirmó probando también `example.com`, no es algo específico de alas7.com/Stripe/Railway). No se pudo hacer `curl`/`fetch` a `https://alas7.com`, `https://buy.stripe.com/...` ni al endpoint de Railway. Esto es una limitación del auditor en esta corrida, no evidencia de que el sitio esté caído.

**Pago y promo:** Cableado correctamente en el código, pero con un hallazgo de seguridad grave (ver abajo). No se pudo probar en vivo por el bloqueo de red.
- El botón/enlace Premium (`STRIPE`, `.js-premium`) apunta a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` ✓ (coincide con el Payment Link esperado).
- El popup de promo llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret` ✓, y usa el código devuelto para prellenar el link de Stripe (`?prefilled_promo_code=...`) ✓.
- El formulario gratuito llama a `POST {API}/subscribe` con `email`, `groups: [GROUP]` y `referral_code` ✓, usando `Authorization: Bearer <token>`.
- **CRÍTICO — secretos expuestos en el HTML público (`index.html` líneas 393–395):**
  - Un **Bearer token JWT completo de MailerLite** está hardcodeado en el JS del cliente. Cualquiera puede verlo con "Ver código fuente" del sitio. Lo decodifiqué: `exp: 4933854509` → **expira en el año ~2126** (prácticamente no expira) y `scopes: []`. Con este token, cualquier persona puede llamar directamente a la API de MailerLite (fuera de la landing) — riesgo de exportar/borrar la lista de suscriptores o abusar de la cuenta, según el alcance real que el token tenga del lado de MailerLite.
  - El secreto `X-ALAS7-Secret` (`alas7secret2026`) también está en texto plano en el JS público. Cualquiera puede llamar directamente a `/generate-promo` sin pasar por la landing y generar **códigos de descuento ilimitados** — riesgo de pérdida de ingresos y abuso de la promoción.
  - Ambos son secretos de backend que nunca deberían vivir en código servido al navegador; deberían moverse detrás del proxy de Railway (que ya existe) y no viajar al cliente.

**Deploy Pages:** Al día. El build más reciente de GitHub Pages (run #35, commit `535f932`, "Trigger Pages rebuild") terminó en **success** y coincide exactamente con el HEAD actual de `main`. No hay builds atorados ni fallidos pendientes — los incidentes del 3-jul (un build no disparado y otro colgado 29 min) ya quedaron resueltos por ese mismo commit.

**Problemas encontrados:**
1. **[Crítico]** Bearer token de MailerLite expuesto en texto plano en `index.html` (línea 395), válido por ~100 años, visible para cualquier visitante vía "Ver código fuente".
2. **[Alto]** Secreto `X-ALAS7-Secret` (`alas7secret2026`, línea 393) también expuesto en el JS público — permite generar códigos promo ilimitados sin pasar por la landing.
3. **[Info/limitación]** No se pudo verificar en vivo alas7.com, Stripe ni el endpoint de Railway por bloqueo total de egress de red en este entorno de auditoría.
4. **[Menor]** `SESSION_KEY = 'vita_promo_seen'` (línea 396) — resabio del nombre de marca anterior "Vita", antes del rebrand a A LAS 7. No rompe nada funcionalmente, es solo una clave interna de `sessionStorage`.
5. **[Menor / contenido pendiente]** Los 3 testimonios son placeholder ("Espacio reservado para un testimonio real...") y el footer tiene `[ Dirección física — CDMX, México ]` sin llenar entre corchetes.
6. **[Menor]** Los íconos sociales (Instagram, X, LinkedIn) usan `href="#"` sin URL real — parecen quedar pendientes de completar, no es un error técnico ya que no se anuncian como enlaces activos.
7. Sin hallazgos: el JS embebido valida sin errores de sintaxis (`node --check`), y el `CNAME` contiene correctamente `alas7.com` (no "vitanews" ni otro dominio incorrecto).
8. **[Info]** Los `catch` en `submitFree` y `showPromo` fallan de forma silenciosa hacia el usuario (solo muestran un mensaje genérico, sin `console.error` ni logging) — no es grave, pero dificultaría diagnosticar una falla real del backend si ocurriera.

**Recomendación:** (qué se debería arreglar, PERO NO SE ARREGLÓ EN ESTA CORRIDA)
- **Prioridad 1:** Revocar/rotar el Bearer token de MailerLite expuesto y regenerar el secreto `X-ALAS7-Secret` de inmediato; mover ambos al backend de Railway (que el cliente solo llame a rutas propias del proxy sin credenciales de terceros incluidas en el HTML).
- Dar al auditor automatizado excepción de red hacia alas7.com, buy.stripe.com y el dominio de Railway para poder confirmar salud en vivo en corridas futuras (o correr un chequeo externo de uptime aparte).
- Actualizar `vita_promo_seen` a un nombre consistente con la marca actual (cosmético, no urgente).
- Completar el contenido pendiente (testimonios reales, dirección física, enlaces sociales) antes del lanzamiento definitivo.
