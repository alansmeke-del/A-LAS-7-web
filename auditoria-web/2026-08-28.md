# Auditoría web — 2026-08-28

## [08:18 CDMX] Auditor web
**Estado:** problemas encontrados (bloqueo de verificación, no de sitio)
**Sitio en vivo:** NO VERIFICADO — el entorno de este auditor tiene bloqueado el egress de red hacia `alas7.com` / `www.alas7.com` (el proxy de red del sandbox devuelve `403 connect_rejected` por política, no es una respuesta del sitio). No se pudo confirmar si responde 200. **Esto no significa que el sitio esté caído**, significa que este auditor automatizado no puede comprobarlo desde aquí.
**Pago y promo:** NO VERIFICADO EN VIVO por el mismo bloqueo de red (`buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app` también devuelven `403 connect_rejected`). Revisión estática del código (sí posible):
  - El botón/CTA premium y el link del popup de promo apuntan correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (coincide con el Payment Link esperado).
  - El JS llama a `POST {API}/generate-promo` con header `X-ALAS7-Secret` y hace fallback silencioso a un código fijo `ALAS7-2026` si la llamada falla (comportamiento por diseño, no es un error nuevo).
  - El formulario gratuito hace `POST {API}/subscribe` con el email, `groups` y `referral_code`; está cableado correctamente al proxy de Railway → MailerLite (grupo `187372665902728776`).
**Deploy Pages:** AL DÍA. El último build de GitHub Pages (run #35, commit `535f932` "Trigger Pages rebuild") terminó en `success` y coincide exactamente con el HEAD actual de `main`. No hay commits nuevos en `main` desde el 2026-07-04, y no hay builds atorados o fallidos pendientes.
**Problemas encontrados:**
  1. **[Bloqueo del propio auditor]** El sandbox de este auditor no tiene salida de red hacia alas7.com, Stripe ni Railway. Mientras esto no se resuelva, este chequeo automatizado NO puede detectar si el sitio, el pago o el endpoint de promo realmente están caídos — solo puede auditar el código fuente del repo.
  2. **[Secretos en cliente, preexistente]** `index.html` expone en JS plano un bearer token de la API de MailerLite y el secreto `X-ALAS7-Secret` (`alas7secret2026`) usados para `/generate-promo`. Cualquiera puede leerlos con "ver código fuente" y llamar esos endpoints directamente. No es un cambio nuevo de esta corrida, pero sigue sin mitigarse.
  3. **[Cosmético, preexistente]** Los enlaces de redes sociales en el footer (Instagram/X/LinkedIn) siguen como `href="#"` placeholders, no rompen nada pero no llevan a ningún lado.
  4. Sintaxis del JS embebido validada con `node --check`: OK, sin errores.
  5. CNAME correcto (`alas7.com`), sin restos de `vitanews` ni `alas7.mx`. Los enlaces internos (`aviso-de-privacidad.html`, `terminos.html`, `assets/logo7-v2.png`) existen en el repo.
**Recomendación:** (qué se debería arreglar, PERO NO SE ARREGLÓ)
  1. Prioridad alta: dar a este auditor (o a un cron externo tipo UptimeRobot/health check) salida de red real hacia alas7.com, buy.stripe.com y el proxy de Railway, para que el chequeo de "sitio en vivo" y "pago/promo" sea real y no solo estático. Mientras tanto, verificar manualmente que alas7.com, el botón de pago y el popup de promo funcionan.
  2. Mover el bearer de MailerLite y el secreto de promo detrás del proxy de Railway (que el navegador nunca los vea), en vez de tenerlos hardcodeados en el HTML público.
  3. Baja prioridad: reemplazar o quitar los links `href="#"` de redes sociales en el footer.
