# Auditoría web — 2026-08-26

## [08:16 CDMX] Auditor web
**Estado:** problemas encontrados (limitación de entorno, no del sitio)

**Sitio en vivo:** NO VERIFICABLE — el entorno de ejecución de esta corrida bloquea
todo el tráfico HTTPS saliente hacia dominios externos (política de red del
proxy del entorno: `connect_rejected` / gateway 403 a `alas7.com:443`,
`buy.stripe.com:443` y `gentle-laughter-production-7f2c.up.railway.app:443`).
No se pudo hacer un `curl`/fetch real a alas7.com para confirmar el 200 OK.
Esto es una restricción del entorno del auditor, no evidencia de que el sitio
esté caído.

**Pago y promo:** NO VERIFICABLE por la misma razón (egress bloqueado). Lo que
sí se pudo revisar en el código fuente (`index.html` en la rama `main`,
commit `535f932`):
- El botón/CTA de pago (`#promoCta` y el flujo premium) apunta al Payment Link
  esperado: `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` ✓
- `API` está configurado a
  `https://gentle-laughter-production-7f2c.up.railway.app` y el JS llama
  correctamente a `API + '/subscribe'` (línea 447) y `API + '/generate-promo'`
  (línea 508, con header `X-ALAS7-Secret`) ✓
- El formulario de suscripción gratuita está cableado al POST `/subscribe` ✓

**Deploy Pages:** AL DÍA. El último build de GitHub Pages (run #35,
"Trigger Pages rebuild", 2026-07-04T06:12:03Z) terminó en `success` sobre el
commit `535f932...`, que es exactamente el HEAD actual de `main`. No hay
commits nuevos en `main` desde esa fecha, así que no hay desfase entre el
HTML en vivo (asumiendo que el build se sirvió bien) y el repo.

**Problemas encontrados:**
1. **Bloqueante para este auditor:** el entorno de red de esta sesión programada
   no permite salidas HTTPS a alas7.com, buy.stripe.com ni al proxy de Railway.
   No hay forma de confirmar hoy, desde aquí, que el sitio en vivo responde 200,
   que el Payment Link de Stripe resuelve, o que `/generate-promo` /
   `/subscribe` respondan. Se necesita correr esta auditoría desde un entorno
   con salida a internet habilitada, o dar de alta esos dominios en la política
   de red del entorno del auditor.
2. **Manejo de error silencioso (menor):** en el flujo del popup de promo
   (línea ~511), si el `fetch` a `/generate-promo` falla, el `catch` no
   registra nada (`console.error` ausente) y simplemente cae a un código fijo
   `'ALAS7-2026'` que se aplica al link de Stripe vía
   `?prefilled_promo_code=`. Si ese código fijo ya no existe o expiró en
   Stripe, el usuario ve un código "válido" en pantalla pero el descuento
   falla silenciosamente en el checkout, y no queda ningún rastro en consola
   para depurar. Vale la pena loguear el error y/o verificar que el código de
   fallback siga vivo en Stripe.
3. **Enlaces sociales sin URL real (cosmético):** los íconos de Instagram, X y
   LinkedIn en el footer (líneas 321-323) usan `href="#"` como placeholder.
   No rompen nada (no son parte del flujo de conversión) pero no llevan a
   ningún lado.
4. Sin problemas de sintaxis en el JS embebido (`node --check` limpio) ni
   referencias a dominios viejos (`alas7.mx`, `vitanews`) — el `CNAME` dice
   correctamente `alas7.com`.

**Recomendación:** (no corregido, solo se documenta)
- Prioridad alta: habilitar salida de red desde el entorno de este auditor
  hacia alas7.com, buy.stripe.com y el proxy de Railway, o mover esta
  auditoría a un entorno que sí tenga egress a internet — hoy el auditor no
  puede confirmar lo más crítico (sitio en vivo, pago, promo responden).
  Mientras tanto, Alan debería verificar manualmente que alas7.com carga y
  que el checkout de Stripe funciona.
- Prioridad media: en el catch de `/generate-promo` (línea ~511), agregar un
  `console.error` con el motivo del fallo y confirmar que el código de
  respaldo `ALAS7-2026` sigue activo en Stripe (o quitar el fallback si ya no
  aplica).
- Prioridad baja: dar URLs reales a los íconos sociales del footer o quitarlos
  si no hay cuentas activas todavía.
