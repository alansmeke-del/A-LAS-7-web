# Auditoría web — A LAS 7

## [08:16 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificado — el entorno de ejecución de esta corrida tiene bloqueado todo el egress de red saliente (política del proxy del entorno), incluyendo alas7.com, buy.stripe.com, el proxy de Railway, e incluso dominios de control como google.com. No fue posible hacer ni un solo `curl`/fetch externo. Esto es una limitación del entorno del auditor, no evidencia de que el sitio esté caído — pero significa que esta corrida NO pudo confirmar que alas7.com responde 200.

**Pago y promo:** No verificado en vivo (mismo bloqueo de red). Revisión estática del código en `index.html` (commit `535f9320`, HEAD actual de `main`):
- El enlace de Stripe `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` aparece correctamente en el botón Premium y en el CTA del popup de promoción (2 apariciones, ambas coinciden con el Payment Link esperado).
- El JS arma correctamente `API + '/generate-promo'` y `API + '/subscribe'` contra `https://gentle-laughter-production-7f2c.up.railway.app`.
- ⚠️ **Hallazgo crítico de seguridad (nuevo):** el archivo `index.html` (línea ~395) tiene **hardcodeado en el JS del cliente** un JWT bearer token de larga duración (`exp` decodificado ≈ año 2126, prácticamente no expira, `scopes: []`) que se envía como `Authorization: Bearer …` al llamar `/subscribe`. También el secreto `X-ALAS7-Secret: alas7secret2026` usado contra `/generate-promo` está expuesto igual en texto plano. Cualquier persona puede ver este token con "Ver código fuente" del navegador. Si es un token real de la API de MailerLite (la estructura del JWT — `aud`, `sub` numérico, `scopes` — es consistente con un Personal Access Token de MailerLite), esto permitiría a un tercero llamar directamente a la API de MailerLite (leer/borrar suscriptores, modificar grupos, etc.), saltándose por completo el proxy de Railway que se supone protege esa credencial.

**Deploy Pages:** Al día. El HEAD actual de `main` (`535f93209144b4d6befdb951db81cedbc3c786d8`, "Trigger Pages rebuild (previous build stuck)", 2026-07-04T06:11 UTC) coincide exactamente con el `head_sha` del build de Pages más reciente y exitoso (run #35, conclusion `success`). No hay commits nuevos en `main` desde esa fecha, así que no hay nada pendiente de desplegar. Se confirma en el historial el incidente ya conocido del 3-jul (run #34 con `conclusion: failure` sobre "Merge branch 'dev'") — ya fue resuelto por el propio equipo con el commit de "Trigger Pages rebuild" que sí tuvo éxito. No se detectan builds nuevos atorados o fallidos después de esa fecha.

**Problemas encontrados:**
1. 🔴 **Crítico:** Bearer token JWT de larga duración y el secreto `X-ALAS7-Secret` expuestos en texto plano en el JS público de `index.html` (línea ~395). Ver detalle arriba.
2. 🟡 Menor: los dos `catch` del JS de suscripción/promo (fetch a `/subscribe` y `/generate-promo`) fallan en silencio — no hacen `console.error` ni registran nada, solo muestran un mensaje genérico al usuario o usan un código de respaldo estático (`ALAS7-2026`). Si el backend de Railway cae, no queda ninguna señal visible en consola para diagnosticar desde el cliente.
3. 🟡 Cosmético: los 3 íconos de redes sociales (Instagram, X, LinkedIn) en el footer usan `href="#"` como placeholder — no apuntan a perfiles reales todavía.
4. 🟡 Cosmético: la clave de `sessionStorage` sigue llamándose `vita_promo_seen`, resto de una marca anterior ("Vita"); es inofensivo funcionalmente pero es un rastro de rebranding incompleto.
5. ⚪ Esta corrida no pudo verificar en vivo el sitio, Stripe ni el endpoint de Railway por bloqueo de red del entorno — recomendable que una corrida futura con acceso de red confirme el estado real en producción.

**Recomendación:** (qué se debería arreglar, PERO NO SE ARREGLÓ EN ESTA CORRIDA)
- Prioridad 1: rotar/revocar el Bearer token de MailerLite hardcodeado y el secreto `X-ALAS7-Secret`, y mover ambos al backend del proxy de Railway (que ya existe para eso) en vez de enviarlos desde el navegador. El cliente no debería necesitar ninguna credencial — solo debería llamar `/subscribe` y `/generate-promo` sin headers de autorización, y que el proxy añada sus propias credenciales del lado del servidor.
- Prioridad 2: agregar `console.error` (o algún registro) dentro de los `catch` de `/subscribe` y `/generate-promo` para poder detectar caídas del backend desde herramientas de monitoreo de errores del navegador.
- Prioridad 3 (opcional): reemplazar los `href="#"` de redes sociales por los perfiles reales o quitarlos si no existen todavía.
- Verificar en la próxima corrida (con acceso de red) que alas7.com, el Payment Link de Stripe y el endpoint de Railway respondan correctamente en vivo.
