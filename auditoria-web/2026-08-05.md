# Auditoría web — 2026-08-05

## [08:23 CDMX] Auditor web
**Estado:** problemas encontrados (no críticos; ver limitación de red abajo)

**Sitio en vivo:** No se pudo verificar directamente. El proxy de red de este entorno de auditoría bloqueó por política (403 `connect_rejected`) las conexiones salientes a `alas7.com`, `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app`. Esto es una restricción del entorno del auditor, no evidencia de que el sitio esté caído — pero significa que esta corrida **no pudo confirmar en vivo** que la home responde 200 ni que el checkout/promo responden. Recomiendo una verificación manual o desde un entorno con salida de red abierta a esos hosts.

**Pago y promo:** Verificado solo a nivel de código (no en vivo, ver arriba).
- El botón premium apunta exactamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` ✓ (coincide con el Payment Link esperado).
- El JS llama correctamente a `POST {API}/generate-promo` con header `X-ALAS7-Secret` y a `POST {API}/subscribe` con `Authorization: Bearer …`, `groups`, y `referral_code` ✓. `API` = `gentle-laughter-production-7f2c.up.railway.app` ✓.
- **Hallazgo de seguridad:** el JWT Bearer (usado para `/subscribe`) y el secreto `X-ALAS7-Secret` (`alas7secret2026`, usado para `/generate-promo`) están **hardcodeados en texto plano en el HTML público** (`index.html`, dentro del `<script>`). Cualquiera puede verlos con "ver código fuente" y llamar esos endpoints directamente, fuera del flujo del sitio — riesgo de abuso de generación de códigos promo o de la suscripción vía API. El JWT decodificado tiene fecha de expiración extremadamente lejana (~año 2126), es decir, una credencial de larga vida expuesta públicamente.
- Fallback silencioso: si `/generate-promo` falla, el JS cae a un código fijo `ALAS7-2026` sin avisar a nadie (ni al usuario ni a un log) — si el endpoint estuviera caído, todos los visitantes verían el mismo código sin que quede evidencia visible del fallo.

**Deploy Pages:** Al día. El build más reciente de `pages-build-deployment` (run `28697306776`) corresponde al commit actual de `main` (`535f932`, "Trigger Pages rebuild") y terminó en `success` en ~36s (2026-07-04 06:11–06:12 UTC). No hay commits en `main` posteriores a ese build, así que no hay desfase entre el HTML publicado y el último commit. No se detectaron builds atorados ni fallidos recientes (el build que falló fue el intento anterior a este, ya reemplazado por el que sí corrió bien).

**Problemas encontrados:**
1. No se pudo verificar en vivo la salud de `alas7.com`, Stripe ni el endpoint de promo/suscripción — bloqueo de red del entorno de auditoría (no confirmado como falla real del sitio).
2. Credenciales de API expuestas en el HTML público: JWT Bearer de larga duración (~año 2126) y secreto estático `alas7secret2026`, ambos visibles en "ver código fuente" y usables fuera del sitio.
3. Tres enlaces sociales decorativos (`Instagram`, `X`, `LinkedIn` en el footer) usan `href="#"` sin URL real — no son un flujo crítico de conversión, pero son enlaces rotos de cara al usuario.
4. El fallback del código promo (`ALAS7-2026` si `/generate-promo` falla) es silencioso — no hay señal visible de que el endpoint de promos esté caído.

**Verificado sin hallazgos:**
- `CNAME` = `alas7.com` (correcto, no dice `vitanews` ni otro dominio).
- JS embebido pasa `node --check` sin errores de sintaxis.
- No se encontraron referencias a dominios inexistentes (`alas7.mx` u otros).
- Archivos locales referenciados (`aviso-de-privacidad.html`, `terminos.html`, `assets/logo7-v2.png`) existen en el repo.

**Recomendación:** (no corregido — solo aviso, decide Alan)
- Confirmar manualmente (o desde un entorno sin el bloqueo de red) que `alas7.com`, el link de Stripe y el endpoint de Railway responden 200 en este momento.
- Mover el JWT Bearer y el secreto `X-ALAS7-Secret` fuera del cliente: que el frontend llame a un endpoint propio del backend que internamente añada esas credenciales, en vez de exponerlas en el HTML. Si no es viable de inmediato, al menos rotar/limitar el scope del JWT expuesto.
- Poner URLs reales (o quitar) los íconos de Instagram/X/LinkedIn del footer.
- Considerar loguear (aunque sea a un servicio de analytics) cuando `/generate-promo` falla y cae al código fijo, para tener visibilidad si el endpoint se cae.
