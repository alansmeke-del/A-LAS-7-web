## [08:12 CDMX] Auditor web
**Estado:** problemas encontrados (persisten, sin novedades críticas nuevas)
**Sitio en vivo:** No verificable. El entorno de este auditor tiene bloqueada por política de egress la salida a `alas7.com`, `buy.stripe.com` y `gentle-laughter-production-7f2c.up.railway.app` (403 `connect_rejected`, confirmado vía `curl` directo y vía la herramienta WebFetch — ambas rutas de red fueron rechazadas). No es evidencia de que el sitio esté caído; es una restricción del sandbox de auditoría. No se pudo confirmar código 200 en vivo ni hoy ni en las corridas de días recientes.
**Pago y promo:** No verificable en vivo (mismo bloqueo de red). Revisión estática de `index.html` en `main` (commit `535f9320`, HEAD actual, sin cambios desde 2026-07-04):
  - Botón de pago Premium: `STRIPE = 'https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01'` presente y coincide con el Payment Link esperado; cableado correctamente a `checkPremiumPrivacy()` → `window.location.href`, con el gate de privacidad (`#premiumPrivacy`) obligatorio antes de redirigir.
  - Endpoint de promo: `showPromo()` llama `POST {API}/generate-promo` con header `X-ALAS7-Secret`, `API` = `gentle-laughter-production-7f2c.up.railway.app`. Coincide con lo esperado.
  - Formulario de suscripción: `submitFree()` hace `POST {API}/subscribe` con `Authorization: Bearer …`, `email`, `groups: [GROUP]` y `referral_code` desde el input oculto `#referralCode` (se llena desde `?ref=` en la URL). El botón `#freeSubmitBtn` permanece `disabled` hasta que se marca `#freePrivacy`, igual que el gate premium. Cableado correcto.
**Deploy Pages:** Al día. Último "pages build and deployment" (run #35, commit `535f9320` "Trigger Pages rebuild") = **success**, y es exactamente el HEAD actual de `main`. No hay commits nuevos en `main` desde el 2026-07-04, por lo que no hay desfase entre el HTML publicado y el repositorio. No se detectaron builds atorados ni fallidos nuevos.

**Problemas encontrados:**
1. **[CRÍTICO — persiste desde hace ~71 días, reportado sin acción en más de 65 auditorías consecutivas]** `index.html` (línea ~395) sigue exponiendo en texto plano, visible por cualquiera con "ver código fuente":
   - Un **bearer token JWT completo** (`const BEARER = 'eyJ0eXAi...'`) usado para autenticar `POST /subscribe`. Su campo `exp` decodificado es `4933854509` → vence en el **año 2126**, es decir, es en la práctica un token permanente.
   - Un secreto estático (`const SECRET = 'alas7secret2026'`) enviado como header `X-ALAS7-Secret` a `POST /generate-promo`.
   Cualquier visitante puede copiar este token/secreto y llamar directamente a la API detrás del proxy de Railway — suscribir emails arbitrarios al grupo de MailerLite, generar códigos promo sin límite, o cualquier otra acción que ese token autorice — sin pasar por el sitio ni por sus límites de uso. Este hallazgo es idéntico, día tras día, desde el PR #1 (2026-07-08) hasta el PR #67 (2026-09-12); ninguno se ha mergeado ni cerrado.
2. **[Menor, no crítico, persiste]** Los íconos sociales de Instagram, X y LinkedIn en el footer (`href="#"`, líneas 321-323) son enlaces muertos sin URL real. (Los otros `href="#"` del sitio, líneas 66 y 308, son intencionales: abren el modal gratuito vía JS y no son enlaces rotos.)
3. **[Menor, por diseño pero vale mencionarlo, persiste]** `showPromo()` (línea ~511) tiene un `catch` que, si `/generate-promo` falla, cae silenciosamente a un código estático `'ALAS7-2026'` sin avisar al usuario ni registrar el error — oculta una posible caída sostenida del endpoint de promociones.
4. **[Proceso, no de código — empeora cada día]** Hay **67 pull requests de auditoría abiertos y sin mergear** (`claude/audit-web-2026-07-08` hasta `claude/audit-web-2026-09-12`), todos reportando esencialmente el mismo hallazgo #1 sobre el mismo `main` sin cambios desde el 2026-07-04. Ninguno ha sido revisado, cerrado ni mergeado en más de dos meses.

**Verificado sin hallazgos nuevos:**
- `CNAME` = `alas7.com` (correcto).
- JS embebido pasa `node --check` sin errores de sintaxis.
- No se encontraron referencias a dominios inexistentes (`alas7.mx` u otros similares).
- Archivos locales referenciados (`aviso-de-privacidad.html`, `terminos.html`, `assets/logo7-v2.png`) existen en el repo.

**Recomendación:** (qué se debería arreglar, PERO NO LO ARREGLES)
- Máxima prioridad, sin cambios respecto a auditorías anteriores: rotar/revocar el bearer token de MailerLite y el `X-ALAS7-Secret` expuestos, y mover esa autenticación al lado servidor del proxy de Railway (que el proxy adjunte las credenciales internamente, nunca en el HTML público). Llevan ~71 días expuestos en producción.
- Revisar y cerrar/mergear el backlog de 67 PRs de auditoría — sin eso, este mismo hallazgo se seguirá reportando indefinidamente sin resolución visible.
- Confirmar manualmente (o desde un entorno con salida de red a esos hosts) que `alas7.com`, el link de Stripe y el endpoint de Railway responden 200 en este momento, ya que este auditor lleva bloqueado desde su primera corrida.
- Opcional: quitar o enlazar correctamente los íconos sociales del footer; loguear cuando `/generate-promo` falla y cae al código fijo.
