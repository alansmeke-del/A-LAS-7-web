# Auditoría web — 2026-07-08

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados (menores, ninguno crítico)

**Sitio en vivo:** No se pudo verificar directamente. El sandbox de este auditor
bloquea las conexiones salientes a `alas7.com`, `buy.stripe.com` y al proxy de
Railway (`gateway answered 403 to CONNECT` — política de red del entorno, no una
respuesta real del sitio). Es una limitación del entorno de auditoría, no
evidencia de una caída. **Recomiendo verificación manual de alas7.com** hasta que
se permita el egreso a esos dominios desde este entorno.

**Pago y promo:** ok (verificado por código, no por HTTP en vivo)
- El botón premium (`#promoCta` y los `.js-premium`) apunta correctamente a
  `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (index.html:361, 391, 470).
- El popup de promo llama a `POST {API}/generate-promo` con el header
  `X-ALAS7-Secret` (index.html:508), y el formulario gratuito hace
  `POST {API}/subscribe` con `Authorization: Bearer …` y el `referral_code`
  capturado de `?ref=` (index.html:447-451, 401-402). El cableado coincide con
  lo documentado.
- No se pudo confirmar que los endpoints de Railway respondan en vivo (mismo
  bloqueo de red del punto anterior).

**Deploy Pages:** al día
- El último run de `pages-build-deployment` (run 28697306776) terminó en
  **success** el 2026-07-04T06:11:27Z para el commit `535f932`, que es
  exactamente el HEAD actual de `main`. No hay commits nuevos desde entonces, así
  que no hay nada pendiente de desplegar.
- El build fallido reportado el 3-jul (run 28696615691, `failure`,
  2026-07-04T05:42:45Z, commit `1c23348`) ya fue resuelto por el commit de
  rebuild manual (`535f932` — "Trigger Pages rebuild (previous build stuck)").
  No requiere acción adicional.

**Problemas encontrados:**
1. No se pudo probar la salud HTTP en vivo de alas7.com/Stripe/Railway desde este
   entorno (bloqueo de red del sandbox, ver arriba) — no es un hallazgo de sitio
   caído, es un hueco de cobertura del auditor.
2. Los íconos de redes sociales del footer (Instagram, X, LinkedIn — index.html
   líneas 321-323) tienen `href="#"` y no llevan a ningún perfil real; son
   enlaces muertos.
3. `showPromo()` (index.html:506-513) atrapa el error del fetch a
   `/generate-promo` en silencio y cae a un código fijo `'ALAS7-2026'` sin
   `console.error` ni ningún registro. Si el endpoint de promo se cae en
   producción, nadie se entera desde el navegador.
4. El JWT Bearer de MailerLite y el secreto `X-ALAS7-Secret` (`alas7secret2026`)
   están hardcodeados en el JS del cliente (index.html:393-395), visibles para
   cualquiera que vea el código fuente. Es preexistente (preservado a propósito
   según el historial de commits), no una regresión de hoy, pero sigue siendo
   una exposición de secretos que vale la pena señalar.
5. CNAME correcto (`alas7.com`), sin restos de `vitanews` u otro dominio.
6. Sintaxis del JS embebido validada con `node --check` sobre el bloque
   `<script>` extraído: sin errores.

**Recomendación:** (no corregido, solo diagnóstico)
- Verificar manualmente que alas7.com responda 200 y que el flujo de pago/promo
  funcione end-to-end, ya que este auditor no tiene salida de red a esos
  dominios en este entorno.
- Reemplazar los `href="#"` de redes sociales por las URLs reales o quitar los
  íconos si aún no hay cuentas.
- Agregar al menos un `console.error` (o telemetría) en el catch de
  `showPromo()` para detectar caídas del endpoint `/generate-promo` en
  producción.
- Evaluar mover el Bearer de MailerLite y el `X-ALAS7-Secret` a un proxy/backend
  en vez de dejarlos en el JS del cliente (riesgo de abuso del endpoint de
  promo y de la cuenta de MailerLite si alguien los extrae del código fuente).
