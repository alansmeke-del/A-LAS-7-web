# Auditoría web — 2026-08-18

## [08:18 CDMX] Auditor web

**Estado:** problemas encontrados

**Sitio en vivo:** No verificable directamente — el entorno de este auditor tiene
bloqueado por política de egress el acceso saliente a `alas7.com`, `buy.stripe.com`
y `gentle-laughter-production-7f2c.up.railway.app` (403 en el proxy de salida, no
un error de red transitorio). No pude confirmar el código HTTP en vivo. Verificado
en su lugar a nivel de código y de despliegue (ver abajo).

**Pago y promo:**
- El botón de pago (`#promoCta` y el flujo premium) apunta correctamente a
  `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` — coincide con el link real.
- El JS sigue llamando a `POST {API}/generate-promo` con el header
  `X-ALAS7-Secret` y a `POST {API}/subscribe`, con `API =
  https://gentle-laughter-production-7f2c.up.railway.app`. El cableado en el
  HTML/JS es correcto.
- No pude confirmar que esos endpoints respondan en vivo (mismo bloqueo de red
  del punto anterior).
- **Problema encontrado (bug de conversión):** los botones "Suscribirme →" del
  nav (línea ~67) y "Ir a Premium · $149/mes" del hero (línea ~85) están
  cableados a `checkPremium()`, que valida el checkbox `#premiumPrivacy`. Ese
  checkbox **solo existe una vez en el DOM, dentro de la tarjeta de Planes**
  (sección Planes, no en el hero ni en el nav). Si un usuario hace clic en esos
  botones sin haber bajado antes a Planes y marcado ahí la casilla, el script
  simplemente muestra el mensaje de error junto a la tarjeta de Planes (fuera
  de la pantalla en ese momento) y no pasa nada visible — no hay redirección a
  Stripe ni retroalimentación donde el usuario está mirando. Efectivamente el
  CTA de premium en el nav/hero puede parecer un botón muerto.
- **Problema encontrado (seguridad):** el token Bearer de MailerLite (JWT sin
  expiración real — el claim `exp` apunta a una fecha extremadamente lejana) y
  el secreto compartido del endpoint de promo (`alas7secret2026`) están
  embebidos en texto plano dentro de `index.html`, visibles para cualquiera
  con "ver código fuente". Cualquiera puede extraerlos y llamar directamente a
  la API de MailerLite o farmear códigos de `/generate-promo`.
- **Menor:** si `/generate-promo` falla, el JS cae en silencio a un código fijo
  `'ALAS7-2026'` sin avisar a nadie (ni al usuario ni a ningún log). Si el
  endpoint está caído, el usuario ve un código aparentemente válido que podría
  no funcionar en el checkout de Stripe, y nadie se entera salvo que alguien
  lo pruebe manualmente.

**Deploy Pages:** Al día. La última corrida de "pages build and deployment"
(run #35, commit `535f9320`) terminó en `success` el 2026-07-04 06:12 UTC y
coincide con el HEAD actual de `main`. No hay commits nuevos en `main` desde
esa fecha que requieran un build nuevo, y no hay corridas atoradas ni
fallidas pendientes en este momento (el build atorado del 3-jul y el fallo
del run #34 ya habían sido resueltos ese mismo día con el commit "Trigger
Pages rebuild").

**Problemas encontrados:**
1. CTA de Premium en nav/hero puede fallar silenciosamente por checkbox de
   privacidad compartido y ubicado solo en la sección Planes (bug de
   conversión).
2. Token Bearer de MailerLite y secreto de `/generate-promo` expuestos en
   texto plano en el HTML público (riesgo de seguridad).
3. Fallback silencioso a código promo fijo si `/generate-promo` falla (sin
   alerta).
4. Menor: los íconos sociales del footer (Instagram, X, LinkedIn) apuntan a
   `href="#"` — son placeholders, no URLs reales.
5. No se pudo verificar en vivo el sitio, Stripe ni los endpoints de Railway
   por bloqueo de egress del entorno de este auditor (limitación del propio
   auditor, no necesariamente del sitio).

**Recomendación:** (NO SE CORRIGIÓ NADA — solo para que Alan decida)
- Prioridad alta: dar a los botones de premio del nav/hero su propio
  checkbox de privacidad (o hacer scroll a Planes / abrir un modal) para que
  el gate de privacidad no dependa de un elemento fuera de pantalla.
- Mover el token Bearer y el secreto de promo detrás del backend (Railway)
  en vez de embebidos en el HTML público, o al menos rotar el token si se
  decide dejarlo temporalmente así.
- Registrar/alertar cuando `/generate-promo` falle en vez de degradarse en
  silencio a un código fijo.
- Enlazar o quitar los íconos sociales del footer.
- Dar acceso de egress a este auditor (o a un runner con acceso) hacia
  alas7.com / buy.stripe.com / el dominio de Railway para que las próximas
  corridas puedan confirmar el estado en vivo, no solo el código en el repo.
