# Auditoría web — 2026-09-04

## [08:15 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno — el proxy de red de la sesión bloquea las conexiones salientes a alas7.com (`connect_rejected`, política de la organización). No se pudo confirmar el código HTTP en vivo por esta vía. Como señal indirecta: el último build de GitHub Pages sobre `main` (run #35, commit `535f932`, "Trigger Pages rebuild") terminó en **success** y coincide con el HEAD actual de `main`, así que el despliegue debería estar al día si el DNS/Pages está sano.

**Pago y promo:**
- El link de Stripe (`https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`) está presente en `index.html` tanto en el botón CTA del modal de promo (línea 361) como en la constante `STRIPE` del JS (línea 391). No se pudo probar la respuesta HTTP en vivo por el mismo bloqueo de red del punto anterior.
- El endpoint `/generate-promo` en `https://gentle-laughter-production-7f2c.up.railway.app` está bien referenciado en el JS (`showPromo()`), con el header `X-ALAS7-Secret`. Tampoco se pudo probar la respuesta en vivo por el bloqueo de red.
- El formulario de suscripción gratuita sí está cableado a `POST {API}/subscribe` con el email, `groups` y `referral_code`.
- **Hallazgo de seguridad (severidad alta):** el JS de `index.html` (línea ~390) trae **hardcodeado en el cliente** un token Bearer JWT completo (`const BEARER = 'eyJ0eXAi...'`) que se manda en cada request a `/subscribe`. El JWT decodificado no tiene expiración práctica (exp ≈ año 2126). Cualquier visitante puede copiar ese token desde el HTML/JS público y usarlo fuera del sitio para llamar la API de MailerLite/el proxy directamente. Lo mismo aplica, en menor medida, al `X-ALAS7-Secret` (`alas7secret2026`) usado para `/generate-promo`: es un "secreto" visible en texto plano para cualquiera que abra el código fuente. Ambos deberían vivir solo en el proxy de Railway, no en el HTML servido al público.
- **Manejo de errores silencioso:** en `showPromo()`, si el fetch a `/generate-promo` falla, el `catch` simplemente usa el código fijo `'ALAS7-2026'` sin loguear el error ni avisar de ninguna forma — si el endpoint cae, nadie se entera desde el frontend y todos los usuarios reciben el mismo código de respaldo indefinidamente.

**Deploy Pages:** Al día. El run #35 (`pages build and deployment`, commit `535f932`) es el más reciente y terminó en success, y ese commit es el HEAD actual de `main`. El run #34 anterior (commit `1c23348`, "Merge branch 'dev'") sí falló, pero fue corregido por el commit siguiente que disparó un rebuild exitoso — no hay commits nuevos en `main` sin desplegar.

**Problemas encontrados:**
1. (Alto) Token Bearer de MailerLite expuesto en texto plano en el JS público de `index.html`, sin expiración práctica.
2. (Medio) `X-ALAS7-Secret` de `/generate-promo` también expuesto en el JS público — es un secreto de nombre pero no de hecho.
3. (Bajo) Manejo de error silencioso en `showPromo()`: si `/generate-promo` falla, no se registra ni se notifica, solo se usa el código de respaldo fijo.
4. (Bajo) Los íconos sociales (Instagram, X, LinkedIn) en el footer usan `href="#"` sin URL real — son placeholders no funcionales.
5. (Informativo) No se pudo verificar en vivo alas7.com, el Payment Link de Stripe ni los endpoints de Railway porque el proxy de red de este entorno bloquea las conexiones salientes a esos dominios. Se recomienda una verificación manual o desde un entorno con salida de red completa.

**Recomendación:** Rotar el token Bearer de MailerLite y mover tanto ese token como el `X-ALAS7-Secret` al backend del proxy de Railway (que ya intermedia las llamadas), para que nunca viajen en el HTML/JS público — esto es lo más urgente porque es dinero/datos de suscriptores en juego. Agregar un log o aviso visible cuando `/generate-promo` falla, en vez de fallar en silencio. Sustituir los `href="#"` de redes sociales por las URLs reales o quitar los íconos si no hay perfiles activos. Confirmar manualmente que alas7.com, el Payment Link de Stripe y los dos endpoints de Railway responden 200 desde un entorno sin restricciones de red. PERO NO LO ARREGLES — esto es solo un reporte.
