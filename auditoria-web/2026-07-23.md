# Auditoría web — 2026-07-23

## [08:11 CDMX] Auditor web
**Estado:** problemas encontrados

**Sitio en vivo:** No verificado. El entorno de ejecución de este auditor tiene bloqueada la salida a internet hacia hosts externos (política de egress de la sandbox) — `alas7.com`, `buy.stripe.com` y el endpoint de Railway devuelven `403` a nivel de proxy antes de llegar al destino real. No es un fallo del sitio, es una limitación del entorno del auditor. Recomiendo correr un `curl -I https://alas7.com` manual o desde un entorno con salida abierta para confirmar el 200 OK.

**Pago y promo:** Verificado solo por código estático (no por request en vivo):
- El link de Stripe `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` está presente y es consistente en las 3 referencias del HTML (constante `STRIPE`, `checkPremium()` → `window.location.href`, y el `href` inicial de `#promoCta`).
- El endpoint de promo (`POST /generate-promo` con header `X-ALAS7-Secret`) y el de suscripción (`POST /subscribe` con `Authorization: Bearer …`) están cableados correctamente en el JS hacia `gentle-laughter-production-7f2c.up.railway.app`.
- **Hallazgo grave (seguridad):** el JS embebido en `index.html` (línea ~395) expone en texto plano un **Bearer token JWT de la API de MailerLite**, visible para cualquiera que vea el código fuente de la página. Decodificando el payload del JWT, su `exp` es `4933854509` (~año 2126), es decir, es un token que **prácticamente no expira**. Cualquier visitante puede copiar este token y llamar directamente a la API de MailerLite con el mismo alcance que usa el sitio (alta de suscriptores al grupo `187372665902728776`, y lo que ese scope permita). También se expone en claro el secreto `X-ALAS7-Secret` (`alas7secret2026`) usado para `/generate-promo`. Esto ya estaba así en el commit actual de `main` (`535f932`), no es una regresión de hoy, pero sigue vigente y es explotable ahora mismo.

**Deploy Pages:** Al día. El último workflow run de `pages-build-deployment` (id `28697306776`) corresponde al commit `535f932` — que es el HEAD actual de `main` — y terminó `completed / success` en ~36s (2026-07-04 06:11–06:12 UTC). No hay commits nuevos en `main` desde entonces, así que no hay desfase entre HTML publicado y el repo. No se detectan builds atorados ni fallidos en el run más reciente (sí hubo un run `failure` el mismo día para el commit `1c23348`, previo al actual, ya superado por el siguiente run exitoso).

**Integridad HTML/JS:**
- `node --check` sobre el JS embebido: sin errores de sintaxis.
- `CNAME` correcto: `alas7.com`.
- No se encontraron dominios rotos o inexistentes (tipo `alas7.mx`); los únicos dominios externos referenciados son `fonts.googleapis.com`, `fonts.gstatic.com`, `buy.stripe.com` y el proxy de Railway.
- `href="#"` sin URL real: los de `.js-open-free` son intencionales (el JS intercepta el click con `preventDefault()` y abre el modal). Los 3 íconos de redes sociales (Instagram, X, LinkedIn) en el footer sí apuntan a `#` sin destino real — son placeholders, no un error nuevo, pero valdría la pena poner los links reales o quitarlos.
- No se detectó manejo de errores silencioso adicional al ya documentado (el `catch` de `/generate-promo` cae a un código fijo `ALAS7-2026`, que es un fallback intencional y visible, no un error silenciado).

**Problemas encontrados:**
1. **[Grave — seguridad]** Bearer token de MailerLite (JWT, ~no expira) y secreto `X-ALAS7-Secret` expuestos en texto plano en el JS público de `index.html`.
2. **[Menor]** Links de redes sociales en el footer (`Instagram`, `X`, `LinkedIn`) apuntan a `href="#"` sin destino real.
3. **[Limitación del auditor, no del sitio]** No se pudo verificar en vivo el 200 OK de `alas7.com`, ni las respuestas reales de Stripe/Railway, por bloqueo de red saliente en este entorno.

**Recomendación:** (no corregido, solo se documenta)
- Rotar el Bearer token de MailerLite y el secreto `X-ALAS7-Secret`, y mover ambos a un endpoint intermedio server-side (el proxy de Railway) en vez de exponerlos en el cliente — hoy cualquiera puede extraerlos del código fuente y llamar a MailerLite directamente.
- Poner URLs reales o quitar los íconos de redes sociales del footer.
- Confirmar manualmente (desde una máquina con salida a internet normal) que `alas7.com` responde 200 y que el checkout de Stripe y el endpoint de Railway están arriba, ya que este auditor no pudo verificarlo por la política de red del entorno.
