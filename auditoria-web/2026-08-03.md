# Auditoría web — 2026-08-03

## [08:12 CDMX] Auditor web

**Estado:** problemas encontrados

**Sitio en vivo:** No verificable desde este entorno. La política de red/egress de este sandbox bloquea las conexiones salientes a alas7.com, buy.stripe.com y al proxy de Railway (403 en el CONNECT del proxy de la organización, confirmado en `/root/.ccr/README.md` y en el status del proxy). Esto **no es evidencia de que el sitio esté caído** — es una limitación del entorno de auditoría, no un hallazgo sobre el sitio. Se necesita correr esta verificación desde una red sin esa restricción (o dar acceso de egress a este runner) para confirmar el código 200 real.

**Pago y promo:** El cableado en el código es correcto y consistente con lo documentado en CLAUDE.md:
- El botón Premium y el CTA del popup de promo apuntan al Stripe Payment Link real `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01` (`index.html:391`, `:361`, `:470`).
- El JS llama a `POST {API}/generate-promo` con el header `X-ALAS7-Secret` (`index.html:508`) y a `POST {API}/subscribe` con `Authorization: Bearer ...` (`index.html:447-451`), ambos contra `gentle-laughter-production-7f2c.up.railway.app`, igual que en la documentación.
- El `groups` enviado (`187372665902728776`) coincide con el group ID de MailerLite documentado en CLAUDE.md.
- No se pudo probar en vivo (ver bloqueo de red arriba), solo se verificó el código fuente.

**Hallazgo de seguridad (crítico):** ambas credenciales que protegen esos dos endpoints están hardcodeadas en texto plano en el JS del lado del cliente, es decir, visibles para cualquier visitante que abra "ver código fuente":
- `index.html:395` — un Bearer JWT completo de MailerLite, con `exp` fijado para el año ~2126 (prácticamente no expira).
- `index.html:393` — el secreto `X-ALAS7-Secret` (`alas7secret2026`) que protege `/generate-promo`.

Cualquier persona puede copiar estas dos credenciales del HTML público y llamar directamente a la API de MailerLite/Railway fuera del flujo normal de la landing — no solo para generar suscripciones o códigos de promo en volumen, sino potencialmente para lo que el alcance real de ese Bearer token permita en la cuenta de MailerLite (listar/exportar/borrar suscriptores, etc., dependiendo de los scopes del token). Verificado con `git log -S "BEARER ="` que esto se introdujo en el commit `cb6f7a9` (3 de julio, "Cut over index.html to the de-runtimed A LAS 7 redesign") y ha estado así en producción desde entonces — se debe tratar como comprometido desde esa fecha.

**Deploy Pages:** Al día. El build más reciente de GitHub Pages (run `28697306776`, 2026-07-04 06:11–06:12 UTC) completó `success` sobre el commit `535f932`, que es el HEAD actual de `main`. No hay ningún build atorado ni fallido pendiente ahora mismo (el problema del 3-jul de un build colgado 29 min y otro que no se disparó ya quedó resuelto por un build posterior exitoso).

**Integridad HTML/JS:**
- `node --check` sobre el `<script>` inline no reportó errores de sintaxis.
- El `CNAME` dice `alas7.com` (correcto, no `vitanews`).
- No se encontraron referencias a dominios inexistentes tipo `alas7.mx`.
- Los `href="#"` en la nav ("Gratis") y footer ("Suscríbete gratis") son intencionales — llevan `class="js-open-free"` con `preventDefault()` + apertura de modal, no están rotos.
- Los tres íconos de redes sociales en el footer (Instagram, X, LinkedIn — `index.html:321-323`) usan `href="#"` sin URL real ni handler de JS asociado: son enlaces muertos/placeholder, no llevan a ningún lado.

**Problemas encontrados:**
1. **[Crítico — seguridad]** Bearer JWT de MailerLite y secreto `X-ALAS7-Secret` hardcodeados en el JS público (`index.html:393,395`), expuestos en producción desde el 3 de julio.
2. **[Bloqueo del entorno]** Este sandbox no tiene salida de red hacia alas7.com / Stripe / Railway, así que la salud en vivo del sitio y los endpoints no se pudo confirmar hoy con una petición real.
3. **[Cosmético]** Íconos de redes sociales del footer sin URL real (`href="#"`).
4. **[Observación menor]** `showPromo()` traga errores de `fetch` silenciosamente y cae a un código fijo `'ALAS7-2026'` sin loguear nada (`index.html:506-513`) — hay fallback funcional, pero dificulta notar si `/generate-promo` empieza a fallar.

**Recomendación:** (1) Rotar de inmediato el Bearer JWT de MailerLite y el secreto `X-ALAS7-Secret`, y mover ambas credenciales al backend (el proxy de Railway debería inyectar el header él mismo en vez de que el navegador lo mande) en lugar de dejarlas en el HTML público. (2) Dar a este auditor (o a un runner con red normal) capacidad de golpear alas7.com/Stripe/Railway en vivo — hoy el sandbox lo bloquea y la sección de salud del sitio queda ciega. (3) Cuando haya tiempo, apuntar los íconos sociales a URLs reales o quitarlos. PERO NO LO ARREGLES — esto queda para que Alan decida.
