# Auditoría web — 2026-08-09

## [08:15 CDMX] Auditor web
**Estado:** problemas encontrados
**Sitio en vivo:** no verificable — el entorno de este auditor tiene el egreso de red bloqueado por política (403 del gateway) hacia alas7.com, el proxy de Railway y buy.stripe.com. No pude hacer una petición HTTP real. No hay evidencia de caída, pero tampoco confirmación de que responda 200; se recomienda una verificación manual o desde un entorno con salida a internet.
**Pago y promo:** ok en el código (verificación estática) / no verificable en vivo
**Deploy Pages:** al día
**Problemas encontrados:**
1. **[CRÍTICO] Credenciales expuestas en JS público.** En `index.html` línea 393 y 395, el bloque de configuración del script inline expone en texto plano:
   - `SECRET = 'alas7secret2026'` (enviado como header `X-ALAS7-Secret` al endpoint `/generate-promo`).
   - `BEARER = 'eyJ0eXAiOiJKV1Qi...'` — un JWT completo de la API de MailerLite (usado como `Authorization: Bearer ...` en el POST a `/subscribe`), con expiración codificada muy lejana (~año 2126, prácticamente no caduca).

   Cualquier visitante puede ver el código fuente de la página y copiar ambos secretos. Con el Bearer token podrían llamar directamente a la API de MailerLite (o al proxy de Railway) fuera del flujo de la landing — spam de suscripciones, agregar contactos arbitrarios al grupo, o abusar del generador de promos. Esto no es un problema de "si" sino de "hace cuánto está expuesto": ambos valores llevan commiteados desde el 4-jul-2026 (commit `cb6f7a9`, "Cut over index.html to the de-runtimed A LAS 7 redesign") y siguen intactos en el HEAD actual de `main`.

2. **Enlaces sociales sin URL real.** Los tres íconos de redes sociales en el footer (Instagram, X, LinkedIn — líneas 321-323) usan `href="#"` como placeholder permanente, sin JS que los intercepte ni URL de destino. No rompen nada, pero son enlaces muertos de cara al usuario.

3. Sin hallazgos de sintaxis: el bloque `<script>` inline de `index.html` pasa `node --check` sin errores.

4. Sin referencias a dominios inexistentes (`alas7.mx` u otros) en el HTML.

5. `CNAME` correcto: contiene únicamente `alas7.com`.

**Deploy Pages (detalle):** El último workflow run de `pages-build-deployment` (id `28697306776`) terminó en `success` el 2026-07-04T06:12:03Z, para el commit `535f932` — que es el HEAD actual de `main`. No hay commits a `main` posteriores a esa fecha, así que el sitio publicado debería coincidir con el último commit. El build fallido previo (`1c23348`, `failure`) quedó superado por el rebuild exitoso inmediato siguiente; no hay builds atorados ni pendientes ahora mismo.

**Integraciones (verificación estática del código, no en vivo):**
- Botón de pago: el enlace directo `id="promoCta"` (línea 361) y la constante `STRIPE` (línea 391) usados en `checkPremiumPrivacy()`/`showPromo()` apuntan correctamente a `https://buy.stripe.com/14AcN50xF8OM2OW4SsafS01`.
- Promo: `showPromo()` llama a `POST {API}/generate-promo` con el header `X-ALAS7-Secret`, cae a un código fijo `ALAS7-2026` si falla (ver nota de manejo de errores abajo).
- Suscripción: `submitFree()` llama a `POST {API}/subscribe` con `email`, `groups`, `referral_code` y el Bearer token expuesto (ver hallazgo #1).
- El endpoint base (`API`) sigue siendo `gentle-laughter-production-7f2c.up.railway.app` en ambos casos — consistente con lo documentado.

**Manejo de errores silencioso:** el `catch` de `showPromo()` (línea 511-513) absorbe cualquier fallo del endpoint `/generate-promo` y sustituye el código por `ALAS7-2026` sin dejar rastro (ni log, ni aviso al usuario). Si el endpoint de Railway cae, la promo seguiría "funcionando" de cara al usuario con un código genérico, y nadie se enteraría de la caída real del servicio a menos que revise esto manualmente. No es un bug nuevo, pero es un punto ciego de monitoreo.

**Recomendación:** (no corregido — solo aviso)
1. Prioridad alta: mover el Bearer token de MailerLite y el `X-ALAS7-Secret` fuera del JS público — deben vivir solo en el backend/proxy de Railway, nunca en el HTML servido al cliente. Si el token sigue siendo válido, considerar rotarlo en MailerLite.
2. Verificar manualmente (o desde un entorno con salida a internet) que alas7.com, el botón de Stripe y los endpoints de Railway respondan 200 en vivo, ya que este auditor no pudo confirmarlo por restricción de red del entorno.
3. Baja prioridad: darle URL real a los íconos sociales del footer o quitarlos si no hay cuentas activas.
4. Opcional: agregar algún tipo de registro (aunque sea `console.warn` o un beacon) en el catch de `/generate-promo` para poder detectar caídas del endpoint sin depender de que un usuario reporte el problema.
